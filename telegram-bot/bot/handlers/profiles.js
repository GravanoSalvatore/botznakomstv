// const RateLimiter = require("telegraf-ratelimit");
// const { default: PQueue } = require("p-queue");
// const NodeCache = require("node-cache");
// const fs = require('fs');
// const path = require('path');
// const zlib = require('zlib');
// // В самом начале profiles.js после импортов
// console.log('=== CACHE DEBUG ENABLED ===');

// // Счетчики
// global.cacheDebug = {
//     loadProfileCacheCalls: 0,
//     lazyLoadFullCacheCalls: 0,
//     getProfilesPageCalls: 0,
//     lastCallTimes: {}
// };

// // Оригинальные функции обернуть
// const originalLoadProfileCache = loadProfileCache;
// loadProfileCache = async function(db, loadFullCache) {
//     global.cacheDebug.loadProfileCacheCalls++;
//     global.cacheDebug.lastCallTimes.loadProfileCache = Date.now();
    
//     console.log(`🔢 [DEBUG] loadProfileCache #${global.cacheDebug.loadProfileCacheCalls}, type: ${loadFullCache ? 'FULL' : 'DEMO'}`);
    
//     return originalLoadProfileCache(db, loadFullCache);
// };

// // Каждые 5 минут логировать статистику
// setInterval(() => {
//     console.log('📊 [CACHE STATS]', {
//         calls: global.cacheDebug,
//         memory: process.memoryUsage(),
//         fullCacheLoaded: cacheManager.isFullCacheLoaded(),
//         demoCacheLoaded: cacheManager.isDemoCacheLoaded()
//     });
// }, 5 * 60 * 1000);
// // ===== УДАЛЕНИЕ LOCK ФАЙЛА ПРИ ЗАПУСКЕ =====
// const LOCK_FILE = path.join(__dirname, 'bot.lock');
// try {
//     if (fs.existsSync(LOCK_FILE)) {
//         console.log('🗑️ УДАЛЯЕМ LOCK ФАЙЛ ДЛЯ RENDER');
//         fs.unlinkSync(LOCK_FILE);
//     }
// } catch (error) {
//     console.log('⚠️ Не удалось удалить lock файл:', error.message);
// }

// // ===================== БЛОКИРОВКА ОТ МНОЖЕСТВЕННОГО ЗАПУСКА =====================
// if (fs.existsSync(LOCK_FILE)) {
//     const existingPid = fs.readFileSync(LOCK_FILE, 'utf8');
//     console.error(`❌ Бот уже запущен с PID: ${existingPid}`);
//     console.error('❌ Остановите предыдущий процесс или удалите файл bot.lock');
//     process.exit(1);
// }
// fs.writeFileSync(LOCK_FILE, process.pid.toString());
// process.on('exit', () => { 
//     if (fs.existsSync(LOCK_FILE)) fs.unlinkSync(LOCK_FILE); 
// });
// process.on('SIGINT', () => { 
//     if (fs.existsSync(LOCK_FILE)) fs.unlinkSync(LOCK_FILE); 
//     process.exit(0); 
// });

// // ===================== ГЛОБАЛЬНЫЕ ФЛАГИ ДЛЯ ПРЕДОТВРАЩЕНИЯ ПОВТОРНОЙ ЗАГРУЗКИ =====================
// let globalCacheInitialized = false;
// let fullCacheLoading = false;
// let fullCacheLoadPromise = null;
// let demoCacheLoading = false;
// let demoCacheLoadPromise = null;

// // ===================== ФУНКЦИЯ ПРЕЛОАДЕРА =====================
// // ===================== КРАСИВЫЙ ПРЕЛОАДЕР =====================
// const sendPreloader = async (ctx, type = "default", title = "✨Magic") => {
//     try {
//         const preloaderConfigs = {
//             'city': {
//                 title: "🔍 ПОИСК АНКЕТ",
//                 text: `🔍 <b>Поиск анкет в выбранном городе...</b>\n\n⏳ <i>База данных обновляется, это может занять до 2 минут</i>\n\n📊 <i>Ищем самые свежие анкеты для вас...</i>\n\n✨ <i>Пожалуйста, подождите</i>`
//             },
//             'country': {
//                 title: "🌍 ЗАГРУЗКА СТРАН", 
//                 text: `🌍 <b>Загружаем список стран и городов...</b>\n\n⏳ <i>Обновляем географию анкет</i>\n\n🌎 <i>Ищем анкеты со всего мира</i>\n\n✨ <i>Пожалуйста, подождите</i>`
//             },
//             'access': {
//                 title: "🔍 ПРОВЕРКА ДОСТУПА",
//                 text: `🔍 <b>Проверяем ваш доступ...</b>\n\n⏳ <i>Проверяем подписку и статус канала</i>\n\n✅ <i>Определяем режим работы</i>\n\n✨ <i>Пожалуйста, подождите</i>`
//             },
//             'default': {
//                 title: "✨Magic",
//                 text: `🔄 <b>${title}</b>\n\n⏳ <i>Пожалуйста, подождите...</i>\n\n✨ <i>Система готовится к работе</i>`
//             }
//         };

//         const config = preloaderConfigs[type] || preloaderConfigs.default;
        
//         const preloaderMsg = await ctx.reply(config.text, {
//             parse_mode: "HTML",
//             reply_markup: {
//                 inline_keyboard: [
//                     [{ text: "🔄 Загружается...", callback_data: "loading" }]
//                 ]
//             }
//         });
        
//         return preloaderMsg;
//     } catch (error) {
//         console.log("❌ Не удалось отправить прелоадер:", error.message);
//         return null;
//     }
// };
// // Функция для удаления прелоадера
// const removePreloader = async (ctx, preloaderMsg) => {
//     if (preloaderMsg) {
//         try {
//             await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
//         } catch (error) {
//             console.log("❌ Не удалось удалить прелоадер:", error.message);
//         }
//     }
// };

// // ===================== СИСТЕМА БЛОКИРОВКИ ПОЛЬЗОВАТЕЛЯ =====================
// const userLocks = new Map();

// const acquireUserLock = (userId, timeoutMs = 10000) => {
//     const now = Date.now();
//     const userLock = userLocks.get(userId);
    
//     if (userLock && now < userLock.expires) {
//         return false;
//     }
    
//     userLocks.set(userId, {
//         expires: now + timeoutMs,
//         timestamp: now
//     });
//     return true;
// };

// const releaseUserLock = (userId) => {
//     userLocks.delete(userId);
// };

// setInterval(() => {
//     const now = Date.now();
//     let cleanedCount = 0;
    
//     userLocks.forEach((lock, userId) => {
//         if (now >= lock.expires) {
//             userLocks.delete(userId);
//             cleanedCount++;
//         }
//     });
    
//     if (cleanedCount > 0) {
//         console.log(`🧹 [LOCKS] Очищено ${cleanedCount} старых блокировок`);
//     }
// }, 60000);

// // ===================== КОНФИГУРАЦИЯ МАСШТАБИРОВАНИЯ =====================
// const SCALING_CONFIG = {
//     MESSAGE_QUEUE: {
//         CONCURRENCY: 50,
//         INTERVAL: 1000,
//         INTERVAL_CAP: 200,
//         TIMEOUT: 30000,
//     },
    
//     CACHE: {
//         PROFILES_TTL: 7 * 24 * 60 * 60, // 7 дней
//         FILTERS_TTL: 600,
//         SESSIONS_TTL: 7 * 24 * 60 * 60, // УВЕЛИЧЕНО: 7 дней вместо 30 минут
//         SUBSCRIPTION_TTL: 24 * 60 * 60, // 24 часа для проверки подписки
//         CHANNEL_TTL: 5 * 60,                // ⚡ ВСЕГО 5 МИНУТ для канала! ⚡
//         MAX_FILTER_KEYS: 500,
//         CHECKPERIOD: 300,
//     },
    
//     PERFORMANCE: {
//         PROFILES_PER_PAGE: 1,
//         MAX_CAPTION_LENGTH: 900,
//         MESSAGE_TTL: 86400000,
//         FILTER_CHUNK_SIZE: 500,
//         MAX_CONCURRENT_FILTERS: 10,
//     }
// };

// // ===================== ВСПОМОГАТЕЛЬНЫЕ КОНСТАНТЫ =====================
// const AGE_RANGES = [
//     { label: "18-25", min: 18, max: 25 },
//     { label: "26-35", min: 26, max: 35 },
//     { label: "36-45", min: 36, max: 45 },
//     { label: "46+", min: 46, max: 999 },
// ];

// const POPULAR_COUNTRIES = [
//     { name: "Россия", flag: "🇷🇺" },
//     { name: "Украина", flag: "🇺🇦" },
//     { name: "Беларусь", flag: "🇧🇾" },
//     { name: "Казахстан", flag: "🇰🇿" },
//     { name: "Турция", flag: "🇹🇷" },
//     { name: "Германия", flag: "🇩🇪" },
//     { name: "США", flag: "🇺🇸" },
//     { name: "Израиль", flag: "🇮🇱" },
// ];

// const PAGINATION_JUMP_SECTIONS = [
//     { label: "1-1000", start: 0, end: 999 },
//     { label: "1000-2000", start: 1000, end: 1999 },
//     { label: "2000-3000", start: 2000, end: 2999 },
// ];

// // Карта нормализации украинских городов
// const cityNormalizationMap = {
//   kyiv: "Киев", kiev: "Киев", kiyv: "Киев",
//   "kryvyi rih": "Кривой Рог", "kryvyi rig": "Кривой Рог",
//   odesa: "Одесса", odessa: "Одесса",
//   kharkiv: "Харьков", lviv: "Львов", dnipro: "Днепр",
//   zaporizhzhia: "Запорожье", zaporozhye: "Запорожье",
//   vinnytsia: "Винница", vinnitsa: "Винница",
//   ternopil: "Тернополь",
//   khmelnytskyi: "Хмельницкий", khmelnitsky: "Хмельницкий",
//   cherkasy: "Черкассы", chernivtsi: "Черновцы", chernovtsy: "Черновцы",
//   "ivano-frankivsk": "Ивано-Франковск",
//   kropyvnytskyi: "Кропивницкий",
//   mykolaiv: "Николаев", nikolaev: "Николаев",
//   poltava: "Полтава", rivne: "Ровно", rovno: "Ровно",
//   sumy: "Сумы", uzhhorod: "Ужгород", zhytomyr: "Житомир",
//   kramatorsk: "Краматорск", sloviansk: "Славянск",
//   lutsk: "Луцк", kherson: "Херсон", bukovel: "Буковель",

//   київ: "Киев", "кривий ріг": "Кривой Рог", одеса: "Одесса",
//   харків: "Харьков", львів: "Львов", дніпро: "Днепр",
//   дніпропетровськ: "Днепр", запоріжжя: "Запорожье",
//   вінниця: "Винница", тернопіль: "Тернополь",
//   хмельницький: "Хмельницкий", черкаси: "Черкассы",
//   чернівці: "Черновцы", "івано-франківськ": "Ивано-Франковск",
//   кропивницький: "Кропивницкий", миколаїв: "Николаев",
//   полтава: "Полтава", рівне: "Ровно", суми: "Сумы",
//   ужгород: "Ужгород", житомир: "Житомир",
//   краматорськ: "Краматорск", "слов'янськ": "Славянск",
//   луцьк: "Луцк", херсон: "Херсон", буковель: "Буковель",
// };

// const countryNormalizationMap = {
//     // Существующие страны Европы
//     'russia': '🇷🇺 Россия', 'russian federation': '🇷🇺 Россия', 'rus': '🇷🇺 Россия',
//     'ukraine': '🇺🇦 Украина', 'ukr': '🇺🇦 Украина',
//     'belarus': '🇧🇾 Беларусь', 'belarus republic': '🇧🇾 Беларусь', 'blr': '🇧🇾 Беларусь',
//     'kazakhstan': '🇰🇿 Казахстан', 'kaz': '🇰🇿 Казахстан',
//     'germany': '🇩🇪 Германия', 'deutschland': '🇩🇪 Германия', 'deu': '🇩🇪 Германия', 'ger': '🇩🇪 Германия',
//     'france': '🇫🇷 Франция', 'french republic': '🇫🇷 Франция', 'fra': '🇫🇷 Франция',
//     'italy': '🇮🇹 Италия', 'italian republic': '🇮🇹 Италия', 'ita': '🇮🇹 Италия',
//     'spain': '🇪🇸 Испания', 'kingdom of spain': '🇪🇸 Испания', 'esp': '🇪🇸 Испания',
//     'united kingdom': '🇬🇧 Великобритания', 'great britain': '🇬🇧 Великобритания', 'england': '🇬🇧 Великобритания', 'gb': '🇬🇧 Великобритания', 'gbr': '🇬🇧 Великобритания',
//     'poland': '🇵🇱 Польша', 'republic of poland': '🇵🇱 Польша', 'pol': '🇵🇱 Польша',
//     'netherlands': '🇳🇱 Нидерланды', 'holland': '🇳🇱 Нидерланды', 'nld': '🇳🇱 Нидерланды',
//     'belgium': '🇧🇪 Бельгия', 'belgian': '🇧🇪 Бельгия', 'bel': '🇧🇪 Бельгия',
//     'switzerland': '🇨🇭 Швейцария', 'swiss': '🇨🇭 Швейцария', 'che': '🇨🇭 Швейцария',
//     'austria': '🇦🇹 Австрия', 'aut': '🇦🇹 Австрия',
//     'portugal': '🇵🇹 Португалия', 'prt': '🇵🇹 Португалия',
//     'greece': '🇬🇷 Греция', 'grc': '🇬🇷 Греция',
//     'czech republic': '🇨🇿 Чехия', 'czech': '🇨🇿 Чехия', 'cze': '🇨🇿 Чехия',
//     'sweden': '🇸🇪 Швеция', 'swe': '🇸🇪 Швеция',
//     'norway': '🇳🇴 Норвегия', 'nor': '🇳🇴 Норвегия',
//     'finland': '🇫🇮 Финляндия', 'fin': '🇫🇮 Финляндия',
//     'denmark': '🇩🇰 Дания', 'dnk': '🇩🇰 Дания',
//     'ireland': '🇮🇪 Ирландия', 'irl': '🇮🇪 Ирландия',
//     'hungary': '🇭🇺 Венгрия', 'hun': '🇭🇺 Венгрия',
//     'romania': '🇷🇴 Румыния', 'rou': '🇷🇴 Румыния',
//     'bulgaria': '🇧🇬 Болгария', 'bgr': '🇧🇬 Болгария',
//     'serbia': '🇷🇸 Сербия', 'srb': '🇷🇸 Сербия',
//     'croatia': '🇭🇷 Хорватия', 'hrv': '🇭🇷 Хорватия',
//     'slovakia': '🇸🇰 Словакия', 'svk': '🇸🇰 Словакия',
//     'slovenia': '🇸🇮 Словения', 'svn': '🇸🇮 Словения',
//     'lithuania': '🇱🇹 Литва', 'ltu': '🇱🇹 Литва',
//     'latvia': '🇱🇻 Латвия', 'lva': '🇱🇻 Латвия',
//     'estonia': '🇪🇪 Эстония', 'est': '🇪🇪 Эстония',
//     'moldova': '🇲🇩 Молдова', 'mda': '🇲🇩 Молдова',
//     'georgia': '🇬🇪 Грузия', 'geo': '🇬🇪 Грузия',
//     'armenia': '🇦🇲 Армения', 'arm': '🇦🇲 Армения',
//     'azerbaijan': '🇦🇿 Азербайджан', 'aze': '🇦🇿 Азербайджан',
//     'luxembourg': '🇱🇺 Люксембург', 'lux': '🇱🇺 Люксембург',
//     'malta': '🇲🇹 Мальта', 'mlt': '🇲🇹 Мальта',
//     'monaco': '🇲🇨 Монако', 'mco': '🇲🇨 Монако',
//     'montenegro': '🇲🇪 Черногория', 'mne': '🇲🇪 Черногория',
//     'iceland': '🇮🇸 Исландия', 'isl': '🇮🇸 Исландия',
//     'albania': '🇦🇱 Албания', 'alb': '🇦🇱 Албания',
//     'bosnia': '🇧🇦 Босния', 'bosnia and herzegovina': '🇧🇦 Босния', 'bih': '🇧🇦 Босния',
//     'cyprus': '🇨🇾 Кипр', 'cyp': '🇨🇾 Кипр',
//     'north macedonia': '🇲🇰 Северная Македония', 'macedonia': '🇲🇰 Северная Македония', 'mkd': '🇲🇰 Северная Македония',

//     // Африка
//     'south africa': '🇿🇦 ЮАР', 'south africa republic': '🇿🇦 ЮАР', 'zaf': '🇿🇦 ЮАР',
//     'egypt': '🇪🇬 Египет', 'egy': '🇪🇬 Египет',
//     'morocco': '🇲🇦 Марокко', 'mar': '🇲🇦 Марокко',
//     'algeria': '🇩🇿 Алжир', 'dza': '🇩🇿 Алжир',
//     'tunisia': '🇹🇳 Тунис', 'tun': '🇹🇳 Тунис',
//     'nigeria': '🇳🇬 Нигерия', 'nga': '🇳🇬 Нигерия',
//     'kenya': '🇰🇪 Кения', 'ken': '🇰🇪 Кения',
//     'ethiopia': '🇪🇹 Эфиопия', 'eth': '🇪🇹 Эфиопия',
//     'ghana': '🇬🇭 Гана', 'gha': '🇬🇭 Гана',
//     'tanzania': '🇹🇿 Танзания', 'tza': '🇹🇿 Танзания',
//     'uganda': '🇺🇬 Уганда', 'uga': '🇺🇬 Уганда',
//     'cameroon': '🇨🇲 Камерун', 'cmr': '🇨🇲 Камерун',
//     'ivory coast': '🇨🇮 Кот-д\'Ивуар', 'cote d\'ivoire': '🇨🇮 Кот-д\'Ивуар', 'civ': '🇨🇮 Кот-д\'Ивуар',
//     'senegal': '🇸🇳 Сенегал', 'sen': '🇸🇳 Сенегал',

//     // Северная и Южная Америка
//     'usa': '🇺🇸 США', 'united states': '🇺🇸 США', 'america': '🇺🇸 США', 'united states of america': '🇺🇸 США',
//     'canada': '🇨🇦 Канада', 'can': '🇨🇦 Канада',
//     'mexico': '🇲🇽 Мексика', 'mex': '🇲🇽 Мексика',
//     'brazil': '🇧🇷 Бразилия', 'bra': '🇧🇷 Бразилия',
//     'argentina': '🇦🇷 Аргентина', 'arg': '🇦🇷 Аргентина',
//     'colombia': '🇨🇴 Колумбия', 'col': '🇨🇴 Колумбия',
//     'chile': '🇨🇱 Чили', 'chl': '🇨🇱 Чили',
//     'peru': '🇵🇪 Перу', 'per': '🇵🇪 Перу',
//     'venezuela': '🇻🇪 Венесуэла', 'ven': '🇻🇪 Венесуэла',
//     'ecuador': '🇪🇨 Эквадор', 'ecu': '🇪🇨 Эквадор',
//     'bolivia': '🇧🇴 Боливия', 'bol': '🇧🇴 Боливия',
//     'paraguay': '🇵🇾 Парагвай', 'pry': '🇵🇾 Парагвай',
//     'uruguay': '🇺🇾 Уругвай', 'ury': '🇺🇾 Уругвай',
//     'cuba': '🇨🇺 Куба', 'cub': '🇨🇺 Куба',
//     'dominican republic': '🇩🇴 Доминикана', 'dom': '🇩🇴 Доминикана',
//     'puerto rico': '🇵🇷 Пуэрто-Рико', 'pri': '🇵🇷 Пуэрто-Рико',
//     'panama': '🇵🇦 Панама', 'pan': '🇵🇦 Панама',
//     'costa rica': '🇨🇷 Коста-Рика', 'cri': '🇨🇷 Коста-Рика',

//     // Азия
//     'china': '🇨🇳 Китай', 'chn': '🇨🇳 Китай',
//     'japan': '🇯🇵 Япония', 'jpn': '🇯🇵 Япония',
//     'south korea': '🇰🇷 Южная Корея', 'korea': '🇰🇷 Южная Корея', 'kor': '🇰🇷 Южная Корея',
//     'north korea': '🇰🇵 Северная Корея', 'prk': '🇰🇵 Северная Корея',
//     'india': '🇮🇳 Индия', 'ind': '🇮🇳 Индия',
//     'pakistan': '🇵🇰 Пакистан', 'pak': '🇵🇰 Пакистан',
//     'bangladesh': '🇧🇩 Бангладеш', 'bgd': '🇧🇩 Бангладеш',
//     'indonesia': '🇮🇩 Индонезия', 'idn': '🇮🇩 Индонезия',
//     'philippines': '🇵🇭 Филиппины', 'phl': '🇵🇭 Филиппины',
//     'vietnam': '🇻🇳 Вьетнам', 'vnm': '🇻🇳 Вьетнам',
//     'thailand': '🇹🇭 Таиланд', 'tha': '🇹🇭 Таиланд',
//     'malaysia': '🇲🇾 Малайзия', 'mys': '🇲🇾 Малайзия',
//     'singapore': '🇸🇬 Сингапур', 'sgp': '🇸🇬 Сингапур',
//     'myanmar': '🇲🇲 Мьянма', 'burma': '🇲🇲 Мьянма', 'mmr': '🇲🇲 Мьянма',
//     'sri lanka': '🇱🇰 Шри-Ланка', 'lka': '🇱🇰 Шри-Ланка',
//     'nepal': '🇳🇵 Непал', 'npl': '🇳🇵 Непал',
//     'afghanistan': '🇦🇫 Афганистан', 'afg': '🇦🇫 Афганистан',
//     'iran': '🇮🇷 Иран', 'irn': '🇮🇷 Иран',
//     'iraq': '🇮🇶 Ирак', 'irq': '🇮🇶 Ирак',
//     'saudi arabia': '🇸🇦 Саудовская Аравия', 'sau': '🇸🇦 Саудовская Аравия',
//     'united arab emirates': '🇦🇪 ОАЭ', 'uae': '🇦🇪 ОАЭ', 'are': '🇦🇪 ОАЭ',
//     'qatar': '🇶🇦 Катар', 'qat': '🇶🇦 Катар',
//     'kuwait': '🇰🇼 Кувейт', 'kwt': '🇰🇼 Кувейт',
//     'oman': '🇴🇲 Оман', 'omn': '🇴🇲 Оман',
//     'bahrain': '🇧🇭 Бахрейн', 'bhr': '🇧🇭 Бахрейн',
//     'jordan': '🇯🇴 Иордания', 'jor': '🇯🇴 Иордания',
//     'lebanon': '🇱🇧 Ливан', 'lbn': '🇱🇧 Ливан',
//     'syria': '🇸🇾 Сирия', 'syr': '🇸🇾 Сирия',
//     'yemen': '🇾🇪 Йемен', 'yem': '🇾🇪 Йемен',
//     'israel': '🇮🇱 Израиль', 'isr': '🇮🇱 Израиль',
//     'palestine': '🇵🇸 Палестина', 'pse': '🇵🇸 Палестина',
//     'turkey': '🇹🇷 Турция', 'tur': '🇹🇷 Турция',

//     // Центральная Азия
//     'uzbekistan': '🇺🇿 Узбекистан', 'uzb': '🇺🇿 Узбекистан',
//     'kyrgyzstan': '🇰🇬 Кыргызстан', 'kgz': '🇰🇬 Кыргызстан',
//     'tajikistan': '🇹🇯 Таджикистан', 'tjk': '🇹🇯 Таджикистан',
//     'turkmenistan': '🇹🇲 Туркменистан', 'tkm': '🇹🇲 Туркменистан',

//     // Океания
//     'australia': '🇦🇺 Австралия', 'aus': '🇦🇺 Австралия',
//     'new zealand': '🇳🇿 Новая Зеландия', 'nzl': '🇳🇿 Новая Зеландия',
//     'fiji': '🇫🇯 Фиджи', 'fji': '🇫🇯 Фиджи',
//     'papua new guinea': '🇵🇬 Папуа-Новая Гвинея', 'png': '🇵🇬 Папуа-Новая Гвинея',

//     // Карибские острова
//     'jamaica': '🇯🇲 Ямайка', 'jam': '🇯🇲 Ямайка',
//     'haiti': '🇭🇹 Гаити', 'hti': '🇭🇹 Гаити',
//     'trinidad and tobago': '🇹🇹 Тринидад и Тобаго', 'tto': '🇹🇹 Тринидад и Тобаго',
//     'bahamas': '🇧🇸 Багамы', 'bhs': '🇧🇸 Багамы',
//     'barbados': '🇧🇧 Барбадос', 'brb': '🇧🇧 Барбадос',

//     // Другие важные страны
//     'mongolia': '🇲🇳 Монголия', 'mng': '🇲🇳 Монголия',
//     'laos': '🇱🇦 Лаос', 'lao': '🇱🇦 Лаос',
//     'cambodia': '🇰🇭 Камбоджа', 'khm': '🇰🇭 Камбоджа',
//     'brunei': '🇧🇳 Бруней', 'brn': '🇧🇳 Бруней',
//     'east timor': '🇹🇱 Восточный Тимор', 'timor-leste': '🇹🇱 Восточный Тимор', 'tls': '🇹🇱 Восточный Тимор',
//     'madagascar': '🇲🇬 Мадагаскар', 'mdg': '🇲🇬 Мадагаскар',
//     'mozambique': '🇲🇿 Мозамбик', 'moz': '🇲🇿 Мозамбик',
//     'zimbabwe': '🇿🇼 Зимбабве', 'zwe': '🇿🇼 Зимбабве',
//     'zambia': '🇿🇲 Замбия', 'zmb': '🇿🇲 Замбия',
//     'botswana': '🇧🇼 Ботсвана', 'bwa': '🇧🇼 Ботсвана',
//     'namibia': '🇳🇦 Намибия', 'nam': '🇳🇦 Намибия',
//     'angola': '🇦🇴 Ангола', 'ago': '🇦🇴 Ангола',
//     'congo': '🇨🇬 Конго', 'cog': '🇨🇬 Конго',
//     'dr congo': '🇨🇩 ДР Конго', 'democratic republic of the congo': '🇨🇩 ДР Конго', 'cod': '🇨🇩 ДР Конго',
//     'rwanda': '🇷🇼 Руанда', 'rwa': '🇷🇼 Руанда',
//     'burundi': '🇧🇮 Бурунди', 'bdi': '🇧🇮 Бурунди',
//     'somalia': '🇸🇴 Сомали', 'som': '🇸🇴 Сомали',
//     'sudan': '🇸🇩 Судан', 'sdn': '🇸🇩 Судан',
//     'south sudan': '🇸🇸 Южный Судан', 'ssd': '🇸🇸 Южный Судан',
//     'libya': '🇱🇾 Ливия', 'lby': '🇱🇾 Ливия',
//     'mauritius': '🇲🇺 Маврикий', 'mus': '🇲🇺 Маврикий',
//     'seychelles': '🇸🇨 Сейшелы', 'syc': '🇸🇨 Сейшелы',
//     'comoros': '🇰🇲 Коморы', 'com': '🇰🇲 Коморы',
//     'djibouti': '🇩🇯 Джибути', 'dji': '🇩🇯 Джибути',
//     'eritrea': '🇪🇷 Эритрея', 'eri': '🇪🇷 Эритрея',
//     'gabon': '🇬🇦 Габон', 'gab': '🇬🇦 Габон',
//     'equatorial guinea': '🇬🇶 Экваториальная Гвинея', 'gnq': '🇬🇶 Экваториальная Гвинея',
//     'chad': '🇹🇩 Чад', 'tcd': '🇹🇩 Чад',
//     'niger': '🇳🇪 Нигер', 'ner': '🇳🇪 Нигер',
//     'mali': '🇲🇱 Мали', 'mli': '🇲🇱 Мали',
//     'burkina faso': '🇧🇫 Буркина-Фасо', 'bfa': '🇧🇫 Буркина-Фасо',
//     'benin': '🇧🇯 Бенин', 'ben': '🇧🇯 Бенин',
//     'togo': '🇹🇬 Того', 'tgo': '🇹🇬 Того',
//     'sierra leone': '🇸🇱 Сьерра-Леоне', 'sle': '🇸🇱 Сьерра-Леоне',
//     'liberia': '🇱🇷 Либерия', 'lbr': '🇱🇷 Либерия',
//     'guinea': '🇬🇳 Гвинея', 'gin': '🇬🇳 Гвинея',
//     'guinea-bissau': '🇬🇼 Гвинея-Бисау', 'gnb': '🇬🇼 Гвинея-Бисау',
//     'gambia': '🇬🇲 Гамбия', 'gmb': '🇬🇲 Гамбия',
//     'cape verde': '🇨🇻 Кабо-Верде', 'cpv': '🇨🇻 Кабо-Верде',
//     'sao tome and principe': '🇸🇸 Сан-Томе и Принсипи', 'stp': '🇸🇸 Сан-Томе и Принсипи',
//     'mauritania': '🇲🇷 Мавритания', 'mrt': '🇲🇷 Мавритания'
// };

// const russianVariants = {
//     'россия': '🇷🇺 Россия', 'рф': '🇷🇺 Россия',
//     'украина': '🇺🇦 Украина',
//     'беларусь': '🇧🇾 Беларусь', 'белоруссия': '🇧🇾 Беларусь',
//     'казахстан': '🇰🇿 Казахстан',
//     'германия': '🇩🇪 Германия',
//     'франция': '🇫🇷 Франция',
//     'италия': '🇮🇹 Италия',
//     'испания': '🇪🇸 Испания',
//     'великобритания': '🇬🇧 Великобритания', 'англия': '🇬🇧 Великобритания',
//     'польша': '🇵🇱 Польша',
//     'нидерланды': '🇳🇱 Нидерланды', 'голландия': '🇳🇱 Нидерланды',
//     'бельгия': '🇧🇪 Бельгия',
//     'швейцария': '🇨🇭 Швейцария',
//     'австрия': '🇦🇹 Австрия',
//     'португалия': '🇵🇹 Португалия',
//     'греция': '🇬🇷 Греция',
//     'чехия': '🇨🇿 Чехия',
//     'швеция': '🇸🇪 Швеция',
//     'норвегия': '🇳🇴 Норвегия',
//     'финляндия': '🇫🇮 Финляндия',
//     'дания': '🇩🇰 Дания',
//     'ирландия': '🇮🇪 Ирландия',
//     'венгрия': '🇭🇺 Венгрия',
//     'румыния': '🇷🇴 Румыния',
//     'болгария': '🇧🇬 Болгария',
//     'сербия': '🇷🇸 Сербия',
//     'хорватия': '🇭🇷 Хорватия',
//     'словакия': '🇸🇰 Словакия',
//     'словения': '🇸🇮 Словения',
//     'литва': '🇱🇹 Литва',
//     'латвия': '🇱🇻 Латвия',
//     'эстония': '🇪🇪 Эстония',
//     'молдова': '🇲🇩 Молдова', 'молдавия': '🇲🇩 Молдова',
//     'грузия': '🇬🇪 Грузия',
//     'армения': '🇦🇲 Армения',
//     'азербайджан': '🇦🇿 Азербайджан',
//     'сша': '🇺🇸 США', 'америка': '🇺🇸 США',
//     'канада': '🇨🇦 Канада',
//     'турция': '🇹🇷 Турция',
//     'израиль': '🇮🇱 Израиль',
//     'китай': '🇨🇳 Китай',
//     'япония': '🇯🇵 Япония',
//     'южная корея': '🇰🇷 Южная Корея', 'корея': '🇰🇷 Южная Корея',
//     'индия': '🇮🇳 Индия',
//     'бразилия': '🇧🇷 Бразилия',
//     'мексика': '🇲🇽 Мексика',
//     'австралия': '🇦🇺 Австралия',
//     'египет': '🇪🇬 Египет',
//     'таиланд': '🇹🇭 Таиланд',
//     'оаэ': '🇦🇪 ОАЭ', 'объединенные арабские эмираты': '🇦🇪 ОАЭ',
//     'узбекистан': '🇺🇿 Узбекистан',
//     'кыргызстан': '🇰🇬 Кыргызстан', 'киргизия': '🇰🇬 Кыргызстан',
//     'таджикистан': '🇹🇯 Таджикистан',
//     'туркменистан': '🇹🇲 Туркменистан',
    
//     // Новые добавления на русском
//     'юар': '🇿🇦 ЮАР', 'южная африка': '🇿🇦 ЮАР',
//     'албания': '🇦🇱 Албания',
//     'босния': '🇧🇦 Босния', 'босния и герцеговина': '🇧🇦 Босния',
//     'кипр': '🇨🇾 Кипр',
//     'македония': '🇲🇰 Северная Македония', 'северная македония': '🇲🇰 Северная Македония',
//     'марокко': '🇲🇦 Марокко',
//     'алжир': '🇩🇿 Алжир',
//     'тунис': '🇹🇳 Тунис',
//     'нигерия': '🇳🇬 Нигерия',
//     'кения': '🇰🇪 Кения',
//     'эфиопия': '🇪🇹 Эфиопия',
//     'гана': '🇬🇭 Гана',
//     'танзания': '🇹🇿 Танзания',
//     'уганда': '🇺🇬 Уганда',
//     'камерун': '🇨🇲 Камерун',
//     'кот-д\'ивуар': '🇨🇮 Кот-д\'Ивуар',
//     'сенегал': '🇸🇳 Сенегал',
//     'аргентина': '🇦🇷 Аргентина',
//     'колумбия': '🇨🇴 Колумбия',
//     'чили': '🇨🇱 Чили',
//     'перу': '🇵🇪 Перу',
//     'венесуэла': '🇻🇪 Венесуэла',
//     'эквадор': '🇪🇨 Эквадор',
//     'боливия': '🇧🇴 Боливия',
//     'парагвай': '🇵🇾 Парагвай',
//     'уругвай': '🇺🇾 Уругвай',
//     'куба': '🇨🇺 Куба',
//     'доминикана': '🇩🇴 Доминикана',
//     'пуэрто-рико': '🇵🇷 Пуэрто-Рико',
//     'панама': '🇵🇦 Панама',
//     'коста-рика': '🇨🇷 Коста-Рика',
//     'пакистан': '🇵🇰 Пакистан',
//     'бангладеш': '🇧🇩 Бангладеш',
//     'индонезия': '🇮🇩 Индонезия',
//     'филиппины': '🇵🇭 Филиппины',
//     'вьетнам': '🇻🇳 Вьетнам',
//     'малайзия': '🇲🇾 Малайзия',
//     'сингапур': '🇸🇬 Сингапур',
//     'мьянма': '🇲🇲 Мьянма',
//     'шри-ланка': '🇱🇰 Шри-Ланка',
//     'непал': '🇳🇵 Непал',
//     'афганистан': '🇦🇫 Афганистан',
//     'иран': '🇮🇷 Иран',
//     'ирак': '🇮🇶 Ирак',
//     'саудовская аравия': '🇸🇦 Саудовская Аравия',
//     'катар': '🇶🇦 Катар',
//     'кувейт': '🇰🇼 Кувейт',
//     'оман': '🇴🇲 Оман',
//     'бахрейн': '🇧🇭 Бахрейн',
//     'иордания': '🇯🇴 Иордания',
//     'ливан': '🇱🇧 Ливан',
//     'сирия': '🇸🇾 Сирия',
//     'йемен': '🇾🇪 Йемен',
//     'палестина': '🇵🇸 Палестина',
//     'новая зеландия': '🇳🇿 Новая Зеландия',
//     'фиджи': '🇫🇯 Фиджи',
//     'папуа-новая гвинея': '🇵🇬 Папуа-Новая Гвинея',
//     'ямайка': '🇯🇲 Ямайка',
//     'гаити': '🇭🇹 Гаити',
//     'тринидад и тобаго': '🇹🇹 Тринидад и Тобаго',
//     'багамы': '🇧🇸 Багамы',
//     'барбадос': '🇧🇧 Барбадос',
//     'монголия': '🇲🇳 Монголия',
//     'лаос': '🇱🇦 Лаос',
//     'камбоджа': '🇰🇭 Камбоджа',
//     'бруней': '🇧🇳 Бруней',
//     'восточный тимор': '🇹🇱 Восточный Тимор',
//     'мадагаскар': '🇲🇬 Мадагаскар',
//     'мозамбик': '🇲🇿 Мозамбик',
//     'зимбабве': '🇿🇼 Зимбабве',
//     'замбия': '🇿🇲 Замбия',
//     'ботсвана': '🇧🇼 Ботсвана',
//     'намибия': '🇳🇦 Намибия',
//     'ангола': '🇦🇴 Ангола',
//     'конго': '🇨🇬 Конго',
//     'др конго': '🇨🇩 ДР Конго',
//     'руанда': '🇷🇼 Руанда',
//     'бурунди': '🇧🇮 Бурунди',
//     'сомали': '🇸🇴 Сомали',
//     'судан': '🇸🇩 Судан',
//     'южный судан': '🇸🇸 Южный Судан',
//     'ливия': '🇱🇾 Ливия',
//     'маврикий': '🇲🇺 Маврикий',
//     'сейшелы': '🇸🇨 Сейшелы',
//     'коморы': '🇰🇲 Коморы',
//     'джибути': '🇩🇯 Джибути',
//     'эритрея': '🇪🇷 Эритрея',
//     'габон': '🇬🇦 Габон',
//     'экваториальная гвинея': '🇬🇶 Экваториальная Гвинея',
//     'чад': '🇹🇩 Чад',
//     'нигер': '🇳🇪 Нигер',
//     'мали': '🇲🇱 Мали',
//     'буркина-фасо': '🇧🇫 Буркина-Фасо',
//     'бенин': '🇧🇯 Бенин',
//     'того': '🇹🇬 Того',
//     'сьерра-леоне': '🇸🇱 Сьерра-Леоне',
//     'либерия': '🇱🇷 Либерия',
//     'гвинея': '🇬🇳 Гвинея',
//     'гвинея-бисау': '🇬🇼 Гвинея-Бисау',
//     'гамбия': '🇬🇲 Гамбия',
//     'кабо-верде': '🇨🇻 Кабо-Верде',
//     'сан-томе и принсипи': '🇸🇸 Сан-Томе и Принсипи',
//     'мавритания': '🇲🇷 Мавритания'
// };

// // Объединяем обе карты
// Object.assign(countryNormalizationMap, russianVariants);

// // ===================== ФУНКЦИЯ ОЧИСТКИ ABOUT ОТ ССЫЛОК =====================
// const replaceSitesInAbout = (aboutText) => {
//     if (!aboutText || typeof aboutText !== 'string') return aboutText;
    
//     const siteRegex = /[a-zA-Z0-9-]+\.\s*[a-zA-Z]{2,}/g;
//     const cleanedAbout = aboutText.replace(siteRegex, 'http://t.me/magicboss_bot/magic');
    
//     return cleanedAbout;
// };

// // ===================== ИНИЦИАЛИЗАЦИЯ СИСТЕМ КЭШИРОВАНИЯ =====================
// const profilesCache = new NodeCache({ 
//     stdTTL: SCALING_CONFIG.CACHE.PROFILES_TTL,
//     checkperiod: SCALING_CONFIG.CACHE.CHECKPERIOD,
//     useClones: false,
//     maxKeys: 80000
// });

// // ДЕМО-КЭШ для пользователей без подписки
// const demoCache = new NodeCache({
//     stdTTL: SCALING_CONFIG.CACHE.PROFILES_TTL, // 7 дней как и полный
//     checkperiod: SCALING_CONFIG.CACHE.CHECKPERIOD
// });

// // КЭШ для хранения информации о том, какой тип кэша загружен для каждого пользователя
// const userCacheStatus = new NodeCache({
//     stdTTL: SCALING_CONFIG.CACHE.SESSIONS_TTL, // 7 дней
//     checkperiod: SCALING_CONFIG.CACHE.CHECKPERIOD
// });

// // КЭШ ДЛЯ ПРОВЕРКИ ПОДПИСОК (СУПЕР-ВАЖНО!)
// const subscriptionCache = new NodeCache({
//     stdTTL: SCALING_CONFIG.CACHE.SUBSCRIPTION_TTL, // 24 часа
//     checkperiod: 600
// });

// // 🔥 ОБНОВЛЕННЫЙ КЭШ ДЛЯ КАНАЛА
// const channelSubscriptionCache = new NodeCache({
//     stdTTL: SCALING_CONFIG.CACHE.CHANNEL_TTL, // Теперь 5 минут
//     checkperiod: 60                           // Проверяем каждую минуту
// });

// class LimitedFilterCache {
//     constructor() {
//         this.keys = new Set();
//         this.maxKeys = SCALING_CONFIG.CACHE.MAX_FILTER_KEYS;
//     }
    
//     set(key, value) {
//         if (this.keys.size >= this.maxKeys) {
//             const firstKey = this.keys.values().next().value;
//             profilesCache.del(`filtered:${firstKey}`);
//             this.keys.delete(firstKey);
//         }
//         this.keys.add(key);
//         return profilesCache.set(`filtered:${key}`, value, SCALING_CONFIG.CACHE.FILTERS_TTL);
//     }
    
//     get(key) {
//         return profilesCache.get(`filtered:${key}`);
//     }
// }

// const filterCache = new LimitedFilterCache();

// // ===================== СИСТЕМА МОНИТОРИНГА =====================
// const readingStats = {
//     totalReads: 0,
//     operations: { 
//         profiles: 0, 
//         subscriptions: 0, 
//         channelSubscriptions: 0,
//         countries: 0,
//         cities: 0,
//         other: 0, 
//         cacheHits: 0, 
//         cacheMisses: 0,
//         firestoreReads: 0
//     },
//     timestamps: [],
//     users: new Map(),
    
//     addRead(operationType = 'other', userId = null, count = 1, source = 'firestore') {
//         this.totalReads += count;
//         this.operations[operationType] = (this.operations[operationType] || 0) + count;
        
//         if (source === 'firestore') {
//             this.operations.firestoreReads = (this.operations.firestoreReads || 0) + count;
//         }
        
//         this.timestamps.push({ 
//             time: Date.now(), 
//             type: operationType, 
//             count, 
//             userId,
//             source 
//         });
        
//         if (this.timestamps.length > 1000) {
//             this.timestamps = this.timestamps.slice(-500);
//         }
        
//         if (userId) {
//             if (!this.users.has(userId)) {
//                 this.users.set(userId, { total: 0, operations: {} });
//             }
//             const userStats = this.users.get(userId);
//             userStats.total += count;
//             userStats.operations[operationType] = (userStats.operations[operationType] || 0) + count;
//         }
        
//         console.log(`📖 [READ] ${operationType}: +${count} | Source: ${source} | Total: ${this.totalReads}`);
//     },
    
//     addCacheHit() { this.operations.cacheHits = (this.operations.cacheHits || 0) + 1; },
//     addCacheMiss() { this.operations.cacheMisses = (this.operations.cacheMisses || 0) + 1; },
    
//     getStats() {
//         const cacheEfficiency = this.operations.cacheHits + this.operations.cacheMisses > 0 
//             ? (this.operations.cacheHits / (this.operations.cacheHits + this.operations.cacheMisses)) * 100 
//             : 0;
            
//         return {
//             totalReads: this.totalReads,
//             operations: this.operations,
//             uniqueUsers: this.users.size,
//             readsPerUser: this.users.size > 0 ? this.totalReads / this.users.size : 0,
//             cacheEfficiency: `${cacheEfficiency.toFixed(2)}%`,
//             firestoreReads: this.operations.firestoreReads || 0,
//             timeline: this.timestamps.slice(-100)
//         };
//     },
    
//     resetStats() {
//         this.totalReads = 0;
//         this.operations = { 
//             profiles: 0, 
//             subscriptions: 0, 
//             channelSubscriptions: 0,
//             countries: 0,
//             cities: 0,
//             other: 0, 
//             cacheHits: 0, 
//             cacheMisses: 0,
//             firestoreReads: 0 
//         };
//         this.timestamps = [];
//         this.users.clear();
//     }
// };

// // ===================== ОПТИМИЗИРОВАННЫЙ КЭШ-МЕНЕДЖЕР =====================
// const cacheManager = {
//     async cacheProfiles(profiles, isDemo = false) {
//         try {
//             console.log(`🔄 [${isDemo ? 'DEMO' : 'FULL'} CACHE] Загрузка ${profiles.length} анкет...`);
            
//             const normalizedProfiles = profiles.map(profile => ({
//                 id: profile.id,
//                 n: profile.name,
//                 a: profile.age,
//                 c: this.normalizeCountryName(profile.country),
//                 ct: profile.city,
//                 ab: profile.about ? profile.about.substring(0, 500) : "",
//                 p: profile.photoUrl,
//                 phs: profile.photos || [],
//                 tg: profile.telegram,
//                 tel: profile.phone,
//                 wa: profile.whatsapp,
//                 ca: profile.createdAt
//             }));

//             // СЖАТИЕ ДАННЫХ
//             const jsonString = JSON.stringify(normalizedProfiles);
//             const compressed = zlib.gzipSync(jsonString);
            
//             if (isDemo) {
//                 demoCache.set("demo:profiles", compressed);
//             } else {
//                 profilesCache.set("profiles:all", compressed);
//             }

//             console.log(`✅ [CACHE] Сжатие: ${jsonString.length} → ${compressed.length} bytes (${Math.round((1 - compressed.length/jsonString.length) * 100)}% экономии)`);
            
//             // КЭШИРУЕМ СТРАНЫ И ГОРОДА
//             const countriesSet = new Set();
//             const citiesMap = new Map();

//             normalizedProfiles.forEach(profile => {
//                 if (profile.c) {
//                     countriesSet.add(profile.c);
//                     if (!citiesMap.has(profile.c)) {
//                         citiesMap.set(profile.c, new Set());
//                     }
//                     if (profile.ct) {
//                         citiesMap.get(profile.c).add(profile.ct);
//                     }
//                 }
//             });

//             // СОХРАНЯЕМ ДАННЫЕ СТРАН В ОТДЕЛЬНЫЙ КЭШ (БЕЗ СЖАТИЯ)
//             if (isDemo) {
//                 demoCache.set("demo:countries", Array.from(countriesSet).sort());
//                 demoCache.set("demo:countries_raw", Array.from(countriesSet));
//                 citiesMap.forEach((citiesSet, country) => {
//                     demoCache.set(`demo:cities:${country}`, Array.from(citiesSet).sort());
//                 });
//             } else {
//                 profilesCache.set("profiles:countries", Array.from(countriesSet).sort());
//                 profilesCache.set("profiles:countries_raw", Array.from(countriesSet));
//                 citiesMap.forEach((citiesSet, country) => {
//                     profilesCache.set(`profiles:cities:${country}`, Array.from(citiesSet).sort());
//                 });
//             }

//             if (!isDemo) {
//                 filterCache.keys.clear();
//             }
            
//             console.log(`✅ [${isDemo ? 'DEMO' : 'FULL'} CACHE] Обновлен: ${normalizedProfiles.length} профилей, ${countriesSet.size} стран`);
            
//         } catch (error) {
//             console.error(`❌ [${isDemo ? 'DEMO' : 'FULL'} CACHE] Ошибка:`, error);
//         }
//     },

//     cacheCountries(countries, isDemo = false) {
//         if (isDemo) {
//             demoCache.set("demo:countries", countries.sort());
//             demoCache.set("demo:countries_raw", countries);
//         } else {
//             profilesCache.set("profiles:countries", countries.sort());
//             profilesCache.set("profiles:countries_raw", countries);
//         }
//     },

//     cacheCities(country, cities, isDemo = false) {
//         if (isDemo) {
//             demoCache.set(`demo:cities:${country}`, cities.sort());
//         } else {
//             profilesCache.set(`profiles:cities:${country}`, cities.sort());
//         }
//     },

//     getCachedProfiles(isDemo = false) { 
//         try {
//             let compressed;
//             if (isDemo) {
//                 compressed = demoCache.get("demo:profiles");
//                 console.log(`🔍 [DEMO CACHE] Запрос демо-профилей: ${compressed ? 'сжатые данные найдены' : 'нет данных'}`);
//             } else {
//                 compressed = profilesCache.get("profiles:all");
//                 console.log(`🔍 [FULL CACHE] Запрос полных профилей: ${compressed ? 'сжатые данные найдены' : 'нет данных'}`);
//             }
            
//             if (!compressed) return null;
            
//             // РАСПАКОВКА ДАННЫХ
//             const decompressed = zlib.gunzipSync(compressed);
//             const profiles = JSON.parse(decompressed.toString());
            
//             console.log(`✅ [CACHE] Распаковано: ${profiles.length} профилей`);
//             return profiles;
            
//         } catch (error) {
//             console.error(`❌ [CACHE] Ошибка распаковки:`, error);
//             return null;
//         }
//     },

//     async createDemoCache(profiles) {
//         try {
//             console.log(`🔄 [DEMO CACHE] Создание демо-кэша из ${profiles.length} анкет...`);
            
//             const demoProfiles = [];
//             const citiesCounter = new Map(); 
//             const allCities = new Set();

//             profiles.forEach(profile => {
//                 console.log(`🏙️ [NORMALIZE] "${profile.city}" → "${this.normalizeCityName(profile.city)}"`);
//                 const normalizedCity = this.normalizeCityName(profile.city);
//                 const cityKey = `${profile.country}_${normalizedCity}`;
                
//                 if (!citiesCounter.has(cityKey)) {
//                     citiesCounter.set(cityKey, 0);
//                 }
                
//                 const currentCount = citiesCounter.get(cityKey);
                
//                 if (currentCount < 3) {
//                     citiesCounter.set(cityKey, currentCount + 1);
                    
//                     const demoProfile = {
//                         ...profile,
//                         city: normalizedCity,
//                         about: replaceSitesInAbout(profile.about),
//                         phone: null,
//                         telegram: null,
//                         whatsapp: null,
//                         isDemo: true
//                     };
//                     demoProfiles.push(demoProfile);
//                 }
//             });
            
//             console.log(`🔍 [DEMO CACHE ДИАГНОСТИКА]`);
//             console.log(`   - Всего уникальных городов в БД: ${allCities.size}`);
//             console.log(`   - Городов в демо-кэше: ${citiesCounter.size}`);
//             console.log(`   - Анкет в демо-кэше: ${demoProfiles.length}`);
            
//             const demoCities = Array.from(citiesCounter.keys());
//             console.log(`   - Примеры городов в демо-кэше: ${demoCities.slice(0, 10).join(', ')}`);
            
//             await this.cacheProfiles(demoProfiles, true);
            
//             console.log(`✅ [DEMO CACHE] Создан демо-кэш: ${demoProfiles.length} профилей из ${citiesCounter.size} городов`);
            
//             return demoProfiles;
            
//         } catch (error) {
//             console.error('❌ [DEMO CACHE] Ошибка создания демо-кэша:', error);
//             return [];
//         }
//     },

//     normalizeCityName(cityName) {
//         if (!cityName || typeof cityName !== 'string') return cityName;
//         const trimmedCity = cityName.trim();
//         if (trimmedCity.length === 0) return cityName;
        
//         const lowerCity = trimmedCity.toLowerCase();
//         if (cityNormalizationMap[lowerCity]) {
//             return cityNormalizationMap[lowerCity];
//         }
        
//         for (const [key, value] of Object.entries(cityNormalizationMap)) {
//             if (lowerCity.includes(key) || key.includes(lowerCity)) {
//                 return value;
//             }
//         }
        
//         return trimmedCity.charAt(0).toUpperCase() + trimmedCity.slice(1);
//     },

//     normalizeCountryName(countryName) {
//         if (!countryName || typeof countryName !== 'string') return countryName;
        
//         const trimmedCountry = countryName.trim();
//         if (trimmedCountry.length === 0) return countryName;
        
//         const lowerCountry = trimmedCountry.toLowerCase();
        
//         if (countryNormalizationMap[lowerCountry]) {
//             return countryNormalizationMap[lowerCountry];
//         }
        
//         for (const [key, value] of Object.entries(countryNormalizationMap)) {
//             if (lowerCountry.includes(key) || key.includes(lowerCountry)) {
//                 return value;
//             }
//         }
        
//         return trimmedCountry.charAt(0).toUpperCase() + trimmedCountry.slice(1);
//     },
    
//     getCachedCountries(isDemo = false) { 
//         if (isDemo) {
//             const countries = demoCache.get("demo:countries") || [];
//             console.log(`🔍 [DEMO CACHE] Запрос демо-стран: ${countries.length} стран`);
//             return countries;
//         } else {
//             const countries = profilesCache.get("profiles:countries") || [];
//             console.log(`🔍 [FULL CACHE] Запрос полных стран: ${countries.length} стран`);
//             return countries;
//         }
//     },
    
//     getCachedCities(country, isDemo = false) { 
//         if (isDemo) {
//             const cities = demoCache.get(`demo:cities:${country}`) || [];
//             console.log(`🔍 [DEMO CACHE] Запрос демо-городов для ${country}: ${cities.length} городов`);
//             return cities;
//         } else {
//             const cities = profilesCache.get(`profiles:cities:${country}`) || [];
//             console.log(`🔍 [FULL CACHE] Запрос полных городов для ${country}: ${cities.length} городов`);
//             return cities;
//         }
//     },
    
//     // ОПТИМИЗИРОВАННОЕ КЭШИРОВАНИЕ ПОДПИСОК (24 часа)
//     cacheSubscription(userId, isActive) { 
//         console.log(`💾 [SUBSCRIPTION CACHE] Сохранение подписки для ${userId}: ${isActive} на 24 часа`);
//         return subscriptionCache.set(`subscription:${userId}`, isActive, SCALING_CONFIG.CACHE.SUBSCRIPTION_TTL); 
//     },
    
//     getCachedSubscription(userId) { 
//         const subscription = subscriptionCache.get(`subscription:${userId}`);
//         console.log(`🔍 [SUBSCRIPTION CACHE] Запрос подписки для ${userId}: ${subscription !== undefined ? subscription : 'нет в кэше'}`);
//         return subscription;
//     },
    
//     // КЭШ ДЛЯ ПРОВЕРКИ ПОДПИСКИ НА КАНАЛ (24 часа)
//     cacheChannelSubscription(userId, isSubscribed) {
//         console.log(`💾 [CHANNEL CACHE] Сохранение проверки канала для ${userId}: ${isSubscribed}`);
//         return channelSubscriptionCache.set(`channel:${userId}`, isSubscribed, SCALING_CONFIG.CACHE.SUBSCRIPTION_TTL);
//     },
    
//     getCachedChannelSubscription(userId) {
//         const subscribed = channelSubscriptionCache.get(`channel:${userId}`);
//         console.log(`🔍 [CHANNEL CACHE] Запрос проверки канала для ${userId}: ${subscribed !== undefined ? subscribed : 'нет в кэше'}`);
//         return subscribed;
//     },
    
//     cacheFilteredProfiles(filterKey, profiles) { 
//         console.log(`💾 [CACHE] Сохранение фильтрованных профилей: ${filterKey} (${profiles.length} профилей)`);
//         filterCache.set(filterKey, profiles); 
//         readingStats.addCacheHit(); 
//     },
    
//     getCachedFilteredProfiles(filterKey) { 
//         const result = filterCache.get(filterKey); 
//         console.log(`🔍 [CACHE] Запрос фильтрованных профилей: ${filterKey} (${result ? result.length : 0} профилей)`);
//         if (result) readingStats.addCacheHit(); else readingStats.addCacheMiss();
//         return result;
//     },
    
//     cacheDemoFilteredProfiles(filterKey, profiles) { 
//         console.log(`💾 [DEMO CACHE] Сохранение демо-фильтра: ${filterKey} (${profiles.length} профилей)`);
//         demoCache.set(`filtered:${filterKey}`, profiles); 
//     },
    
//     getCachedDemoFilteredProfiles(filterKey) { 
//         const result = demoCache.get(`filtered:${filterKey}`);
//         console.log(`🔍 [DEMO CACHE] Запрос демо-фильтра: ${filterKey} (${result ? result.length : 0} профилей)`);
//         return result;
//     },
    
//     setUserCacheStatus(userId, cacheType) {
//         console.log(`💾 [USER CACHE] Установка статуса кэша для ${userId}: ${cacheType}`);
//         userCacheStatus.set(`cache_status:${userId}`, cacheType, SCALING_CONFIG.CACHE.SESSIONS_TTL);
//     },
    
//     getUserCacheStatus(userId) {
//         const status = userCacheStatus.get(`cache_status:${userId}`);
//         console.log(`🔍 [USER CACHE] Статус кэша для ${userId}: ${status}`);
//         return status;
//     },
    
//     isFullCacheLoaded() {
//         const fullProfiles = this.getCachedProfiles(false);
//         return !!(fullProfiles && fullProfiles.length > 0);
//     },
    
//     isDemoCacheLoaded() {
//         const demoProfiles = this.getCachedProfiles(true);
//         return !!(demoProfiles && demoProfiles.length > 0);
//     },
    
//     getCacheStats() {
//         return {
//             profilesCount: profilesCache.get("profiles:all")?.length || 0,
//             demoProfilesCount: demoCache.get("demo:profiles")?.length || 0,
//             filterKeysCount: filterCache.keys.size,
//             subscriptionCacheCount: subscriptionCache.keys().length,
//             channelCacheCount: channelSubscriptionCache.keys().length,
//             userCacheStatusCount: userCacheStatus.keys().length,
//             filterCacheLimit: SCALING_CONFIG.CACHE.MAX_FILTER_KEYS,
//             fullCacheLoaded: this.isFullCacheLoaded(),
//             demoCacheLoaded: this.isDemoCacheLoaded()
//         };
//     },
    
//     getCacheMemoryUsage() {
//         const fullCompressed = profilesCache.get("profiles:all");
//         const demoCompressed = demoCache.get("demo:profiles");
        
//         let fullSize = 0;
//         let demoSize = 0;
        
//         if (fullCompressed) {
//             try {
//                 const decompressed = zlib.gunzipSync(fullCompressed);
//                 const originalSize = decompressed.length;
//                 const compressedSize = fullCompressed.length;
//                 fullSize = compressedSize;
//             } catch (e) {}
//         }
        
//         if (demoCompressed) {
//             try {
//                 const decompressed = zlib.gunzipSync(demoCompressed);
//                 const originalSize = decompressed.length;
//                 const compressedSize = demoCompressed.length;
//                 demoSize = compressedSize;
//             } catch (e) {}
//         }
        
//         return {
//             fullCacheSize: fullSize,
//             demoCacheSize: demoSize,
//             totalKeys: profilesCache.keys().length + demoCache.keys().length,
//             memoryUsage: process.memoryUsage()
//         };
//     }
// };

// // ===================== ОПТИМИЗИРОВАННАЯ СИСТЕМА ФИЛЬТРАЦИИ =====================
// class AsyncFilterManager {
//     constructor() {
//         this.filterQueue = new PQueue({
//             concurrency: SCALING_CONFIG.PERFORMANCE.MAX_CONCURRENT_FILTERS,
//             timeout: SCALING_CONFIG.MESSAGE_QUEUE.TIMEOUT
//         });
//     }
    
//     async filterProfilesAsync(profiles, filters, isDemo = false) {
//         return this.filterQueue.add(async () => {
//             console.log(`🔍 [FILTER] Фильтрация ${profiles.length} профилей (демо: ${isDemo})`);
            
//             const chunkSize = SCALING_CONFIG.PERFORMANCE.FILTER_CHUNK_SIZE;
//             const results = [];
            
//             for (let i = 0; i < profiles.length; i += chunkSize) {
//                 const chunk = profiles.slice(i, i + chunkSize);
//                 const filteredChunk = this.applyFiltersToChunk(chunk, filters);
//                 results.push(...filteredChunk);
                
//                 if (i % (chunkSize * 2) === 0) {
//                     await new Promise(resolve => setImmediate(resolve));
//                 }
//             }
            
//             console.log(`✅ [FILTER] Завершено: ${results.length} результатов`);
//             return results;
//         });
//     }
    
//     applyFiltersToChunk(chunk, filters) {
//         return chunk.filter(profile => {
//             const fullProfile = {
//                 id: profile.id,
//                 name: profile.n,
//                 age: profile.a,
//                 country: profile.c,
//                 city: profile.ct,
//                 about: profile.ab,
//                 photoUrl: profile.p,
//                 photos: profile.phs,
//                 telegram: profile.tg,
//                 phone: profile.tel,
//                 whatsapp: profile.wa,
//                 createdAt: profile.ca
//             };

//             if (filters.country && fullProfile.country !== filters.country) return false;
//             if (filters.city && fullProfile.city !== filters.city) return false;
//             if (filters.ageRange) {
//                 const age = parseInt(fullProfile.age) || 0;
//                 if (age < filters.ageRange.min || age > filters.ageRange.max) return false;
//             }
//             return true;
//         });
//     }
// }

// const asyncFilterManager = new AsyncFilterManager();

// // ===================== ГЛАВНЫЙ МОДУЛЬ БОТА =====================
// module.exports = (bot, db) => {
//     let startModule = null;
//     if (!global.cacheLoadStats) {
//         global.cacheLoadStats = {
//             fullCacheLoads: 0,
//             demoCacheLoads: 0,
//             lastFullCacheLoad: 0,
//             lastDemoCacheLoad: 0,
//             loadErrors: 0
//         };
//     }
    
//     // ОЧЕРЕДЬ СООБЩЕНИЙ ДЛЯ МАСШТАБИРОВАНИЯ
//     const messageQueue = new PQueue({
//         concurrency: SCALING_CONFIG.MESSAGE_QUEUE.CONCURRENCY,
//         interval: SCALING_CONFIG.MESSAGE_QUEUE.INTERVAL,
//         intervalCap: SCALING_CONFIG.MESSAGE_QUEUE.INTERVAL_CAP,
//         timeout: SCALING_CONFIG.MESSAGE_QUEUE.TIMEOUT,
//         throwOnTimeout: false
//     });

//     messageQueue.on('active', () => {
//         if (messageQueue.size > 10) {
//             console.log(`📊 [QUEUE] Активные: ${messageQueue.pending} | Ожидание: ${messageQueue.size}`);
//         }
//     });

//     // Rate Limiter для защиты от спама
//     const limiter = new RateLimiter({
//         window: 1000,
//         limit: 8,
//         keyGenerator: (ctx) => `${ctx.from.id}:${ctx.updateType}`,
//         onLimitExceeded: (ctx) => {
//             console.log(`⚠️ [RATE LIMIT] Лимит для ${ctx.from.id}`);
//             return ctx.reply("⚠️ Слишком много запросов, подождите...");
//         },
//     });

//     bot.use(limiter);

//     // ===================== ОПТИМИЗИРОВАННЫЕ ФУНКЦИИ ПРОВЕРКИ ПОДПИСОК =====================
    
//     // 1. ФУНКЦИЯ ПРОВЕРКИ ПОДПИСКИ НА КАНАЛ (с кэшем на 24 часа)
//     const checkChannelSubscription = async (ctx) => {
//         const userId = ctx.from.id;
        
//         try {
//             // Проверяем кэш ПЕРВЫМ делом
//             const cached = cacheManager.getCachedChannelSubscription(userId);
//             if (cached !== undefined) {
//                 console.log(`✅ [CHANNEL CACHE HIT] Пользователь ${userId}: ${cached ? 'подписан' : 'не подписан'}`);
//                 readingStats.addRead('channelSubscriptions', userId, 1, 'cache');
//                 return cached;
//             }
            
//             console.log(`🔍 [CHANNEL CACHE MISS] Проверяем подписку на канал для ${userId}`);
//             const channelUsername = "@MagicYourClub";
            
//             const chatMember = await ctx.telegram.getChatMember(channelUsername, userId);
            
//             const isSubscribed = 
//                 chatMember.status === 'member' || 
//                 chatMember.status === 'administrator' || 
//                 chatMember.status === 'creator';
            
//             // Сохраняем в кэш на 24 часа
//             cacheManager.cacheChannelSubscription(userId, isSubscribed);
//             readingStats.addRead('channelSubscriptions', userId, 1, 'telegram_api');
            
//             console.log(`✅ [CHANNEL] Пользователь ${userId}: ${isSubscribed ? 'подписан' : 'не подписан'}`);
            
//             return isSubscribed;
//         } catch (error) {
//             console.error(`❌ [CHANNEL] Ошибка проверки подписки на канал для ${userId}:`, error);
            
//             // При ошибке считаем, что не подписан, но кэшируем на 1 час
//             cacheManager.cacheChannelSubscription(userId, false);
//             return false;
//         }
//     };

//     // 2. ФУНКЦИЯ ПРОВЕРКИ ПОДПИСКИ (с кэшем на 24 часа)
//     const checkSubscription = async (userId) => {
//         try {
//             console.log(`🔍 [SUBSCRIPTION] Проверка подписки для пользователя ${userId}`);
            
//             // Сначала пробуем получить из кэша (24 часа)
//             const cachedSubscription = cacheManager.getCachedSubscription(userId);
//             if (cachedSubscription !== undefined) {
//                 console.log(`✅ [SUBSCRIPTION CACHE HIT] Подписка из кэша: ${cachedSubscription}`);
//                 readingStats.addRead('subscriptions', userId, 1, 'cache');
//                 return cachedSubscription;
//             }
            
//             console.log(`🔍 [SUBSCRIPTION CACHE MISS] Запрашиваем из Firestore для ${userId}`);
//             readingStats.addRead('subscriptions', userId, 1, 'firestore');
            
//             const subRef = db.collection('subscriptions').doc(userId.toString());
//             const doc = await subRef.get();
            
//             if (!doc.exists) {
//                 console.log(`❌ [SUBSCRIPTION] Подписка не найдена в БД для ${userId}`);
//                 cacheManager.cacheSubscription(userId, false);
//                 return false;
//             }
            
//             const subData = doc.data();
//             const isActive = subData.isActive && subData.endDate.toDate() > new Date();
            
//             console.log(`✅ [SUBSCRIPTION] Подписка из БД для ${userId}: ${isActive}`);
            
//             cacheManager.cacheSubscription(userId, isActive);
//             return isActive;
            
//         } catch (error) {
//             console.error('❌ Ошибка проверки подписки:', error);
            
//             // При ошибке считаем, что подписки нет, но кэшируем на 1 час
//             cacheManager.cacheSubscription(userId, false);
//             return false;
//         }
//     };

//     // ===================== ФУНКЦИЯ ПАГИНИРОВАННОЙ ЗАГРУЗКИ КЭША =====================
// async function loadProfileCache(db, loadFullCache = false) {
//     try {
//         console.log(`🔄 [CACHE] Загрузка ${loadFullCache ? 'ПОЛНОГО' : 'ДЕМО'} кэша...`);
        
//         if (loadFullCache) {
//             // 🔥 ПАГИНИРОВАННАЯ ЗАГРУЗКА ПОЛНОГО КЭША
//             console.log(`🚀 [FULL CACHE PAGINATED] Начинаем пагинированную загрузку...`);
            
//             let allProfiles = [];
//             let lastDoc = null;
//             let batchCount = 0;
//             const BATCH_SIZE = 5000; // 🔥 5000 анкет за раз
//             const MAX_PROFILES = 70000; // 🔥 Максимум 70к анкет
            
//             while (allProfiles.length < MAX_PROFILES) {
//                 batchCount++;
//                 console.log(`📦 [BATCH ${batchCount}] Загружаем пачку ${BATCH_SIZE} анкет...`);
                
//                 let query = db.collection("profiles")
//                     .orderBy("createdAt", "desc")
//                     .limit(BATCH_SIZE)
//                     .select("id", "name", "age", "country", "city", "about", "photoUrl", "telegram", "phone", "whatsapp", "photos", "createdAt");
                
//                 if (lastDoc) {
//                     query = query.startAfter(lastDoc);
//                 }
                
//                 const snapshot = await query.get();
                
//                 if (snapshot.empty) {
//                     console.log(`✅ [FULL CACHE] Больше анкет нет`);
//                     break;
//                 }
                
//                 const batchProfiles = snapshot.docs.map(doc => ({
//                     id: doc.id,
//                     ...doc.data()
//                 }));
                
//                 allProfiles.push(...batchProfiles);
//                 lastDoc = snapshot.docs[snapshot.docs.length - 1];
                
//                 console.log(`📊 [BATCH ${batchCount}] Загружено ${batchProfiles.length} анкет, всего: ${allProfiles.length}`);
                
//                 // 🔥 КЭШИРУЕМ КАЖДУЮ ПАЧКУ СРАЗУ
//                 await cacheManager.cacheProfiles(allProfiles, false);
//                 console.log(`💾 [BATCH ${batchCount}] Кэшировано ${allProfiles.length} анкет`);
                
//                 // 🔥 ПАУЗА МЕЖДУ ПАЧКАМИ (чтобы не перегружать Firestore)
//                 if (snapshot.docs.length === BATCH_SIZE && allProfiles.length < MAX_PROFILES) {
//                     await new Promise(resolve => setTimeout(resolve, 1000)); // 1 секунда паузы
//                 }
//             }
            
//             console.log(`✅ [FULL CACHE PAGINATED] Завершено! Всего загружено: ${allProfiles.length} анкет`);
//             return allProfiles.length;
            
//         } else {
//             // 🔥 ДЕМО-КЭШ - ТОЛЬКО 5000 САМЫХ СВЕЖИХ
//             console.log(`📊 [DEMO CACHE] Загружаем 5000 свежих анкет для демо`);
            
//             const snapshot = await db.collection("profiles")
//                 .orderBy("createdAt", "desc")
//                 .limit(5000)
//                 .select("id", "name", "age", "country", "city", "about", "photoUrl", "telegram", "phone", "whatsapp", "photos", "createdAt")
//                 .get();

//             const allProfiles = snapshot.docs.map(doc => ({
//                 id: doc.id,
//                 ...doc.data()
//             }));

//             console.log(`📊 [DEMO CACHE] Получено ${allProfiles.length} анкет для демо-кэша`);
            
//             // Создаем демо-кэш
//             await cacheManager.createDemoCache(allProfiles);
//             console.log(`✅ [DEMO CACHE] Создан демо-кэш из ${allProfiles.length} анкет`);
            
//             return allProfiles.length;
//         }
        
//     } catch (error) {
//         console.error(`❌ [CACHE] Ошибка:`, error.message);
//         throw error;
//     }
// }
//     /// ===================== ФУНКЦИЯ ЛЕНИВОЙ ЗАГРУЗКИ ПОЛНОГО КЭША =====================
// const lazyLoadFullCache = async (userId = 'system', force = false) => {
//     // 🔥 ПРОВЕРЯЕМ, НЕ ИДЕТ ЛИ УЖЕ ЗАГРУЗКА
//     if (fullCacheLoading) {
//         console.log(`⏳ [LAZY LOAD] Полный кэш уже загружается, ждем...`);
//         if (fullCacheLoadPromise) {
//             try {
//                 await fullCacheLoadPromise;
//                 console.log(`✅ [LAZY LOAD] Дождались загрузки полного кэша`);
//                 return true;
//             } catch (error) {
//                 console.error(`❌ [LAZY LOAD] Ошибка ожидания загрузки:`, error.message);
//                 return false;
//             }
//         }
//         return false;
//     }
    
//     // 🔥 ЕСЛИ ПОЛНЫЙ КЭШ УЖЕ ЗАГРУЖЕН
//     if (!force && cacheManager.isFullCacheLoaded()) {
//         console.log(`✅ [LAZY LOAD] Полный кэш уже загружен`);
//         return true;
//     }
    
//     console.log(`🚀 [LAZY LOAD] Запускаем пагинированную загрузку полного кэша (инициатор: ${userId})...`);
    
//     fullCacheLoading = true;
//     fullCacheLoadPromise = (async () => {
//         try {
//             // 🔥 🔥 🔥 УВЕЛИЧИВАЕМ ТАЙМАУТ ДО 5 МИНУТ (300 секунд)!
//             const timeoutPromise = new Promise((_, reject) => {
//                 setTimeout(() => reject(new Error('Таймаут загрузки полного кэша (300 секунд)')), 300000);
//             });
            
//             const loadPromise = loadProfileCache(db, true);
            
//             // 🔥 ЖДЕМ С УВЕЛИЧЕННЫМ ТАЙМАУТОМ
//             await Promise.race([loadPromise, timeoutPromise]);
            
//             console.log(`✅ [LAZY LOAD] Полный кэш успешно загружен (пагинированно)`);
//             return true;
            
//         } catch (error) {
//             console.error(`❌ [LAZY LOAD] Ошибка загрузки полного кэша:`, error.message);
            
//             // 🔥 🔥 🔥 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: ЕСЛИ НЕ УДАЛОСЬ ЗАГРУЖИТЬ ПОЛНЫЙ КЭШ
//             // ИСПОЛЬЗУЕМ ДЕМО-КЭШ КАК РЕЗЕРВНУЮ КОПИЮ
//             if (!cacheManager.isDemoCacheLoaded()) {
//                 console.log(`⚠️ [LAZY LOAD] Демо-кэш не загружен, загружаем...`);
//                 try {
//                     await loadProfileCache(db, false);
//                     console.log(`✅ [LAZY LOAD] Демо-кэш загружен как резерв`);
//                 } catch (demoError) {
//                     console.error(`💥 [LAZY LOAD] Не удалось загрузить даже демо-кэш:`, demoError.message);
//                 }
//             }
            
//             throw error; // Пробрасываем ошибку дальше
            
//         } finally {
//             fullCacheLoading = false;
//             fullCacheLoadPromise = null;
//         }
//     })();
    
//     try {
//         await fullCacheLoadPromise;
//         return true;
//     } catch (error) {
//         throw error;
//     }
// };

   
// // ===================== ОПТИМИЗИРОВАННАЯ ФУНКЦИЯ ПРОВЕРКИ И ОБНОВЛЕНИЯ КЭША =====================
// const ensureProperCache = async (ctx, forceRefresh = false) => {
//     const userId = ctx.from.id;
    
//     console.log(`🔍 [ENSURE CACHE] Проверка для ${userId} (force: ${forceRefresh})`);
    
//     // 🔥 ВСЕГДА ОЧИЩАЕМ СТАРЫЕ КЭШИ ПРИ ПРОВЕРКЕ
//     if (forceRefresh) {
//         subscriptionCache.del(`subscription:${userId}`);
//         channelSubscriptionCache.del(`channel:${userId}`);
//     }
    
//     // 🔥 ВСЕГДА ПРОВЕРЯЕМ ПОДПИСКУ И КАНАЛ ЗАНОВО
//     const hasSubscription = await checkSubscription(userId);
//     const hasChannelSubscription = await checkChannelSubscription(ctx);
//     const hasFullAccess = hasSubscription && hasChannelSubscription;
    
//     const cacheType = hasFullAccess ? 'full' : 'demo';
    
//     console.log(`✅ [FRESH CHECK] ${userId}: подписка=${hasSubscription}, канал=${hasChannelSubscription}, доступ=${cacheType}`);
    
//     // Сохраняем статус
//     cacheManager.setUserCacheStatus(userId, cacheType);
    
//     // Обновляем сессию
//     if (!ctx.session) ctx.session = {};
//     ctx.session.fullAccessCache = {
//         value: hasFullAccess,
//         timestamp: Date.now(),
//         subscription: hasSubscription,
//         channel: hasChannelSubscription
//     };
    
//     ctx.session.cacheSession = {
//         type: cacheType,
//         timestamp: Date.now(),
//         fullAccess: hasFullAccess
//     };
    
//     return cacheType;
// };
    
//     // ===================== ФУНКЦИЯ ПРОВЕРКИ ПОЛНОГО ДОСТУПА (profiles.js) =====================
// const checkFullAccess = async (ctx, forceRefresh = false) => {
//     const userId = ctx.from.id;
    
//     // Используем сессионное кэширование
//     if (!forceRefresh && ctx.session?.fullAccessCache) {
//         const accessCache = ctx.session.fullAccessCache;
//         const accessAge = Date.now() - accessCache.timestamp;
        
//         // Кэш действителен 10 минут для канала, 60 минут для подписки
//         if (accessAge < 10 * 60 * 1000) {
//             console.log(`✅ [FULL ACCESS CACHE] Используем кэш для ${userId}: ${accessCache.value}`);
//             return accessCache.value;
//         }
//     }
    
//     try {
//         // 🔥 КРИТИЧЕСКИЙ ФИКС: СНАЧАЛА ПРОВЕРЯЕМ ПОДПИСКУ, ПОТОМ КАНАЛ
//         const hasSubscription = await checkSubscription(userId);
        
//         if (!hasSubscription) {
//             console.log(`❌ [FULL ACCESS] ${userId}: НЕТ ПОДПИСКИ -> демо`);
            
//             // Сохраняем в кэш
//             if (!ctx.session) ctx.session = {};
//             ctx.session.fullAccessCache = {
//                 value: false,
//                 timestamp: Date.now(),
//                 subscription: false,
//                 channel: false
//             };
            
//             return false;
//         }
        
//         // 🔥 ЕСЛИ ЕСТЬ ПОДПИСКА - ПРОВЕРЯЕМ КАНАЛ
//         const hasChannelSubscription = await checkChannelSubscription(ctx);
//         const hasFullAccess = hasSubscription && hasChannelSubscription;
        
//         console.log(`📊 [FULL ACCESS] ${userId}: подписка=${hasSubscription}, канал=${hasChannelSubscription}, полный доступ=${hasFullAccess}`);
        
//         // Сохраняем в сессионный кэш
//         if (!ctx.session) ctx.session = {};
//         ctx.session.fullAccessCache = {
//             value: hasFullAccess,
//             timestamp: Date.now(),
//             subscription: hasSubscription,
//             channel: hasChannelSubscription
//         };
        
//         return hasFullAccess;
        
//     } catch (error) {
//         console.error(`❌ [FULL ACCESS ERROR] Ошибка для ${userId}:`, error);
        
//         // При ошибке - демо
//         if (!ctx.session) ctx.session = {};
//         ctx.session.fullAccessCache = {
//             value: false,
//             timestamp: Date.now(),
//             error: error.message
//         };
        
//         return false;
//     }
// };

//     // ===================== СИСТЕМА УПРАВЛЕНИЯ СООБЩЕНИЯМИ =====================
//     const chatStorage = {
//         messages: new Map(),
//         mainMenu: new Map(),
//         userState: new Map(),
//         messageTimestamps: new Map(),
//         countryKeyboard: new Map(),
//         cityKeyboard: new Map(),
//     };

//     setInterval(() => {
//         const now = Date.now();
//         let cleanedCount = 0;
        
//         chatStorage.messages.forEach((messages, chatId) => {
//             messages.forEach(messageId => {
//                 if (now - (chatStorage.messageTimestamps.get(messageId) || 0) > SCALING_CONFIG.PERFORMANCE.MESSAGE_TTL) {
//                     messages.delete(messageId);
//                     chatStorage.messageTimestamps.delete(messageId);
//                     cleanedCount++;
//                 }
//             });
            
//             if (messages.size === 0) {
//                 chatStorage.messages.delete(chatId);
//             }
//         });
        
//         if (cleanedCount > 0) {
//             console.log(`🧹 [CLEANUP] Очищено ${cleanedCount} сообщений`);
//         }
//     }, 3600000);

//     // ===================== ОПТИМИЗИРОВАННАЯ ФУНКЦИЯ ПОЛУЧЕНИЯ СТРАН =====================
//     const getUniqueCountries = async (isDemo = false) => {
//         try {
//             console.log(`🔍 [COUNTRIES] Запрос списка стран (демо: ${isDemo})`);
            
//             // 🔥 ВАЖНОЕ ИЗМЕНЕНИЕ: ПРОВЕРЯЕМ РАЗНЫЕ ВИДЫ КЭША
//             let cachedCountries;
            
//             if (isDemo) {
//                 // Для демо: сначала пробуем получить сортированный список
//                 cachedCountries = demoCache.get("demo:countries");
//                 if (!cachedCountries || cachedCountries.length === 0) {
//                     // Если нет сортированного, пробуем сырые данные
//                     cachedCountries = demoCache.get("demo:countries_raw");
//                 }
//             } else {
//                 // Для полного: сначала пробуем получить сортированный список
//                 cachedCountries = profilesCache.get("profiles:countries");
//                 if (!cachedCountries || cachedCountries.length === 0) {
//                     // Если нет сортированного, пробуем сырые данные
//                     cachedCountries = profilesCache.get("profiles:countries_raw");
//                 }
//             }
            
//             if (cachedCountries && cachedCountries.length > 0) {
//                 console.log(`✅ [COUNTRIES] Страны из кэша: ${cachedCountries.length}`);
//                 return cachedCountries;
//             }
            
//             // 🔥 ЕСЛИ КЭШ ПУСТ, СОЗДАЕМ МИНИМАЛЬНЫЙ КЭШ НА ЛЕТУ
//             console.log(`⚠️ [COUNTRIES] Нет стран в кэше, создаем минимальный кэш из популярных стран`);
            
//             const popularCountries = POPULAR_COUNTRIES.map(c => c.flag + ' ' + c.name);
            
//             // Сохраняем в кэш
//             if (isDemo) {
//                 cacheManager.cacheCountries(popularCountries, true);
//             } else {
//                 cacheManager.cacheCountries(popularCountries, false);
//             }
            
//             console.log(`✅ [COUNTRIES] Создан минимальный кэш из ${popularCountries.length} популярных стран`);
//             return popularCountries;
            
//         } catch (error) {
//             console.error("❌ [COUNTRIES] Ошибка загрузки стран:", error);
//             return POPULAR_COUNTRIES.map(c => c.flag + ' ' + c.name);
//         }
//     };

//     // ===================== ОПТИМИЗИРОВАННАЯ ФУНКЦИЯ ПОЛУЧЕНИЯ ГОРОДОВ =====================
//     const getUniqueCitiesForCountry = async (country, isDemo = false) => {
//         try {
//             console.log(`🔍 [CITIES] Запрос городов для ${country} (демо: ${isDemo})`);
            
//             const cachedCities = cacheManager.getCachedCities(country, isDemo);
//             if (cachedCities && cachedCities.length > 0) {
//                 console.log(`✅ [CITIES] Города из кэша для ${country}: ${cachedCities.length}`);
//                 return cachedCities;
//             }
            
//             console.log(`⚠️ [CITIES] Нет городов в кэше для ${country}! Возвращаем пустой массив`);
            
//             // Если кэш пуст, возвращаем пустой массив
//             // В реальном использовании пользователь увидит, что городов нет
//             return [];
            
//         } catch (error) {
//             console.error(`❌ [CITIES] Ошибка загрузки городов для ${country}:`, error);
//             return [];
//         }
//     };

//     const formatCountryWithFlag = (countryName) => {
//         return countryName;
//     };

//     // ===================== СИСТЕМА ПАГИНАЦИИ =====================
//     const createEnhancedPaginationKeyboard = (currentPage, totalPages, filterKey, currentFilters = {}, isDemo = false) => {
//         const keyboard = [];
        
//         if (currentFilters.country || currentFilters.city || currentFilters.ageRange) {
//             let filtersText = "";
//             const filters = [];
//             if (currentFilters.country) filters.push(currentFilters.country);
//             if (currentFilters.city) filters.push(currentFilters.city);
//             if (currentFilters.ageRange) filters.push(currentFilters.ageRange.label);
//             filtersText += filters.join(", ");
            
//             keyboard.push([{ text: filtersText, callback_data: "filters_info" }]);
//         }
        
//         const navRow = [];
//         if (currentPage > 0) {
//             navRow.push({ text: "⏪", callback_data: `page_first_${currentPage}` });
//             navRow.push({ text: "◀️", callback_data: `page_prev_${currentPage}` });
//         }
        
//         navRow.push({ text: `${currentPage + 1}/${totalPages}`, callback_data: "page_info" });
        
//         if (currentPage < totalPages - 1) {
//             navRow.push({ text: "▶️", callback_data: `page_next_${currentPage}` });
//             navRow.push({ text: "⏩", callback_data: `page_last_${currentPage}` });
//         }
        
//         keyboard.push(navRow);

//         if (totalPages > 10) {
//             const jumpRow = [];
//             const totalProfiles = totalPages * SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE;
            
//             PAGINATION_JUMP_SECTIONS.forEach(section => {
//                 if (section.start < totalProfiles) {
//                     const sectionPage = Math.floor(section.start / SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE);
//                     if (sectionPage < totalPages) {
//                         jumpRow.push({ text: section.label, callback_data: `page_${sectionPage}_${currentPage}` });
//                     }
//                 }
//             });
            
//             if (jumpRow.length > 0) keyboard.push(jumpRow);
//         }

//         if (totalPages > 1) {
//             const quickPagesRow = [];
//             const pagesToShow = Math.min(5, totalPages);
//             let startPage = Math.max(0, currentPage - Math.floor(pagesToShow / 2));
            
//             if (startPage + pagesToShow > totalPages) startPage = Math.max(0, totalPages - pagesToShow);

//             for (let i = 0; i < pagesToShow; i++) {
//                 const pageNum = startPage + i;
//                 if (pageNum >= 0 && pageNum < totalPages) {
//                     quickPagesRow.push({
//                         text: pageNum === currentPage ? `• ${pageNum + 1} •` : `${pageNum + 1}`,
//                         callback_data: `page_${pageNum}_${currentPage}`,
//                     });
//                 }
//             }
            
//             if (quickPagesRow.length > 0) keyboard.push(quickPagesRow);
//         }

//         keyboard.push([
//             { text: "📝 СОЗДАТЬ АНКЕТУ", web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } }
//         ]);

//         if (isDemo) {
//             keyboard.push([
//                 { text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }
//             ]);
//         }

//         return keyboard;
//     };

//     // ===================== ОПТИМИЗИРОВАННАЯ ФУНКЦИЯ ПОЛУЧЕНИЯ ПРОФИЛЕЙ =====================
//     const getProfilesPage = async (page = 0, searchCountry = null, ageRange = null, searchCity = null, isDemo = false) => {
//         try {
//             console.log(`🔍 [PROFILES PAGE] Запрос страницы ${page}, демо=${isDemo}, страна=${searchCountry}, город=${searchCity}`);
            
//             // Если нужен полный доступ, но полного кэша нет - загружаем ЛЕНИВО
//             if (!isDemo && !cacheManager.isFullCacheLoaded()) {
//                 console.log(`🔄 [PROFILES PAGE] Полный кэш не загружен, запускаем ленивую загрузку...`);
//                 try {
//                     await lazyLoadFullCache('profiles_page_request');
//                 } catch (error) {
//                     console.error(`❌ [PROFILES PAGE] Не удалось загрузить полный кэш:`, error);
//                     // Если не удалось загрузить полный кэш, используем демо
//                     isDemo = true;
//                 }
//             }
            
//             // Если нужен демо-кэш, но его нет (маловероятно)
//             if (isDemo && !cacheManager.isDemoCacheLoaded()) {
//                 console.log(`⚠️ [PROFILES PAGE] Демо-кэш не загружен, а должен быть!`);
//                 return [];
//             }
            
//             const normalizedSearchCity = searchCity ? cacheManager.normalizeCityName(searchCity) : null;
            
//             const filterKey = `country:${searchCountry || 'all'}:age:${ageRange?.label || 'all'}:city:${normalizedSearchCity || 'all'}`;
            
//             let filteredProfiles;
            
//             if (isDemo) {
//                 filteredProfiles = cacheManager.getCachedDemoFilteredProfiles(filterKey);
//             } else {
//                 filteredProfiles = cacheManager.getCachedFilteredProfiles(filterKey);
//             }
            
//             if (!filteredProfiles) {
//                 console.log(`🔍 [FILTER] Промах кэша: ${filterKey} (демо: ${isDemo})`);
//                 let allProfiles = cacheManager.getCachedProfiles(isDemo);
                
//                 console.log(`📊 [FILTER] Всего профилей в кэше: ${allProfiles ? allProfiles.length : 0} (демо: ${isDemo})`);
                
//                 if (!allProfiles || allProfiles.length === 0) {
//                     console.error(`❌ [CACHE] Нет профилей в ${isDemo ? 'демо' : 'полном'} кэше`);
                    
//                     // Критическая ошибка - кэш должен быть загружен
//                     if (isDemo) {
//                         // Демо-кэш должен быть загружен при старте бота
//                         console.error(`💥 [CACHE] Демо-кэш не загружен! Это критическая ошибка!`);
//                     }
                    
//                     return [];
//                 }

//                 filteredProfiles = await asyncFilterManager.filterProfilesAsync(allProfiles, {
//                     country: searchCountry,
//                     city: normalizedSearchCity,
//                     ageRange: ageRange
//                 }, isDemo);

//                 console.log(`✅ [FILTER] Отфильтровано: ${filteredProfiles.length} профилей`);

//                 if (filteredProfiles.length > 0) {
//                     if (isDemo) {
//                         cacheManager.cacheDemoFilteredProfiles(filterKey, filteredProfiles);
//                     } else {
//                         cacheManager.cacheFilteredProfiles(filterKey, filteredProfiles);
//                     }
//                 }
//             }

//             const startIndex = page * SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE;
//             const endIndex = startIndex + SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE;
            
//             const result = filteredProfiles.slice(startIndex, endIndex);
//             console.log(`📄 [PAGE] Возвращаем страницу ${page}: ${result.length} профилей`);
            
//             return result;

//         } catch (error) {
//             console.error("❌ [PROFILES PAGE] Ошибка загрузки анкет:", error);
//             return [];
//         }
//     };

//     // ===================== МЕНЕДЖЕР СООБЩЕНИЙ =====================
//     const messageManager = {
//         track: function (chatId, messageId) {
//             if (!messageId) return;
//             if (!chatStorage.messages.has(chatId)) chatStorage.messages.set(chatId, new Set());
//             chatStorage.messages.get(chatId).add(messageId);
//             chatStorage.messageTimestamps.set(messageId, Date.now());
//         },

//         clear: async function (ctx, keepCityKeyboard = false, keepCountryKeyboard = false) {
//             const chatId = ctx.chat.id;
//             if (!chatStorage.messages.has(chatId)) return;

//             const messages = [...chatStorage.messages.get(chatId)];
//             const mainMenuId = chatStorage.mainMenu.get(chatId);
//             const countryKeyboardId = chatStorage.countryKeyboard.get(chatId);
//             const cityKeyboardId = chatStorage.cityKeyboard.get(chatId);

//             let deletedCount = 0;

//             for (const messageId of messages) {
//                 const shouldKeep = 
//                     (keepCountryKeyboard && messageId === countryKeyboardId) ||
//                     (keepCityKeyboard && messageId === cityKeyboardId) ||
//                     messageId === mainMenuId;
                    
//                 if (!shouldKeep) {
//                     try {
//                         await ctx.telegram.deleteMessage(chatId, messageId);
//                         chatStorage.messages.get(chatId).delete(messageId);
//                         chatStorage.messageTimestamps.delete(messageId);
//                         deletedCount++;
//                     } catch (e) {
//                         if (e.response?.error_code !== 400) {
//                             console.error(`❌ Ошибка удаления ${messageId}:`, e.message);
//                         }
//                     }
//                 }
//             }

//             if (cityKeyboardId && !keepCityKeyboard) {
//                 try {
//                     await ctx.telegram.deleteMessage(chatId, cityKeyboardId);
//                     chatStorage.messages.get(chatId)?.delete(cityKeyboardId);
//                     chatStorage.messageTimestamps.delete(cityKeyboardId);
//                     chatStorage.cityKeyboard.delete(chatId);
//                     deletedCount++;
//                 } catch (e) {
//                     if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления клавиатуры городов:", e);
//                 }
//             }

//             if (countryKeyboardId && !keepCountryKeyboard) {
//                 try {
//                     await ctx.telegram.deleteMessage(chatId, countryKeyboardId);
//                     chatStorage.messages.get(chatId)?.delete(countryKeyboardId);
//                     chatStorage.messageTimestamps.delete(countryKeyboardId);
//                     chatStorage.countryKeyboard.delete(chatId);
//                     deletedCount++;
//                 } catch (e) {
//                     if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления клавиатуры стран:", e);
//                 }
//             }

//             chatStorage.userState.delete(ctx.from.id);
//             if (deletedCount > 0) console.log(`🧹 [CLEAN] Удалено ${deletedCount} сообщений для чата ${chatId}`);
//         },

//         sendMainMenu: async function (ctx) {
//             return messageQueue.add(async () => {
//                 const chatId = ctx.chat.id;
//                 const self = this;

//                 try {
//                     if (chatStorage.mainMenu.has(chatId)) {
//                         try {
//                             await ctx.telegram.deleteMessage(chatId, chatStorage.mainMenu.get(chatId));
//                             chatStorage.messages.get(chatId)?.delete(chatStorage.mainMenu.get(chatId));
//                             chatStorage.messageTimestamps.delete(chatStorage.mainMenu.get(chatId));
//                         } catch (e) {
//                             if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления меню:", e);
//                         }
//                     }

//                     const hasFullAccess = await checkFullAccess(ctx);
//                     const menuButtons = [];

//                     menuButtons.push([{ text: "🎂 Фильтр по возрасту", callback_data: "filter_by_age" }]);
//                     menuButtons.push([{ text: "🌍 Все страны", callback_data: "all_countries_with_check" }]);
                    
//                     menuButtons.push([{ text: "📝 СОЗДАТЬ АНКЕТУ", web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } }]);
                    
//                     menuButtons.push([{ text: "🧹 Очистить экран", callback_data: "clear_screen" }]);

//                     if (!hasFullAccess) {
//                         menuButtons.push([{ text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }]);
//                     }

//                     const menu = await ctx.reply("Главное меню:", { reply_markup: { inline_keyboard: menuButtons } });
//                     chatStorage.mainMenu.set(chatId, menu.message_id);
//                     self.track(chatId, menu.message_id);

//                 } catch (error) {
//                     console.error("❌ Ошибка отправки меню:", error);
//                     throw error;
//                 }
//             });
//         },
        
//         sendCountriesKeyboard: async function (ctx, isDemo = false, page = 0) {
//             return messageQueue.add(async () => {
//                 const chatId = ctx.chat.id;
//                 const self = this;

//                 try {
//                     if (chatStorage.countryKeyboard.has(chatId)) {
//                         try {
//                             await ctx.telegram.deleteMessage(chatId, chatStorage.countryKeyboard.get(chatId));
//                             chatStorage.messages.get(chatId)?.delete(chatStorage.countryKeyboard.get(chatId));
//                             chatStorage.messageTimestamps.delete(chatStorage.countryKeyboard.get(chatId));
//                         } catch (e) {
//                             if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления клавиатуры стран:", e);
//                         }
//                     }

//                     const preloaderMsg = await sendPreloader(ctx, 'country');
//                     const uniqueCountries = await getUniqueCountries(isDemo);
//                     await removePreloader(ctx, preloaderMsg);
                    
//                     if (!uniqueCountries || uniqueCountries.length === 0) {
//                         const msg = await ctx.reply("❌ Список стран временно недоступен. Попробуйте позже.");
//                         self.track(chatId, msg.message_id);
//                         return;
//                     }
                    
//                     const countriesPerPage = 60;
//                     const totalPages = Math.ceil(uniqueCountries.length / countriesPerPage);
                    
//                     if (page < 0) page = 0;
//                     if (page >= totalPages) page = totalPages - 1;
                    
//                     const startIndex = page * countriesPerPage;
//                     const endIndex = Math.min(startIndex + countriesPerPage, uniqueCountries.length);
//                     const pageCountries = uniqueCountries.slice(startIndex, endIndex);

//                     const keyboard = [];
//                     let row = [];

//                     pageCountries.forEach((country, index) => {
//                         const countryWithFlag = formatCountryWithFlag(country);
//                         row.push({ text: countryWithFlag, callback_data: `country_${country}` });

//                         if (row.length === 3 || index === pageCountries.length - 1) {
//                             keyboard.push(row);
//                             row = [];
//                         }
//                     });

//                     if (totalPages > 1) {
//                         const paginationRow = [];
                        
//                         if (page > 0) {
//                             paginationRow.push({ 
//                                 text: "◀️ Назад", 
//                                 callback_data: `countries_page_${page - 1}` 
//                             });
//                         }
                        
//                         paginationRow.push({ 
//                             text: `Страна ${page + 1}/${totalPages}`, 
//                             callback_data: "countries_page_info" 
//                         });
                        
//                         if (page < totalPages - 1) {
//                             paginationRow.push({ 
//                                 text: "Вперед ▶️", 
//                                 callback_data: `countries_page_${page + 1}` 
//                             });
//                         }
                        
//                         if (paginationRow.length > 0) {
//                             keyboard.push(paginationRow);
//                         }
                        
//                         if (totalPages > 5) {
//                             const quickPagesRow = [];
//                             const pagesToShow = Math.min(5, totalPages);
                            
//                             for (let i = 0; i < pagesToShow; i++) {
//                                 quickPagesRow.push({
//                                     text: i === page ? `• ${i + 1} •` : `${i + 1}`,
//                                     callback_data: `countries_page_${i}`
//                                 });
//                             }
                            
//                             if (page >= pagesToShow) {
//                                 quickPagesRow.push({ text: "...", callback_data: "countries_page_info" });
//                                 quickPagesRow.push({
//                                     text: `• ${page + 1} •`,
//                                     callback_data: `countries_page_${page}`
//                                 });
//                             }
                            
//                             if (quickPagesRow.length > 0) {
//                                 keyboard.push(quickPagesRow);
//                             }
//                         }
//                     }

//                     keyboard.push([{ text: "📝 СОЗДАТЬ АНКЕТУ", web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } }]);

//                     if (isDemo) {
//                         keyboard.push([{ text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }]);
//                     }

//                     keyboard.push([{ text: "🔙 Назад", callback_data: "back_to_menu" }]);

//                     const msgText = isDemo ? 
//                         `👀 ДЕМО-РЕЖИМ: Выберите страну (показано по 3 анкеты на город)\n\n📄 Страница ${page + 1} из ${totalPages}\n🌍 Всего стран: ${uniqueCountries.length}\n\n💎 Для полного доступа Вы должны быть подписаны на наш канал @MagicYourClub и оплатить подписку` : 
//                         `🌍 Выберите страну (страница ${page + 1} из ${totalPages})\n📊 Всего стран: ${uniqueCountries.length}\n📍 Показано: ${pageCountries.length} стран`;

//                     const msg = await ctx.reply(msgText, { 
//                         reply_markup: { inline_keyboard: keyboard },
//                         parse_mode: "HTML"
//                     });
                    
//                     chatStorage.countryKeyboard.set(chatId, msg.message_id);
//                     self.track(chatId, msg.message_id);
                    
//                 } catch (error) {
//                     console.error("❌ Ошибка отправки клавиатуры стран:", error);
//                     throw error;
//                 }
//             });
//         },

//         sendCitiesKeyboard: async function (ctx, country, isDemo = false) {
//             return messageQueue.add(async () => {
//                 const chatId = ctx.chat.id;
//                 const self = this;

//                 try {
//                     if (chatStorage.cityKeyboard.has(chatId)) {
//                         try {
//                             await ctx.telegram.deleteMessage(chatId, chatStorage.cityKeyboard.get(chatId));
//                             chatStorage.messages.get(chatId)?.delete(chatStorage.cityKeyboard.get(chatId));
//                             chatStorage.messageTimestamps.delete(chatStorage.cityKeyboard.get(chatId));
//                         } catch (e) {
//                             if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления клавиатуры городов:", e);
//                         }
//                     }

//                     const preloaderMsg = await sendPreloader(ctx, 'profiles');
                    
//                     const cities = await getUniqueCitiesForCountry(country, isDemo);
                    
//                     await removePreloader(ctx, preloaderMsg);
                    
//                     if (!cities || cities.length === 0) {
//                         const msg = await ctx.reply(`❌ Для страны "${country}" нет доступных городов`);
//                         self.track(chatId, msg.message_id);
//                         return;
//                     }

//                     console.log(`🏙️ [CITIES] Показано городов для ${country}: ${cities.length}`);

//                     const citiesPerPage = 50;
//                     let currentPage = 0;
//                     const totalPages = Math.ceil(cities.length / citiesPerPage);

//                     const createCitiesKeyboard = (page) => {
//                         const startIndex = page * citiesPerPage;
//                         const endIndex = Math.min(startIndex + citiesPerPage, cities.length);
//                         const pageCities = cities.slice(startIndex, endIndex);
                        
//                         const keyboard = [];
//                         let row = [];

//                         pageCities.forEach((city, index) => {
//                             row.push({ text: city, callback_data: `city_${city}` });
//                             if (row.length === 3 || index === pageCities.length - 1) {
//                                 keyboard.push(row);
//                                 row = [];
//                             }
//                         });

//                         const paginationRow = [];
//                         if (totalPages > 1) {
//                             if (page > 0) {
//                                 paginationRow.push({ 
//                                     text: "◀️ Предыдущие", 
//                                     callback_data: `cities_page_${country}_${page - 1}` 
//                                 });
//                             }
                            
//                             paginationRow.push({ 
//                                 text: `${page + 1}/${totalPages}`, 
//                                 callback_data: "cities_page_info" 
//                             });
                            
//                             if (page < totalPages - 1) {
//                                 paginationRow.push({ 
//                                     text: "Следующие ▶️", 
//                                     callback_data: `cities_page_${country}_${page + 1}` 
//                                 });
//                             }
                            
//                             if (paginationRow.length > 0) {
//                                 keyboard.push(paginationRow);
//                             }
//                         }

//                         keyboard.push([{ 
//                             text: "📝 СОЗДАТЬ АНКЕТУ", 
//                             web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } 
//                         }]);

//                         if (isDemo) {
//                             keyboard.push([{ 
//                                 text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", 
//                                 callback_data: "get_full_access" 
//                             }]);
//                         }

//                         keyboard.push([{ 
//                             text: "🔙 Назад к странам", 
//                             callback_data: "back_to_countries" 
//                         }]);

//                         return keyboard;
//                     };

//                     const msgText = isDemo ?
//                         `👀 ДЕМО-РЕЖИМ: Выберите город в ${country} (${cities.length} городов, показано по 3 анкеты на город)\n\n💎 Для полного доступа ко всем анкетам получите подписку!` :
//                         `🏙️ Выберите город в ${country} (всего ${cities.length} городов):`;

//                     const msg = await ctx.reply(msgText, { 
//                         reply_markup: { inline_keyboard: createCitiesKeyboard(currentPage) } 
//                     });
                    
//                     chatStorage.cityKeyboard.set(chatId, msg.message_id);
//                     self.track(chatId, msg.message_id);
                    
//                 } catch (error) {
//                     console.error("❌ Ошибка отправки клавиатуры городов:", error);
//                     throw error;
//                 }
//             });
//         },
//     };

//     // ===================== ОБРАБОТЧИКИ КОМАНД =====================
    
//     bot.command("start", async (ctx) => {
//     await messageQueue.add(async () => {
//         try {
//             console.log(`🚀 Пользователь ${ctx.from.id} запустил бота`);
            
//             // 🔥 ОЧИЩАЕМ ЭКРАН
//             await messageManager.clear(ctx);
            
//             // 🔥 ПОКАЗЫВАЕМ МЕНЮ СРАЗУ
//             await messageManager.sendMainMenu(ctx);
//             // В обработчике /start или при запуске бота:
// const loadDemoCacheWithProgress = async () => {
//     console.log('🚀 Загрузка демо-кэша...');
    
//     // Показываем прелоадер пользователю
//     const preloaderMsg = await ctx.reply(`
// 🔄 <b>ПОДГОТОВКА БАЗЫ ДАННЫХ</b>

// ⏳ <i>Загружаем анкеты из базы...</i>
// 📊 <i>Это может занять до 2 минут</i>
// ⏱️ <i>Пожалуйста, подождите</i>

// <code>Инициализация демо-режима</code>
//     `, { parse_mode: "HTML" });
    
//     try {
//         // Загружаем демо-кэш
//         await loadProfileCache(db, false);
        
//         // Удаляем прелоадер
//         await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
        
//         console.log('✅ Демо-кэш загружен');
        
//     } catch (error) {
//         console.error('❌ Ошибка загрузки демо-кэша:', error);
        
//         // Показываем ошибку
//         await ctx.telegram.editMessageText(
//             ctx.chat.id,
//             preloaderMsg.message_id,
//             null,
//             `❌ <b>ОШИБКА ЗАГРУЗКИ БАЗЫ</b>

// Не удалось загрузить базу анкет.

// <b>Что делать:</b>
// 1. Проверьте интернет-соединение
// 2. Попробуйте через 5 минут
// 3. Используйте /start для повторной попытки

// <em>Бот работает в ограниченном режиме</em>`,
//             { parse_mode: "HTML" }
//         );
//     }
// };
//             // 🔥 ЗАГРУЖАЕМ ДЕМО-КЭШ В ФОНЕ (без прелоадера)
//             if (!cacheManager.isDemoCacheLoaded() && !demoCacheLoading) {
//                 console.log(`🔄 [START] Фоновая загрузка демо-кэша...`);
//                 demoCacheLoading = true;
                
//                 setTimeout(async () => {
//                     try {
//                         await loadProfileCache(db, false);
//                         console.log(`✅ [START] Демо-кэш загружен в фоне`);
//                     } catch (error) {
//                         console.error(`❌ [START] Ошибка загрузки:`, error.message);
//                     } finally {
//                         demoCacheLoading = false;
//                     }
//                 }, 1000); // Задержка 1 секунда
//             }
            
//         } catch (error) {
//             console.error("❌ Ошибка команды start:", error);
//             await ctx.reply("⚠️ Ошибка запуска. Попробуйте еще раз.");
//         }
//     });
// });

//     bot.command("init_cache", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 const userId = ctx.from.id;
//                 console.log(`🚀 [INIT CACHE] Принудительная инициализация кэша для ${userId}`);
                
//                 await ctx.reply(`
// 🔄 <b>ИНИЦИАЛИЗАЦИЯ КЭША</b>

// Загружаем демо-кэш...
// ⏱️ Это может занять несколько секунд

// <em>Пожалуйста, подождите</em>
//                 `, { parse_mode: "HTML" });
                
//                 try {
//                     // Пытаемся загрузить демо-кэш
//                     await loadProfileCache(db, false);
                    
//                     await ctx.reply(`
// ✅ <b>КЭШ УСПЕШНО ЗАГРУЖЕН!</b>

// • 📊 Анкеты: загружены
// • 🌍 Страны: доступны
// • 📍 Города: доступны

// Теперь вы можете использовать бота полноценно!
//                     `, { parse_mode: "HTML" });
                    
//                 } catch (error) {
//                     console.error(`❌ [INIT CACHE] Ошибка:`, error.message);
                    
//                     // Создаем минимальный кэш
//                     const popularCountries = POPULAR_COUNTRIES.map(c => c.flag + ' ' + c.name);
//                     cacheManager.cacheCountries(popularCountries, true);
                    
//                     await ctx.reply(`
// ⚠️ <b>НЕ УДАЛОСЬ ЗАГРУЖИТЬ КЭШ</b>

// Создан минимальный кэш с популярными странами.

// <b>Что делать:</b>
// 1. Проверьте подключение к интернету
// 2. Попробуйте через 5 минут
// 3. Используйте команду /start

// Бот работает в ограниченном режиме.
//                     `, { parse_mode: "HTML" });
//                 }
                
//             } catch (error) {
//                 console.error("❌ Ошибка команды init_cache:", error);
//                 await ctx.reply("❌ Ошибка инициализации кэша");
//             }
//         });
//     });
// bot.action("all_countries_with_check", async (ctx) => {
//     const userId = ctx.from.id;
//     console.log(`🌍 [COUNTRIES] Пользователь ${userId} запросил страны`);
    
//     // 🔥 ДОБАВЛЯЕМ ПРЕЛОАДЕР СРАЗУ
//     const accessPreloader = await sendPreloader(ctx, 'access', "🔍 ПРОВЕРКА ДОСТУПА");
    
//     // 🔥 БЫСТРАЯ ПРОВЕРКА БЛОКИРОВКИ
//     if (!acquireUserLock(userId, 2000)) {
//         try {
//             await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//             // Удаляем прелоадер
//             if (accessPreloader) {
//                 try {
//                     await ctx.telegram.deleteMessage(ctx.chat.id, accessPreloader.message_id);
//                 } catch (e) {}
//             }
//         } catch (e) {}
//         return;
//     }
    
//     try {
//         await ctx.answerCbQuery("🌍 Загружаем страны...");
        
//         // Очищаем предыдущие сообщения
//         await messageManager.clear(ctx);
        
//         // 🔥 🔥 🔥 ЭТАП 1: ВСЕГДА ПРОВЕРЯЕМ ДОСТУП ЗАНОВО
//         console.log(`🔄 [FORCE CHECK] Принудительная проверка для ${userId}`);
        
//         // Очищаем ВСЕ кэши
//         subscriptionCache.del(`subscription:${userId}`);
//         channelSubscriptionCache.del(`channel:${userId}`);
//         userCacheStatus.del(`cache_status:${userId}`);
        
//         // 🔥 ПРОВЕРЯЕМ ПОДПИСКУ И КАНАЛ ЗАНОВО
//         console.log(`🔍 [ACCESS CHECK] Проверка доступа для ${userId}`);
        
//         const hasSubscription = await checkSubscription(userId);
//         const hasChannelSubscription = await checkChannelSubscription(ctx);
//         const hasFullAccess = hasSubscription && hasChannelSubscription;
        
//         console.log(`✅ [ACCESS RESULT] ${userId}: подписка=${hasSubscription}, канал=${hasChannelSubscription}, полный доступ=${hasFullAccess}`);
        
//         // Определяем тип кэша
//         const cacheType = hasFullAccess ? 'full' : 'demo';
//         const isDemo = cacheType === 'demo';
        
//         // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР
//         if (accessPreloader) {
//             try {
//                 await ctx.telegram.deleteMessage(ctx.chat.id, accessPreloader.message_id);
//             } catch (e) {
//                 console.log("Не удалось удалить прелоадер:", e.message);
//             }
//         }
        
//         // 🔥 🔥 🔥 ЭТАП 2: ЕСЛИ ДЕМО - ПОКАЗЫВАЕМ ИНФОРМАЦИЮ
//         if (isDemo) {
//             if (hasSubscription && !hasChannelSubscription) {
//                 // ЕСТЬ ПОДПИСКА, НЕТ КАНАЛА
//                 await ctx.reply(`
// 🎯 <b>ВАШ СТАТУС</b>

// ✅ Подписка: <b>АКТИВНА</b>
// ❌ Канал: <b>НЕ ПОДПИСАН</b>

// <em>Вы в <b>ДЕМО-РЕЖИМЕ</b> (3 анкеты на город)</em>

// 📢 Для полного доступа подпишитесь на @MagicYourClub
// 🔄 После подписки нажмите "✅ Я ПОДПИСАЛСЯ НА КАНАЛ"
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
//                             [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }],
//                             [{ text: "🌍 ВСЕ СТРАНЫ (демо)", callback_data: "all_countries_with_check" }]
//                         ]
//                     }
//                 });
//             } else if (!hasSubscription && hasChannelSubscription) {
//                 // ЕСТЬ КАНАЛ, НЕТ ПОДПИСКИ
//                 await ctx.reply(`
// ⚠️ <b>ДЕМО-РЕЖИМ</b>

// ✅ Канал: <b>ПОДПИСАН</b>
// ❌ Подписка: <b>НЕТ</b>

// 💎 Для полного доступа купите подписку

// <em>В демо-режиме вы видите по 3 анкеты на каждый город</em>
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }],
//                             [{ text: "🌍 ВСЕ СТРАНЫ (демо)", callback_data: "all_countries_with_check" }]
//                         ]
//                     }
//                 });
//             } else {
//                 // НИЧЕГО НЕТ
//                 await ctx.reply(`
// 👀 <b>ДЕМО-РЕЖИМ</b>

// Для полного доступа:
// 1. 💎 Купите подписку
// 2. 📢 Подпишитесь на @MagicYourClub

// <em>В демо-режиме вы видите по 3 анкеты на каждый город</em>
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }],
//                             [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }],
//                             [{ text: "🌍 ВСЕ СТРАНЫ (демо)", callback_data: "all_countries_with_check" }]
//                         ]
//                     }
//                 });
//             }
            
//             // 🔥 ПОКАЗЫВАЕМ СТРАНЫ В ДЕМО-РЕЖИМЕ
//             await messageManager.sendCountriesKeyboard(ctx, true, 0);
//             return;
//         }
        
//         // 🔥 🔥 🔥 ЭТАП 3: ЕСЛИ ПОЛНЫЙ ДОСТУП
//         console.log(`🎉 [FULL ACCESS] Пользователь ${userId} получил полный доступ`);
        
//         // 🔥 ЭТАП 3.1: ЕСЛИ ПОЛНЫЙ КЭШ НЕ ЗАГРУЖЕН - НАЧИНАЕМ ФОНОВУЮ ЗАГРУЗКУ
//         if (!cacheManager.isFullCacheLoaded()) {
//             console.log(`🚀 [FULL CACHE NEEDED] Полный кэш не загружен, начинаем фонную загрузку`);
            
//             // Показываем сообщение о начале загрузки
//             const loadingMsg = await ctx.reply(`
// 🔄 <b>ЗАГРУЗКА ПОЛНОГО ДОСТУПА</b>

// 🎉 У вас есть полный доступ!
// 📊 Начинаем загрузку 70,000+ анкет...

// ⏱️ <i>Это займет 2-3 минуты</i>
// 📦 <i>Загружаем пачками по 5000 анкет</i>
// ✨ <i>Вы можете сразу начать просмотр</i>

// <em>Не закрывайте чат, загрузка продолжается в фоне</em>
//             `, { parse_mode: "HTML" });
            
//             // 🔥 ЗАПУСКАЕМ ФОНОВУЮ ЗАГРУЗКУ
//             lazyLoadFullCache(userId, false)
//                 .then(() => {
//                     console.log(`✅ [BACKGROUND CACHE] Полный кэш загружен для ${userId}`);
//                     // Удаляем сообщение о загрузке
//                     try {
//                         ctx.telegram.deleteMessage(ctx.chat.id, loadingMsg.message_id);
//                         // Показываем успешное сообщение
//                         ctx.reply(`
// ✅ <b>ДАННЫЕ ЗАГРУЖЕНЫ!</b>

// 🎉 Теперь доступны все 70,000+ анкет!
// • 👤 Все контакты видны
// • 📞 Телефоны, Telegram, WhatsApp
// • ⚡ Максимальная скорость
//                         `, { parse_mode: "HTML" });
//                     } catch (e) {}
//                 })
//                 .catch(error => {
//                     console.error(`❌ [BACKGROUND CACHE ERROR] ${userId}:`, error.message);
//                     // Показываем сообщение об ошибке
//                     try {
//                         ctx.telegram.editMessageText(
//                             ctx.chat.id,
//                             loadingMsg.message_id,
//                             null,
//                             `⚠️ <b>ОШИБКА ЗАГРУЗКИ</b>\n\nНе удалось загрузить полный кэш.\nИспользуем ограниченный доступ.\n\n<em>Попробуйте через несколько минут</em>`,
//                             { parse_mode: "HTML" }
//                         );
//                     } catch (e) {}
//                 });
            
//             // 🔥 ПОКАЗЫВАЕМ ПЕРВУЮ ПАЧКУ СТРАН (из демо-кэша пока идет загрузка)
//             // Но сначала проверяем, есть ли хоть что-то в демо-кэше
//             if (cacheManager.isDemoCacheLoaded()) {
//                 console.log(`📊 [TEMPORARY] Показываем демо-страны пока загружается полный кэш`);
//                 await messageManager.sendCountriesKeyboard(ctx, false, 0);
//             } else {
//                 // Если даже демо-кэша нет, показываем сообщение и ждем
//                 await ctx.reply(`
// ⏳ <b>ИНИЦИАЛИЗАЦИЯ КЭША</b>

// 📊 Загружаем первые анкеты...
// ⏱️ Подождите 30 секунд

// <em>Система готовится к работе</em>
//                 `, { parse_mode: "HTML" });
                
//                 // Ждем 5 секунд и показываем страны
//                 setTimeout(async () => {
//                     try {
//                         await messageManager.sendCountriesKeyboard(ctx, false, 0);
//                     } catch (e) {
//                         console.error("Ошибка показа стран:", e);
//                     }
//                 }, 5000);
//             }
            
//             return;
//         }
        
//         // 🔥 ЭТАП 3.2: ЕСЛИ ПОЛНЫЙ КЭШ УЖЕ ЗАГРУЖЕН
//         console.log(`✅ [FULL CACHE READY] Полный кэш уже загружен для ${userId}`);
        
//         // Показываем сообщение об успешном доступе
//         await ctx.reply(`
// ✅ <b>ПОЛНЫЙ ДОСТУП АКТИВИРОВАН!</b>

// 🎉 Теперь у вас доступ ко всем 70,000+ анкетам
// • 👤 Все контакты видны
// • 📞 Телефоны, Telegram, WhatsApp
// • ⚡ Максимальная скорость

// <code>Выберите страну для начала поиска</code>
//         `, {
//             parse_mode: "HTML",
//             reply_markup: {
//                 inline_keyboard: [
//                     [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
//                     [{ text: "🎂 Фильтр по возрасту", callback_data: "filter_by_age" }]
//                 ]
//             }
//         });
        
//         // 🔥 ПОКАЗЫВАЕМ СТРАНЫ
//         await messageManager.sendCountriesKeyboard(ctx, false, 0);
        
//     } catch (error) {
//         console.error("❌ [COUNTRIES CRITICAL] Критическая ошибка:", error);
        
//         // УДАЛЯЕМ ПРЕЛОАДЕР ПРИ ОШИБКЕ
//         if (accessPreloader) {
//             try {
//                 await ctx.telegram.deleteMessage(ctx.chat.id, accessPreloader.message_id);
//             } catch (e) {}
//         }
        
//         try {
//             await ctx.answerCbQuery("❌ Ошибка загрузки");
            
//             // ПРИ ОШИБКЕ - ДЕМО
//             await ctx.reply(`
// ❌ <b>КРИТИЧЕСКАЯ ОШИБКА</b>

// Не удалось загрузить список стран.

// <em>Используем демо-режим:</em>
// • 👀 3 анкеты на каждый город
// • 🚫 Контакты скрыты

// Попробуйте через несколько минут
//             `, { parse_mode: "HTML" });
            
//             await messageManager.sendCountriesKeyboard(ctx, true, 0);
//         } catch (e) {
//             console.error("Не удалось отправить сообщение об ошибке:", e);
//         }
        
//     } finally {
//         releaseUserLock(userId);
//     }
// });

//     bot.action("get_full_access", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 await ctx.answerCbQuery("💎 Переходим к оплате...");
                
//                 const keyboard = {
//                     inline_keyboard: [
//                         [
//                             { text: "💎 Купить подписку", callback_data: "choose_payment_method" }
//                         ],
//                         [
//                             { text: "📢 Подписаться на канал", url: "https://t.me/MagicYourClub" }
//                         ],
//                         [
//                             { text: "🔙 Назад", callback_data: "back_to_menu" }
//                         ]
//                     ]
//                 };

//                 await ctx.reply(`
// 💎 <b>ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП</b>

// Для получения полного доступа ко всем анкетам необходимо:

// ✅ <b>1. Активная подписка</b>
//    • Доступ ко всем анкетам
//    • Все контакты профилей
//    • Полная функциональность

// ✅ <b>2. Подписка на канал @MagicYourClub</b>
//    • Новые анкеты и обновления
//    • Эксклюзивный контент
//    • Специальные предложения

// <b>После оплаты подписки и подписки на канал вы получите:</b>
// • 🔓 Полный доступ ко всем анкетам
// • 📞 Все контакты профилей  
// • 🌍 Неограниченный поиск по странам и городам
// • ⚡ Максимальную скорость работы

// Нажмите "Купить подписку" чтобы начать!
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: keyboard
//                 });
                
//             } catch (error) {
//                 console.error("❌ Ошибка обработки получения доступа:", error);
//             }
//         });
//     });

//     bot.command("cache_debug", async (ctx) => {
//         await messageQueue.add(async () => {
//             const userId = ctx.from.id;
//             const cacheType = cacheManager.getUserCacheStatus(userId);
//             const hasFullAccess = await checkFullAccess(ctx, false);
//             const sessionType = ctx.session?.cacheSession?.type || 'нет';
            
//             const message = `
// 🔧 <b>ДЕБАГ КЭША</b>

// 👤 <b>Пользователь:</b> ${userId}
// 💾 <b>Тип кэша:</b> ${cacheType || 'не установлен'}
// 🔓 <b>Полный доступ:</b> ${hasFullAccess ? '✅' : '❌'}
// 💼 <b>Сессия:</b> ${sessionType}
// 🕒 <b>Возраст сессии:</b> ${ctx.session?.cacheSession?.timestamp ? 
//                 Math.round((Date.now() - ctx.session.cacheSession.timestamp) / 1000 / 60) + ' мин' : 'нет'}

// 📊 <b>Статус кэшей:</b>
// • Полный кэш: ${cacheManager.isFullCacheLoaded() ? '✅ Загружен' : '❌ Не загружен'}
// • Демо кэш: ${cacheManager.isDemoCacheLoaded() ? '✅ Загружен' : '❌ Не загружен'}

// 🔄 <b>Загрузки:</b>
// • Полных загрузок: ${global.cacheLoadStats?.fullCacheLoads || 0}
// • Ошибок: ${global.cacheLoadStats?.loadErrors || 0}
// • Последняя загрузка: ${global.cacheLoadStats?.lastFullCacheLoad ? 
//                 Math.round((Date.now() - global.cacheLoadStats.lastFullCacheLoad) / 1000 / 60) + ' мин назад' : 'никогда'}
//             `;
            
//             await ctx.reply(message, { parse_mode: "HTML" });
//         });
//     });

//     bot.command("force_check", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 const userId = ctx.from.id;
                
//                 // ОЧИЩАЕМ ВСЕ КЭШИ
//                 channelSubscriptionCache.del(`channel:${userId}`);
//                 subscriptionCache.del(`subscription:${userId}`);
//                 userCacheStatus.del(`cache_status:${userId}`);
                
//                 if (ctx.session) {
//                     delete ctx.session.cacheType;
//                     delete ctx.session.hasFullAccess;
//                     delete ctx.session.fullAccessChecked;
//                     delete ctx.session.fullAccessCheckTime;
//                 }
                
//                 // ПРОВЕРЯЕМ ЗАНОВО с forceRefresh
//                 const [hasSubscription, hasChannel] = await Promise.all([
//                     checkSubscription(userId),
//                     checkChannelSubscription(ctx)
//                 ]);
                
//                 const accessType = hasSubscription && hasChannel ? 'full' : 'demo';
//                 cacheManager.setUserCacheStatus(userId, accessType);
                
//                 // Сохраняем в сессию
//                 if (!ctx.session) ctx.session = {};
//                 ctx.session.hasFullAccess = hasSubscription && hasChannel;
//                 ctx.session.fullAccessChecked = true;
//                 ctx.session.fullAccessCheckTime = Date.now();
                
//                 await ctx.reply(`
// 🔄 <b>ПРИНУДИТЕЛЬНАЯ ПРОВЕРКА</b>

// ✅ Кэши очищены
// ✅ Проверка выполнена

// <b>Результат:</b>
// • 💎 Подписка: ${hasSubscription ? '✅ АКТИВНА' : '❌ НЕТ'}
// • 📢 Канал: ${hasChannel ? '✅ ПОДПИСАН' : '❌ НЕТ'}
// • 👤 Режим: ${accessType === 'full' ? '🔓 ПОЛНЫЙ' : '👀 ДЕМО'}

// ${!hasChannel ? '⚠️ <b>Вы не подписаны на канал @MagicYourClub</b>' : ''}
//                 `, { parse_mode: "HTML" });
                
//             } catch (error) {
//                 console.error("❌ Ошибка force_check:", error);
//                 await ctx.reply("❌ Ошибка проверки");
//             }
//         });
//     });

//     bot.command("load_full_cache", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 const userId = ctx.from.id;
//                 await ctx.answerCbQuery("🔄 Загружаем полный кэш...");
                
//                 console.log(`🚀 [LOAD FULL CACHE] Команда от пользователя ${userId}`);
                
//                 const hasFullAccess = await checkFullAccess(ctx, true);
                
//                 if (!hasFullAccess) {
//                     await ctx.reply(`
// ❌ <b>НЕТ ДОСТУПА К КОМАНДЕ</b>

// Эта команда доступна только пользователям с полным доступом.

// <b>Ваш статус:</b>
// 💎 Подписка: ${hasFullAccess ? '✅' : '❌'}
// 📢 Канал: ${hasFullAccess ? '✅' : '❌'}

// Используйте /refresh_access для проверки доступа
//                     `, { parse_mode: "HTML" });
//                     return;
//                 }
                
//                 // Показываем прогресс
//                 const progressMsg = await ctx.reply(`
// 🔄 <b>ЗАГРУЗКА ПОЛНОГО КЭША</b>

// ⏳ Пожалуйста, подождите...
// 📊 Загружаем 70,000+ анкет
// ⏱️ Это может занять 1-2 минуты

// <em>Не закрывайте чат во время загрузки</em>
//                 `, { parse_mode: "HTML" });
                
//                 // Загружаем полный кэш
//                 try {
//                     await lazyLoadFullCache(userId, true);
                    
//                     const cacheStatsAfter = cacheManager.getCacheStats();
//                     const memoryUsage = cacheManager.getCacheMemoryUsage();
                    
//                     await ctx.telegram.deleteMessage(ctx.chat.id, progressMsg.message_id);
                    
//                     await ctx.reply(`
// ✅ <b>ДАННЫЕ ЗАГРУЖЕНЫ!</b>

// 📊 <b>Статистика:</b>
// • Анкет загружено: ${cacheStatsAfter.profilesCount}
// • Стран в кэше: ${cacheManager.getCachedCountries(false)?.length || 0}
// • Размер кэша: ${Math.round(memoryUsage.fullCacheSize / 1024 / 1024)} MB
// • Сжатие: ~74% экономии

// 🎉 <b>Теперь доступны все функции:</b>
// • 🌍 Все 70,000+ анкет
// • 📍 Полный список стран и городов
// • 👤 Все контакты профилей
// • ⚡ Максимальная скорость работы

// <code>Используйте /stats для просмотра статистики</code>
//                     `, {
//                         parse_mode: "HTML",
//                         reply_markup: {
//                             inline_keyboard: [
//                                 [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
//                                 [{ text: "📊 Статистика", callback_data: "show_cache_stats" }]
//                             ]
//                         }
//                     });
                    
//                 } catch (error) {
//                     await ctx.telegram.deleteMessage(ctx.chat.id, progressMsg.message_id);
//                     throw error;
//                 }
                
//             } catch (error) {
//                 console.error("❌ Ошибка загрузки полного кэша:", error);
//                 await ctx.reply(`
// ❌ <b>ОШИБКА ЗАГРУЗКИ КЭША</b>

// Не удалось загрузить полный кэш.

// <b>Причина:</b> ${error.message}

// Попробуйте:
// 1. Подождать несколько минут
// 2. Использовать /refresh_access
// 3. Написать в поддержку @MagicAdd
//                 `, { parse_mode: "HTML" });
//             }
//         });
//     });

//     bot.command("reset_cache_stats", async (ctx) => {
//         await messageQueue.add(async () => {
//             const userId = ctx.from.id;
//             // ТОЛЬКО ДЛЯ АДМИНОВ (добавьте проверку)
//             const ADMINS = [123456789]; // Ваш ID
            
//             if (!ADMINS.includes(userId)) {
//                 return ctx.reply("❌ Эта команда только для администраторов");
//             }
            
//             global.cacheLoadStats = {
//                 fullCacheLoads: 0,
//                 demoCacheLoads: 0,
//                 lastFullCacheLoad: 0,
//                 lastDemoCacheLoad: 0,
//                 loadErrors: 0
//             };
            
//             await ctx.reply("✅ Статистика загрузок кэша сброшена");
//         });
//     });

//     // Действие для показа статистики кэша
//     bot.action("show_cache_stats", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 const cacheStats = cacheManager.getCacheStats();
//                 const memoryUsage = cacheManager.getCacheMemoryUsage();
//                 const demoCountries = cacheManager.getCachedCountries(true)?.length || 0;
//                 const fullCountries = cacheManager.getCachedCountries(false)?.length || 0;
                
//                 const statsMessage = `
// 📊 <b>СТАТИСТИКА КЭША</b>

// 🔹 <b>Демо-кэш:</b>
// • Анкет: ${cacheStats.demoProfilesCount}
// • Стран: ${demoCountries}
// • Загружен: ${cacheStats.demoCacheLoaded ? '✅' : '❌'}

// 🔹 <b>Полный кэш:</b>
// • Анкет: ${cacheStats.profilesCount}
// • Стран: ${fullCountries}
// • Загружен: ${cacheStats.fullCacheLoaded ? '✅' : '❌'}

// 🔹 <b>Использование памяти:</b>
// • Демо-кэш: ${Math.round(memoryUsage.demoCacheSize / 1024)} KB
// • Полный кэш: ${Math.round(memoryUsage.fullCacheSize / 1024 / 1024)} MB
// • Всего ключей: ${memoryUsage.totalKeys}

// 🔹 <b>Кэши подписок:</b>
// • Статусов пользователей: ${cacheStats.userCacheStatusCount}
// • Проверок подписки: ${cacheStats.subscriptionCacheCount}
// • Проверок канала: ${cacheStats.channelCacheCount}

// <code>${cacheStats.fullCacheLoaded ? 
//                     '✅ Полный кэш загружен' : 
//                     '❌ Полный кэш не загружен. Используйте /load_full_cache'}</code>
//                 `;
                
//                 const keyboard = {
//                     inline_keyboard: []
//                 };
                
//                 if (!cacheStats.fullCacheLoaded) {
//                     keyboard.inline_keyboard.push([
//                         { text: "🚀 Загрузить полный кэш", callback_data: "force_load_full_cache" }
//                     ]);
//                 }
                
//                 keyboard.inline_keyboard.push([
//                     { text: "🔄 Обновить", callback_data: "show_cache_stats" },
//                     { text: "🔙 Назад", callback_data: "back_to_menu" }
//                 ]);
                
//                 await ctx.reply(statsMessage, {
//                     parse_mode: "HTML",
//                     reply_markup: keyboard
//                 });
//                 await ctx.answerCbQuery();
                
//             } catch (error) {
//                 console.error("❌ Ошибка показа статистики кэша:", error);
//             }
//         });
//     });

//     // Действие для принудительной загрузки полного кэша
//     bot.action("force_load_full_cache", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 await ctx.answerCbQuery("🚀 Запускаем загрузку...");
//                 await ctx.reply("Используйте команду /load_full_cache для загрузки полного кэша");
//             } catch (error) {
//                 console.error("❌ Ошибка:", error);
//             }
//         });
//     });

//     // ОБРАБОТЧИК ПАГИНАЦИИ СТРАН
//     bot.action(/^countries_page_(\d+)$/, async (ctx) => {
//         const userId = ctx.from.id;
        
//         if (!acquireUserLock(userId, 2000)) {
//             await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//             return;
//         }
        
//         await messageQueue.add(async () => {
//             try {
//                 const page = parseInt(ctx.match[1]);
                
//                 // ПРОВЕРЯЕМ И ОБНОВЛЯЕМ КЭШ (с сессионным кэшированием)
//                 const cacheType = await ensureProperCache(ctx);
//                 const isDemo = cacheType === 'demo';
                
//                 // Обновляем сообщение с новой страницей
//                 const uniqueCountries = await getUniqueCountries(isDemo);
//                 const countriesPerPage = 60;
//                 const totalPages = Math.ceil(uniqueCountries.length / countriesPerPage);
                
//                 if (page < 0 || page >= totalPages) {
//                     await ctx.answerCbQuery("❌ Неверная страница");
//                     return;
//                 }
                
//                 const startIndex = page * countriesPerPage;
//                 const endIndex = Math.min(startIndex + countriesPerPage, uniqueCountries.length);
//                 const pageCountries = uniqueCountries.slice(startIndex, endIndex);
                
//                 const keyboard = [];
//                 let row = [];
                
//                 pageCountries.forEach((country, index) => {
//                     const countryWithFlag = formatCountryWithFlag(country);
//                     row.push({ text: countryWithFlag, callback_data: `country_${country}` });
                    
//                     if (row.length === 3 || index === pageCountries.length - 1) {
//                         keyboard.push(row);
//                         row = [];
//                     }
//                 });
                
//                 if (totalPages > 1) {
//                     const paginationRow = [];
                    
//                     if (page > 0) {
//                         paginationRow.push({ 
//                             text: "◀️ Назад", 
//                             callback_data: `countries_page_${page - 1}` 
//                         });
//                     }
                    
//                     paginationRow.push({ 
//                         text: `Страна ${page + 1}/${totalPages}`, 
//                         callback_data: "countries_page_info" 
//                     });
                    
//                     if (page < totalPages - 1) {
//                         paginationRow.push({ 
//                             text: "Вперед ▶️", 
//                             callback_data: `countries_page_${page + 1}` 
//                         });
//                     }
                    
//                     if (paginationRow.length > 0) {
//                         keyboard.push(paginationRow);
//                     }
                    
//                     if (totalPages > 5) {
//                         const quickPagesRow = [];
//                         const pagesToShow = Math.min(5, totalPages);
                        
//                         for (let i = 0; i < pagesToShow; i++) {
//                             quickPagesRow.push({
//                                 text: i === page ? `• ${i + 1} •` : `${i + 1}`,
//                                 callback_data: `countries_page_${i}`
//                             });
//                         }
                        
//                         if (page >= pagesToShow) {
//                             quickPagesRow.push({ text: "...", callback_data: "countries_page_info" });
//                             quickPagesRow.push({
//                                 text: `• ${page + 1} •`,
//                                 callback_data: `countries_page_${page}`
//                             });
//                         }
                        
//                         if (quickPagesRow.length > 0) {
//                             keyboard.push(quickPagesRow);
//                         }
//                     }
//                 }
                
//                 keyboard.push([{ text: "📝 СОЗДАТЬ АНКЕТУ", web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } }]);
                
//                 if (isDemo) {
//                     keyboard.push([{ text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }]);
//                 }
                
//                 keyboard.push([{ text: "🔙 Назад", callback_data: "back_to_menu" }]);
                
//                 try {
//                     await ctx.editMessageText(
//                         isDemo ? 
//                         `👀 ДЕМО-РЕЖИМ: Выберите страну (показано по 3 анкеты на город)\n\n📄 Страница ${page + 1} из ${totalPages}\n🌍 Всего стран: ${uniqueCountries.length}\n\n💎 Для полного доступа Вы должны быть подписаны на наш канал @MagicYourClub и оплатить подписку` : 
//                         `🌍 Выберите страну (страница ${page + 1} из ${totalPages})\n📊 Всего стран: ${uniqueCountries.length}\n📍 Показано: ${pageCountries.length} стран`,
//                         {
//                             parse_mode: "HTML",
//                             reply_markup: { inline_keyboard: keyboard }
//                         }
//                     );
                    
//                     await ctx.answerCbQuery(`📄 Страница ${page + 1} из ${totalPages}`);
                    
//                 } catch (error) {
//                     console.error("❌ Ошибка обновления клавиатуры стран:", error);
//                     await ctx.answerCbQuery("❌ Ошибка загрузки");
//                 }
                
//             } catch (error) {
//                 console.error("❌ Ошибка пагинации стран:", error);
//                 await ctx.answerCbQuery("❌ Ошибка загрузки");
//             } finally {
//                 releaseUserLock(userId);
//             }
//         });
//     });

//     bot.command("countries_stats", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 const cacheType = await ensureProperCache(ctx);
//                 const isDemo = cacheType === 'demo';
                
//                 const uniqueCountries = await getUniqueCountries(isDemo);
//                 const totalCountries = uniqueCountries.length;
                
//                 const removeFlag = (countryName) => {
//                     if (!countryName) return countryName;
//                     const firstChar = countryName.charAt(0);
//                     const isEmojiFlag = firstChar.codePointAt(0) > 127;
                    
//                     if (isEmojiFlag && countryName.length > 2) {
//                         return countryName.substring(2).trim();
//                     }
//                     return countryName;
//                 };
                
//                 const regions = {
//                     europe: ['Россия', 'Украина', 'Беларусь', 'Германия', 'Франция', 'Италия', 'Испания', 'Польша', 'Нидерланды', 'Бельгия', 'Швейцария', 'Австрия', 'Португалия', 'Греция', 'Чехия', 'Швеция', 'Норвегия', 'Финляндия', 'Дания', 'Ирландия', 'Венгрия', 'Румыния', 'Болгария', 'Сербия', 'Хорватия', 'Словакия', 'Словения', 'Литва', 'Латвия', 'Эстония', 'Молдова', 'Албания'],
//                     asia: ['Казахстан', 'Турция', 'Китай', 'Япония', 'Южная Корея', 'Индия', 'Таиланд', 'ОАЭ', 'Узбекистан', 'Кыргызстан', 'Таджикистан', 'Туркменистан', 'Грузия', 'Армения', 'Азербайджан', 'Израиль'],
//                     america: ['США', 'Канада', 'Мексика', 'Бразилия'],
//                     africa: ['ЮАР', 'Египет'],
//                     oceania: ['Австралия']
//                 };
                
//                 let statsByRegion = {};
//                 Object.keys(regions).forEach(region => {
//                     const count = uniqueCountries.filter(country => {
//                         const cleanCountry = removeFlag(country);
//                         return regions[region].includes(cleanCountry);
//                     }).length;
//                     statsByRegion[region] = count;
//                 });
                
//                 const topCountries = uniqueCountries.slice(0, 10).map((country, i) => {
//                     return `${i + 1}. ${country}`;
//                 }).join('\n');
                
//                 const statsMessage = `
// 🌍 **СТАТИСТИКА СТРАН**

// 📊 **Общая информация:**
// • Всего стран: ${totalCountries}
// • Демо-режим: ${isDemo ? '✅' : '❌'}
// • Страниц: ${Math.ceil(totalCountries / 60)}

// 🗺️ **По регионам:**
// • Европа: ${statsByRegion.europe || 0}
// • Азия: ${statsByRegion.asia || 0}
// • Америка: ${statsByRegion.america || 0}
// • Африка: ${statsByRegion.africa || 0}
// • Океания: ${statsByRegion.oceania || 0}
// • Другие: ${totalCountries - Object.values(statsByRegion).reduce((a, b) => a + b, 0)}

// 📋 **Топ-10 стран:**
// ${topCountries}

// ${totalCountries > 10 ? `... и еще ${totalCountries - 10} стран` : ''}
//                 `;
                
//                 const msg = await ctx.reply(statsMessage, { parse_mode: "Markdown" });
//                 messageManager.track(ctx.chat.id, msg.message_id);
                
//             } catch (error) {
//                 console.error("❌ Ошибка команды countries_stats:", error);
//                 await ctx.reply("❌ Ошибка загрузки статистики стран");
//             }
//         });
//     });

//     bot.action("countries_page_info", async (ctx) => {
//         await ctx.answerCbQuery("📄 Используйте кнопки для навигации по странам");
//     });

//     bot.action(/^country_(.+)$/, async (ctx) => {
//     const userId = ctx.from.id;
    
//     if (!acquireUserLock(userId, 2500)) {
//         await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//         return;
//     }
    
//     await messageQueue.add(async () => {
//         let preloaderMsg = null;
        
//         try {
//             const country = ctx.match[1];
            
//             // 🔥 ДОБАВЛЯЕМ ПРЕЛОАДЕР
//             preloaderMsg = await sendPreloader(ctx, 'country', "🌍 ЗАГРУЗКА ГОРОДОВ");
            
//             const cacheType = await ensureProperCache(ctx);
//             const isDemo = cacheType === 'demo';
            
//             ctx.session = ctx.session || {};
//             ctx.session.profilesPage = 0;
//             ctx.session.filterCountry = country;
//             ctx.session.displayCountry = country;
//             ctx.session.filterCity = null;
//             ctx.session.isDemo = isDemo;

//             await messageManager.clear(ctx);
            
//             await messageManager.sendCitiesKeyboard(ctx, country, isDemo);
            
//             // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР
//             if (preloaderMsg) {
//                 await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
//             }
            
//             await ctx.answerCbQuery();
            
//         } catch (error) {
//             console.error("❌ Ошибка обработки выбора страны:", error);
            
//             // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР ПРИ ОШИБКЕ
//             if (preloaderMsg) {
//                 try {
//                     await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
//                 } catch (e) {}
//             }
            
//         } finally {
//             releaseUserLock(userId);
//         }
//     });
// });
    
//    bot.action(/^city_(.+)$/, async (ctx) => {
//     const userId = ctx.from.id;
    
//     if (!acquireUserLock(userId, 30000)) {
//         await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//         return;
//     }
    
//     await messageQueue.add(async () => {
//         let preloaderMsg = null;
        
//         try {
//             const city = ctx.match[1];
            
//             await ctx.answerCbQuery("🔍 Ищем анкеты...");
            
//             // 🔥 ДОБАВЛЯЕМ ПРЕЛОАДЕР ДЛЯ ПОИСКА ГОРОДА
//             preloaderMsg = await sendPreloader(ctx, 'city', "🔍 ПОИСК АНКЕТ");
            
//             const cacheType = await ensureProperCache(ctx);
//             const isDemo = cacheType === 'demo';
            
//             console.log(`🏙️ [CITY] Пользователь ${userId} выбрал город ${city}, демо=${isDemo}`);
            
//             ctx.session = ctx.session || {};
//             ctx.session.profilesPage = 0;
//             ctx.session.filterCity = city;
//             ctx.session.isDemo = isDemo;

//             await messageManager.clear(ctx, true, true);
            
//             console.log(`🔍 [CITY] Загружаем анкеты для города ${city}...`);
//             const profiles = await getProfilesPage(0, ctx.session.filterCountry, ctx.session.ageRange, city, isDemo);

//             // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР ПОСЛЕ ЗАГРУЗКИ
//             if (preloaderMsg) {
//                 await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
//             }
//             preloaderMsg = null;

//             if (!profiles.length) {
//                 const msg = await ctx.reply(`❌ Анкет из города "${city}" не найдено`);
//                 messageManager.track(ctx.chat.id, msg.message_id);
//                 return;
//             }

//             console.log(`✅ [CITY] Найдено ${profiles.length} анкет для города ${city}`);

//             const foundMsg = await ctx.reply(
//                 `📍 <b>Город:</b> ${city}\n` +
//                 `🌍 <b>Страна:</b> ${ctx.session.filterCountry}\n` +
//                 `👀 <b>Режим:</b> ${isDemo ? 'Демо (3 анкеты на город)' : 'Полный доступ'}`,
//                 { parse_mode: "HTML" }
//             );
//             messageManager.track(ctx.chat.id, foundMsg.message_id);

//             for (let i = 0; i < profiles.length; i++) {
//                 const isLast = i === profiles.length - 1;
                
//                 if (i === 0 && profiles.length > 1) {
//                     const progressMsg = await ctx.reply(
//                         `📤 <b>Отправляем анкеты...</b>\n` +
//                         `📊 <i>Прогресс: 1/${profiles.length}</i>`,
//                         { parse_mode: "HTML" }
//                     );
//                     messageManager.track(ctx.chat.id, progressMsg.message_id);
//                 }
                
//                 await sendProfile(ctx, profiles[i], 0, profiles.length, isLast, isDemo);
                
//                 if (!isLast) {
//                     await new Promise((resolve) => setTimeout(resolve, 500));
//                 }
//             }

//         } catch (error) {
//             console.error("❌ Ошибка обработки выбора города:", error);
            
//             // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР ПРИ ОШИБКЕ
//             if (preloaderMsg) {
//                 try {
//                     await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
//                 } catch (e) {
//                     console.log("Не удалось удалить прелоадер при ошибке:", e.message);
//                 }
//             }
            
//             try {
//                 await ctx.reply(
//                     "❌ <b>Произошла ошибка при загрузке анкет</b>\n\n" +
//                     "⚠️ <i>Попробуйте еще раз через несколько секунд</i>",
//                     { parse_mode: "HTML" }
//                 );
//             } catch (e) {
//                 console.error("Не удалось отправить сообщение об ошибке:", e);
//             }
//         } finally {
//             releaseUserLock(userId);
//         }
//     });
// }); 

//     bot.action(/^cities_page_(.+)_(\d+)$/, async (ctx) => {
//         const userId = ctx.from.id;
        
//         if (!acquireUserLock(userId, 2000)) {
//             await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//             return;
//         }
        
//         await messageQueue.add(async () => {
//             try {
//                 const [_, country, page] = ctx.match;
//                 const pageNum = parseInt(page);
                
//                 const cacheType = await ensureProperCache(ctx);
//                 const isDemo = cacheType === 'demo';
                
//                 const cities = await getUniqueCitiesForCountry(country, isDemo);
                
//                 if (!cities || cities.length === 0) {
//                     await ctx.answerCbQuery("❌ Нет доступных городов");
//                     return;
//                 }

//                 const citiesPerPage = 50;
//                 const totalPages = Math.ceil(cities.length / citiesPerPage);
                
//                 if (pageNum < 0 || pageNum >= totalPages) {
//                     await ctx.answerCbQuery("❌ Неверная страница");
//                     return;
//                 }

//                 const keyboard = [];
//                 let row = [];

//                 const startIndex = pageNum * citiesPerPage;
//                 const endIndex = Math.min(startIndex + citiesPerPage, cities.length);
//                 const pageCities = cities.slice(startIndex, endIndex);

//                 pageCities.forEach((city, index) => {
//                     row.push({ text: city, callback_data: `city_${city}` });
//                     if (row.length === 3 || index === pageCities.length - 1) {
//                         keyboard.push(row);
//                         row = [];
//                     }
//                 });

//                 const paginationRow = [];
//                 if (totalPages > 1) {
//                     if (pageNum > 0) {
//                         paginationRow.push({ 
//                             text: "◀️ Предыдущие", 
//                             callback_data: `cities_page_${country}_${pageNum - 1}` 
//                         });
//                     }
                    
//                     paginationRow.push({ 
//                         text: `${pageNum + 1}/${totalPages}`, 
//                         callback_data: "cities_page_info" 
//                     });
                    
//                     if (pageNum < totalPages - 1) {
//                         paginationRow.push({ 
//                             text: "Следующие ▶️", 
//                             callback_data: `cities_page_${country}_${pageNum + 1}` 
//                         });
//                     }
                    
//                     if (paginationRow.length > 0) {
//                         keyboard.push(paginationRow);
//                     }
//                 }

//                 keyboard.push([{ 
//                     text: "📝 СОЗДАТЬ АНКЕТУ", 
//                     web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } 
//                 }]);

//                 if (isDemo) {
//                     keyboard.push([{ 
//                         text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", 
//                         callback_data: "get_full_access" 
//                     }]);
//                 }

//                 keyboard.push([{ 
//                     text: "🔙 Назад к странам", 
//                     callback_data: "back_to_countries" 
//                 }]);

//                 try {
//                     await ctx.editMessageReplyMarkup({
//                         inline_keyboard: keyboard
//                     });
//                     await ctx.answerCbQuery(`📄 Страница ${pageNum + 1} из ${totalPages}`);
//                 } catch (error) {
//                     console.error("❌ Ошибка обновления клавиатуры городов:", error);
//                     await ctx.answerCbQuery("❌ Ошибка обновления");
//                 }
                
//             } catch (error) {
//                 console.error("❌ Ошибка пагинации городов:", error);
//                 await ctx.answerCbQuery("❌ Ошибка загрузки");
//             } finally {
//                 releaseUserLock(userId);
//             }
//         });
//     });

//     bot.action("cities_page_info", async (ctx) => {
//         await ctx.answerCbQuery("📄 Используйте кнопки для навигации по городам");
//     });

//     bot.action("back_to_countries", async (ctx) => {
//         const userId = ctx.from.id;
        
//         if (!acquireUserLock(userId, 2000)) {
//             await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//             return;
//         }
        
//         await messageQueue.add(async () => {
//             try {
//                 const cacheType = await ensureProperCache(ctx);
//                 const isDemo = cacheType === 'demo';
                
//                 await messageManager.clear(ctx, false, true);
//                 await messageManager.sendCountriesKeyboard(ctx, isDemo);
//                 await ctx.answerCbQuery("✅ Возврат к странам");
                
//             } catch (error) {
//                 console.error("❌ Ошибка возврата к странам:", error);
//                 try {
//                     await ctx.answerCbQuery("❌ Ошибка возврата");
//                 } catch (e) {
//                 }
//             } finally {
//                 releaseUserLock(userId);
//             }
//         });
//     });
    
//     bot.action("back_to_menu", async (ctx) => {
//         const userId = ctx.from.id;
        
//         if (!acquireUserLock(userId, 2000)) {
//             await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//             return;
//         }
        
//         await messageQueue.add(async () => {
//             try {
//                 await messageManager.clear(ctx);
//                 await messageManager.sendMainMenu(ctx);
//                 await ctx.answerCbQuery();
//             } catch (error) {
//                 console.error("❌ Ошибка возврата в меню:", error);
//             } finally {
//                 releaseUserLock(userId);
//             }
//         });
//     });

//     bot.action("filter_by_age", async (ctx) => {
//         const userId = ctx.from.id;
        
//         if (!acquireUserLock(userId, 2000)) {
//             await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//             return;
//         }
        
//         await messageQueue.add(async () => {
//             try {
//                 const chatId = ctx.chat.id;
//                 const countryKeyboardId = chatStorage.countryKeyboard.get(chatId);
//                 const cityKeyboardId = chatStorage.cityKeyboard.get(chatId);

//                 await messageManager.clear(ctx, true, true);
//                 if (countryKeyboardId) chatStorage.countryKeyboard.set(chatId, countryKeyboardId);
//                 if (cityKeyboardId) chatStorage.cityKeyboard.set(chatId, cityKeyboardId);

//                 const keyboard = AGE_RANGES.map((range) => [
//                     { text: range.label, callback_data: `age_range_${range.label}` },
//                 ]);
                
//                 keyboard.push([{ text: "❌ Сбросить фильтр возраста", callback_data: "age_range_reset" }]);
                
//                 const hasFullAccess = await checkFullAccess(ctx);
//                 if (!hasFullAccess) {
//                     keyboard.push([{ text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }]);
//                 }
                
//                 keyboard.push([{ text: "🔙 Назад в меню", callback_data: "back_to_menu" }]);

//                 const msg = await ctx.reply("Выберите возрастной диапазон:", { reply_markup: { inline_keyboard: keyboard } });
//                 messageManager.track(ctx.chat.id, msg.message_id);
//                 await ctx.answerCbQuery();
//             } catch (error) {
//                 console.error("❌ Ошибка фильтра по возрасту:", error);
//             } finally {
//                 releaseUserLock(userId);
//             }
//         });
//     });

//     bot.action(/^age_range_(.+)$/, async (ctx) => {
//         const userId = ctx.from.id;
        
//         if (!acquireUserLock(userId, 3000)) {
//             await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//             return;
//         }
        
//         await messageQueue.add(async () => {
//             try {
//                 const [_, range] = ctx.match;
                
//                 const cacheType = await ensureProperCache(ctx);
//                 const isDemo = cacheType === 'demo';
                
//                 ctx.session = ctx.session || {};
//                 ctx.session.profilesPage = 0;
//                 ctx.session.isDemo = isDemo;

//                 if (range === "reset") {
//                     ctx.session.ageRange = null;
//                     await ctx.answerCbQuery("✅ Фильтр по возрасту сброшен");
//                 } else {
//                     const selectedRange = AGE_RANGES.find((r) => r.label === range);
//                     if (selectedRange) {
//                         ctx.session.ageRange = selectedRange;
//                         await ctx.answerCbQuery(`✅ Установлен фильтр: ${range} лет`);
//                     }
//                 }

//                 const currentCountry = ctx.session.filterCountry;
//                 const currentCity = ctx.session.filterCity;
//                 const chatId = ctx.chat.id;
//                 const countryKeyboardId = chatStorage.countryKeyboard.get(chatId);
//                 const cityKeyboardId = chatStorage.cityKeyboard.get(chatId);

//                 await messageManager.clear(ctx, true, true);
//                 if (countryKeyboardId) chatStorage.countryKeyboard.set(chatId, countryKeyboardId);
//                 if (cityKeyboardId) chatStorage.cityKeyboard.set(chatId, cityKeyboardId);

//                 const profiles = await getProfilesPage(0, currentCountry, ctx.session.ageRange, currentCity, isDemo);

//                 if (!profiles.length) {
//                     const msg = await ctx.reply("Анкет по выбранным критериям не найдено.");
//                     messageManager.track(ctx.chat.id, msg.message_id);
//                     return;
//                 }

//                 let filtersText = "🎯 Применены фильтры: ";
//                 if (ctx.session.ageRange) filtersText += `Возраст: ${ctx.session.ageRange.label}`;
//                 if (currentCountry) filtersText += `, Страна: ${currentCountry}`;
//                 if (currentCity) filtersText += `, Город: ${currentCity}`;
                
//                 if (isDemo) {
//                     filtersText += "\n👀 ДЕМО-РЕЖИМ: показано по 3 анкеты на город";
//                 }
                
//                 const filtersMsg = await ctx.reply(filtersText);
//                 messageManager.track(ctx.chat.id, filtersMsg.message_id);

//                 for (let i = 0; i < profiles.length; i++) {
//                     const isLast = i === profiles.length - 1;
//                     await sendProfile(ctx, profiles[i], 0, profiles.length, isLast, isDemo);
//                     if (!isLast) await new Promise((resolve) => setTimeout(resolve, 300));
//                 }

//                 if (currentCountry && !currentCity) {
//                     await messageManager.sendCitiesKeyboard(ctx, currentCountry, isDemo);
//                 } else {
//                     await messageManager.sendMainMenu(ctx);
//                 }
//             } catch (error) {
//                 console.error("❌ Ошибка обработки возрастного диапазона:", error);
//                 await ctx.answerCbQuery("❌ Ошибка применения фильтра");
//             } finally {
//                 releaseUserLock(userId);
//             }
//         });
//     });

//     // ===================== УЛУЧШЕННАЯ ПРОВЕРКА ПОДПИСКИ НА КАНАЛ =====================
//    bot.action("check_channel_subscription", async (ctx) => {
//     await messageQueue.add(async () => {
//         try {
//             const userId = ctx.from.id;
//             await ctx.answerCbQuery("🔍 Проверяем подписку на канал...");
            
//             console.log(`🔍 [CHANNEL CHECK] Пользователь ${userId} проверяет подписку на канал`);
            
//             // 🔥 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: ОЧИЩАЕМ ВСЕ КЭШИ КАК В "ВСЕ СТРАНЫ"
//             console.log(`🔄 [CHANNEL CHECK] Очищаем кэши для ${userId}`);
            
//             // Очищаем ВСЕ кэши как в all_countries_with_check
//             subscriptionCache.del(`subscription:${userId}`);
//             channelSubscriptionCache.del(`channel:${userId}`);
//             userCacheStatus.del(`cache_status:${userId}`);
            
//             // Очищаем сессию
//             if (ctx.session) {
//                 delete ctx.session.fullAccessCache;
//                 delete ctx.session.cacheSession;
//                 delete ctx.session.recentChannelCheck;
//             }
            
//             // 🔥 ПРОВЕРЯЕМ ПОДПИСКУ И КАНАЛ ЗАНОВО (ТАК ЖЕ КАК В "ВСЕ СТРАНЫ")
//             const hasSubscription = await checkSubscription(userId);
//             const hasChannelSubscription = await checkChannelSubscription(ctx);
//             const hasFullAccess = hasSubscription && hasChannelSubscription;
            
//             console.log(`✅ [CHANNEL CHECK RESULT] ${userId}: подписка=${hasSubscription}, канал=${hasChannelSubscription}, полный доступ=${hasFullAccess}`);
            
//             // 🔥 ЕСЛИ ПОЛНЫЙ ДОСТУП - ПОКАЗЫВАЕМ УСПЕХ И ПРЕДЛАГАЕМ ПЕРЕЙТИ К АНКЕТАМ
//             if (hasFullAccess) {
//                 console.log(`🎉 [CHANNEL CHECK SUCCESS] ${userId} получил полный доступ!`);
                
//                 // 🔥 ТАКЖЕ ОЧИЩАЕМ КЭШ ПОЛЬЗОВАТЕЛЯ (ВАЖНО!)
//                 cacheManager.setUserCacheStatus(userId, 'full');
                
//                 // Обновляем сессию
//                 if (!ctx.session) ctx.session = {};
//                 ctx.session.fullAccessCache = {
//                     value: true,
//                     timestamp: Date.now(),
//                     subscription: hasSubscription,
//                     channel: hasChannelSubscription
//                 };
                
//                 ctx.session.cacheSession = {
//                     type: 'full',
//                     timestamp: Date.now(),
//                     fullAccess: true
//                 };
                
//                 const successKeyboard = {
//                     inline_keyboard: [
//                         [
//                             {
//                                 text: "🌍 СМОТРЕТЬ АНКЕТЫ",
//                                 callback_data: "all_countries_with_check",
//                             },
//                         ],
//                         [
//                             { text: "🔙 Назад", callback_data: "back_to_main" },
//                             { text: "🧹 Очистить экран", callback_data: "clear_screen" },
//                         ],
//                     ],
//                 };

//                 await ctx.reply(
//                     `
// 🎉 <b>ПОДПИСКА ПОДТВЕРЖДЕНА!</b>

// ✅ Все условия выполнены!
// ✨ Теперь у вас полный доступ ко всем анкетам!

// <b>Что теперь доступно:</b>
// • 👤 Все 70,000+ анкет
// • 📞 Контакты профилей (телефоны, Telegram, WhatsApp)
// • 🌍 Неограниченный поиск по странам и городам
// • ⚡ Максимальная скорость работы

// <em>Нажмите "СМОТРЕТЬ АНКЕТЫ" чтобы начать!</em>
//                     `,
//                     {
//                         parse_mode: "HTML",
//                         reply_markup: successKeyboard,
//                     }
//                 );
                
//             } else if (hasSubscription && !hasChannelSubscription) {
//                 // 🔥 ЕСТЬ ПОДПИСКА, НО КАНАЛ ВСЕ ЕЩЕ НЕ НАЙДЕН
//                 await ctx.reply(`
// ❌ <b>ПОДПИСКА НА КАНАЛ НЕ НАЙДЕНА</b>

// ✅ Подписка: <b>АКТИВНА</b>
// ❌ Канал: <b>НЕ ПОДПИСАН</b>

// <em>Мы все еще не видим вашу подписку на @MagicYourClub</em>

// <b>Что нужно сделать:</b>
// 1. Убедитесь, что вы подписались на канал @MagicYourClub
// 2. Если подписались - попробуйте выйти и зайти в канал
// 3. Проверьте, что используете тот же аккаунт Telegram
// 4. Подождите 1-2 минуты и проверьте снова

// <code>Если проблема persists, напишите в поддержку @MagicAdd</code>
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
//                             [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }],
//                             [{ text: "🔄 Проверить подписку", callback_data: "check_subscription_now" }]
//                         ]
//                     }
//                 });
                
//             } else if (!hasSubscription && hasChannelSubscription) {
//                 // 🔥 ЕСТЬ КАНАЛ, НЕТ ПОДПИСКИ
//                 await ctx.reply(`
// ✅ <b>Вы подписаны на канал!</b>

// ❌ <b>Но у вас нет активной подписки</b>

// Что делать:
// 1. Нажмите "💎 КУПИТЬ ПОДПИСКУ" для оплаты
// 2. После оплаты проверьте доступ снова

// <em>Без подписки доступ ограничен демо-режимом (3 анкеты на город)</em>
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }],
//                             [{ text: "🔄 Проверить подписку", callback_data: "check_subscription_now" }],
//                             [{ text: "🌍 ВСЕ СТРАНЫ (демо)", callback_data: "all_countries_with_check" }]
//                         ]
//                     }
//                 });
                
//             } else {
//                 // 🔥 НИЧЕГО НЕТ
//                 await ctx.reply(`
// ❌ <b>ПОДПИСКА НА КАНАЛ НЕ НАЙДЕНА</b>

// Мы не видим вашу подписку на @MagicYourClub
// И у вас нет активной подписки

// <b>Что нужно сделать:</b>
// 1. Нажмите "📢 ПОДПИСАТЬСЯ НА КАНАЛ"
// 2. После подписки нажмите "✅ Я ПОДПИСАЛСЯ" снова
// 3. Купите подписку для полного доступа

// <em>Если вы уже подписаны, но бот не видит подписку:</em>
// • Попробуйте выйти и зайти в канал
// • Убедитесь, что вы используете тот же аккаунт Telegram
// • Подождите 1-2 минуты и проверьте снова
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
//                             [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }],
//                             [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }]
//                         ]
//                     }
//                 });
//             }
            
//         } catch (error) {
//             console.error("Ошибка проверки подписки на канал:", error);
//             await ctx.answerCbQuery("❌ Ошибка проверки");
//         }
//     });
// });

//     // ===================== ПРОВЕРКА ТОЛЬКО ПОДПИСКИ =====================
//    bot.action("check_subscription_now", async (ctx) => {
//     await messageQueue.add(async () => {
//         try {
//             const userId = ctx.from.id;
//             await ctx.answerCbQuery("🔍 Проверяем подписку...");
            
//             console.log(`🔍 [SUBSCRIPTION CHECK] Пользователь ${userId} проверяет подписку`);
            
//             // 🔥 ТАКЖЕ ОЧИЩАЕМ КЭШИ КАК В "ВСЕ СТРАНЫ"
//             subscriptionCache.del(`subscription:${userId}`);
            
//             const hasSubscription = await checkSubscription(userId);
//             const hasChannelSubscription = await checkChannelSubscription(ctx);
//             const hasFullAccess = hasSubscription && hasChannelSubscription;
            
//             if (hasFullAccess) {
//                 // 🔥 ОЧИЩАЕМ ВСЕ КЭШИ И ОБНОВЛЯЕМ СТАТУС
//                 channelSubscriptionCache.del(`channel:${userId}`);
//                 userCacheStatus.del(`cache_status:${userId}`);
//                 cacheManager.setUserCacheStatus(userId, 'full');
                
//                 await ctx.reply(`
// 🎉 <b>ВСЕ УСЛОВИЯ ВЫПОЛНЕНЫ!</b>

// ✅ Подписка: <b>АКТИВНА</b>
// ✅ Канал: <b>ПОДПИСАН</b>

// ✨ Теперь у вас полный доступ ко всем анкетам!

// <em>Нажмите "🌍 ВСЕ СТРАНЫ" для начала поиска</em>
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
//                             [{ text: "🔄 Проверить еще раз", callback_data: "check_all_access" }]
//                         ]
//                     }
//                 });
//             } else if (hasSubscription) {
//                 await ctx.reply(`
// ✅ <b>ПОДПИСКА АКТИВНА!</b>

// ❌ <b>Но нет подписки на канал @MagicYourClub</b>

// Что нужно сделать:
// 1. Подпишитесь на @MagicYourClub
// 2. Нажмите "✅ Я ПОДПИСАЛСЯ НА КАНАЛ"
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
//                             [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }]
//                         ]
//                     }
//                 });
//             } else {
//                 await ctx.reply(`
// ❌ <b>ПОДПИСКА НЕ НАЙДЕНА</b>

// У вас нет активной подписки.

// <b>Что нужно сделать:</b>
// 1. Нажмите "💎 КУПИТЬ ПОДПИСКУ" для оплаты
// 2. После оплаты проверьте снова
//                 `, {
//                     parse_mode: "HTML",
//                     reply_markup: {
//                         inline_keyboard: [
//                             [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }],
//                             [{ text: "🔄 Проверить снова", callback_data: "check_subscription_now" }]
//                         ]
//                     }
//                 });
//             }
            
//         } catch (error) {
//             console.error("Ошибка проверки подписки:", error);
//             await ctx.answerCbQuery("❌ Ошибка проверки");
//         }
//     });
// });

//     bot.action("force_refresh_access", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 const userId = ctx.from.id;
//                 await ctx.answerCbQuery("🔄 Принудительное обновление...");
                
//                 console.log(`🔄 [FORCE REFRESH] Полный сброс кэшей для ${userId}`);
                
//                 // Очищаем ВСЕ кэши пользователя
//                 userCacheStatus.del(`cache_status:${userId}`);
//                 subscriptionCache.del(`subscription:${userId}`);
//                 channelSubscriptionCache.del(`channel:${userId}`);
                
//                 // Очищаем сессию
//                 if (ctx.session) {
//                     ctx.session = {};
//                 }
                
//                 // Проверяем заново
//                 const [hasSubscription, hasChannelSubscription] = await Promise.all([
//                     checkSubscription(userId),
//                     checkChannelSubscription(ctx)
//                 ]);
                
//                 // Устанавливаем правильный статус кэша
//                 if (hasSubscription && hasChannelSubscription) {
//                     cacheManager.setUserCacheStatus(userId, 'full');
                    
//                     await ctx.reply(`
// 🔄 <b>СТАТУС ОБНОВЛЕН!</b>

// ✅ Подписка: ${hasSubscription ? 'АКТИВНА' : 'НЕ АКТИВНА'}
// ✅ Канал: ${hasChannelSubscription ? 'ПОДПИСАН' : 'НЕ ПОДПИСАН'}

// ${hasSubscription && hasChannelSubscription ? 
//                         '🎉 <b>Теперь у вас полный доступ!</b>\nНажмите "🌍 ВСЕ СТРАНЫ"' : 
//                         '❌ <b>Не все условия выполнены</b>\nПроверьте что отсутствует выше'}
//                     `, {
//                         parse_mode: "HTML",
//                         reply_markup: {
//                             inline_keyboard: [
//                                 [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
//                                 [{ text: "🔄 Проверить еще раз", callback_data: "check_all_access" }]
//                             ]
//                         }
//                     });
//                 }
                
//             } catch (error) {
//                 console.error("Ошибка принудительного обновления:", error);
//                 await ctx.answerCbQuery("❌ Ошибка обновления");
//             }
//         });
//     });

// //     bot.command("refresh_access", async (ctx) => {
// //         await messageQueue.add(async () => {
// //             try {
// //                 const userId = ctx.from.id;
// //                 console.log(`🔄 [REFRESH COMMAND] Полный сброс для ${userId}`);
                
// //                 // Очищаем все кэши
// //                 userCacheStatus.del(`cache_status:${userId}`);
// //                 subscriptionCache.del(`subscription:${userId}`);
// //                 channelSubscriptionCache.del(`channel:${userId}`);
                
// //                 if (ctx.session) {
// //                     ctx.session = {};
// //                 }
                
// //                 // Проверяем заново
// //                 const [hasSubscription, hasChannelSubscription] = await Promise.all([
// //                     checkSubscription(userId),
// //                     checkChannelSubscription(ctx)
// //                 ]);
                
// //                 let statusMessage = `
// // 🔄 <b>СИСТЕМНЫЙ СБРОС</b>

// // 👤 Пользователь: <code>${userId}</code>
// // 💎 Подписка: ${hasSubscription ? '✅ АКТИВНА' : '❌ НЕ АКТИВНА'}
// // 📢 Канал: ${hasChannelSubscription ? '✅ ПОДПИСАН' : '❌ НЕ ПОДПИСАН'}

// // <b>Результат:</b>
// //                 `;
                
// //                 if (hasSubscription && hasChannelSubscription) {
// //                     cacheManager.setUserCacheStatus(userId, 'full');
// //                     statusMessage += '\n🎉 <b>ПОЛНЫЙ ДОСТУП АКТИВИРОВАН!</b>';
                    
// //                     await ctx.reply(statusMessage, {
// //                         parse_mode: "HTML",
// //                         reply_markup: {
// //                             inline_keyboard: [
// //                                 [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
// //                                 [{ text: "🔄 Проверить анкеты", callback_data: "force_refresh_access" }]
// //                             ]
// //                         }
// //                     });
// //                 } else {
// //                     statusMessage += '\n❌ <b>Не все условия выполнены</b>';
                    
// //                     const keyboard = {
// //                         inline_keyboard: [
// //                             !hasSubscription ? [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }] : [],
// //                             !hasChannelSubscription ? [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }] : [],
// //                             [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
// //                             [{ text: "🔄 Проверить снова", callback_data: "check_all_access" }]
// //                         ].filter(row => row.length > 0)
// //                     };
                    
// //                     await ctx.reply(statusMessage, {
// //                         parse_mode: "HTML",
// //                         reply_markup: keyboard
// //                     });
// //                 }
                
// //             } catch (error) {
// //                 console.error("❌ Ошибка команды refresh_access:", error);
// //                 await ctx.reply("❌ Ошибка обновления статуса");
// //             }
// //         });
// //     });

//     // ===================== ПРОВЕРКА ВСЕХ УСЛОВИЙ =====================
//     bot.action("check_all_access", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 const userId = ctx.from.id;
//                 await ctx.answerCbQuery("🔍 Проверяем все условия...");
                
//                 console.log(`🔍 [FULL CHECK] Полная проверка доступа для ${userId}`);
                
//                 // Очищаем ВСЕ кэши
//                 subscriptionCache.del(`subscription:${userId}`);
//                 channelSubscriptionCache.del(`channel:${userId}`);
                
//                 const [hasSubscription, hasChannelSubscription] = await Promise.all([
//                     checkSubscription(userId),
//                     checkChannelSubscription(ctx)
//                 ]);
                
//                 if (hasSubscription && hasChannelSubscription) {
//                     // ОБНОВЛЯЕМ СТАТУС В КЭШЕ ПОЛЬЗОВАТЕЛЯ
//                     cacheManager.setUserCacheStatus(userId, 'full');
                    
//                     // Очищаем сессию
//                     if (ctx.session) {
//                         ctx.session.cacheType = 'full';
//                         ctx.session.hasFullAccess = true;
//                     }
                    
//                     await ctx.reply(`
// 🎉 <b>ВСЕ УСЛОВИЯ ВЫПОЛНЕНЫ!</b>

// ✅ Подписка: АКТИВНА
// ✅ Канал: ПОДПИСАН

// <b>Полный доступ активирован!</b>

// Теперь вы можете:
// • 🌍 Просматривать все страны
// • 👤 Видеть все контакты в анкетах
// • 🚀 Использовать полный функционал

// <code>Нажмите "🌍 ВСЕ СТРАНЫ" чтобы начать</code>
//                     `, {
//                         parse_mode: "HTML",
//                         reply_markup: {
//                             inline_keyboard: [
//                                 [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
//                                 [{ text: "🧹 Очистить экран", callback_data: "clear_screen" }]
//                             ]
//                         }
//                     });
                    
//                 } else {
//                     let missing = [];
//                     if (!hasSubscription) missing.push("активной подписки");
//                     if (!hasChannelSubscription) missing.push("подписки на канал @MagicYourClub");
                    
//                     await ctx.reply(`
// ❌ <b>УСЛОВИЯ НЕ ВЫПОЛНЕНЫ</b>

// Отсутствует: ${missing.join(" и ")}

// <b>Что нужно сделать:</b>
// ${!hasSubscription ? "1. 💎 Купите подписку через 'ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП'\n" : ""}
// ${!hasChannelSubscription ? "2. 📢 Подпишитесь на канал @MagicYourClub\n" : ""}
// 3. 🔄 Проверьте снова после выполнения условий
//                     `, {
//                         parse_mode: "HTML",
//                         reply_markup: {
//                             inline_keyboard: [
//                                 !hasSubscription ? [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }] : [],
//                                 !hasChannelSubscription ? [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }] : [],
//                                 [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
//                                 [{ text: "🔄 Проверить снова", callback_data: "check_all_access" }]
//                             ].filter(row => row.length > 0)
//                         }
//                     });
//                 }
                
//             } catch (error) {
//                 console.error("Ошибка полной проверки:", error);
//                 await ctx.answerCbQuery("❌ Ошибка проверки");
//             }
//         });
//     });

//     bot.action(/^page_(first|prev|next|last|\d+)_(\d+)$/, async (ctx) => {
//         const userId = ctx.from.id;
        
//         if (!acquireUserLock(userId, 2500)) {
//             console.log(`⏳ [LOCK] Пользователь ${userId} уже выполняет действие, игнорируем клик`);
//             try {
//                 await ctx.answerCbQuery("⏳ Подождите, загружаем...");
//             } catch (e) {
//             }
//             return;
//         }
        
//         await messageQueue.add(async () => {
//             try {
//                 const [_, action, currentPage] = ctx.match;
//                 let newPage = parseInt(currentPage);

//                 if (action === "first") newPage = 0;
//                 else if (action === "prev") newPage = Math.max(0, newPage - 1);
//                 else if (action === "next") newPage = newPage + 1;
//                 else if (action === "last") {
//                     const isDemo = ctx.session?.isDemo || false;
//                     const filterKey = `country:${ctx.session.filterCountry || 'all'}:age:${ctx.session.ageRange?.label || 'all'}:city:${ctx.session.filterCity || 'all'}`;
//                     const filteredProfiles = isDemo ? 
//                         cacheManager.getCachedDemoFilteredProfiles(filterKey) : 
//                         cacheManager.getCachedFilteredProfiles(filterKey);
//                     newPage = Math.ceil((filteredProfiles?.length || 0) / SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE) - 1;
//                 } else {
//                     newPage = parseInt(action);
//                 }

//                 await messageManager.clear(ctx, true);
                
//                 ctx.session = ctx.session || {};
//                 const isDemo = ctx.session.isDemo || false;
//                 const profiles = await getProfilesPage(newPage, ctx.session.filterCountry, ctx.session.ageRange, ctx.session.filterCity, isDemo);

//                 if (profiles.length) {
//                     ctx.session.profilesPage = newPage;

//                     for (let i = 0; i < profiles.length; i++) {
//                         const isLast = i === profiles.length - 1;
//                         await sendProfile(ctx, profiles[i], newPage, profiles.length, isLast, isDemo);
//                         if (!isLast) await new Promise((resolve) => setTimeout(resolve, 300));
//                     }
                    
//                     await ctx.answerCbQuery(`📄 Страница ${newPage + 1}`);
//                 } else {
//                     const msg = await ctx.reply("Больше анкет нет");
//                     messageManager.track(ctx.chat.id, msg.message_id);
//                     await ctx.answerCbQuery("❌ Больше анкет нет");
//                 }
                
//             } catch (error) {
//                 console.error("❌ Ошибка пагинации:", error);
//                 try {
//                     await ctx.answerCbQuery("❌ Ошибка загрузки");
//                 } catch (e) {
//                 }
//             } finally {
//                 releaseUserLock(userId);
//             }
//         });
//     });

//     bot.action("clear_screen", async (ctx) => {
//         const userId = ctx.from.id;
        
//         if (!acquireUserLock(userId, 2000)) {
//             await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
//             return;
//         }
        
//         await messageQueue.add(async () => {
//             try {
//                 await messageManager.clear(ctx);
//                 await ctx.answerCbQuery("Экран очищен");
//             } catch (error) {
//                 console.error("❌ Ошибка очистки:", error);
//                 await ctx.answerCbQuery("Ошибка при очистке");
//             } finally {
//                 releaseUserLock(userId);
//             }
//         });
//     });

//     // ===================== ФУНКЦИЯ ОТПРАВКИ ПРОФИЛЯ =====================
//     const sendProfile = async (ctx, profile, page, total, isLast, isDemo = false) => {
//         return messageQueue.add(async () => {
//             try {
//                 const fullProfile = {
//                     id: profile.id,
//                     name: profile.n || profile.name,
//                     age: profile.a || profile.age, 
//                     country: profile.c || profile.country,
//                     city: profile.ct || profile.city,
//                     about: profile.ab || profile.about,
//                     photoUrl: profile.p || profile.photoUrl,
//                     photos: profile.phs || profile.photos || [],
//                     telegram: profile.tg || profile.telegram,
//                     phone: profile.tel || profile.phone,
//                     whatsapp: profile.wa || profile.whatsapp,
//                     createdAt: profile.ca || profile.createdAt,
//                     isDemo: profile.isDemo
//                 };

//                 console.log(`🔍 [DEBUG] Профиль ${fullProfile.name}: фото URL=${fullProfile.photoUrl}, галерея=${fullProfile.photos?.length || 0} фото`);

//                 const cleanText = (text) => {
//                     if (!text) return "";
//                     return text.replace(/[^\x00-\x7F\u0400-\u04FF\u0500-\u052F\u2DE0-\u2DFF\uA640-\uA69F]/g, '')
//                               .replace(/[^\w\s\u0400-\u04FF\u0500-\u052F\u2DE0-\u2DFF\uA640-\uA69F.,!?;:()\-+=\[\]{}@#$%^&*<>\/\\|'"`~]/g, '')
//                               .trim();
//                 };

//                 const about = cleanText(fullProfile.about)?.length > SCALING_CONFIG.PERFORMANCE.MAX_CAPTION_LENGTH
//                     ? cleanText(fullProfile.about).substring(0, SCALING_CONFIG.PERFORMANCE.MAX_CAPTION_LENGTH - 3) + "..."
//                     : cleanText(fullProfile.about) || "";

//                 if (isDemo || fullProfile.isDemo) {
//                     const demoCaption = `
// 👤 <b>${cleanText(fullProfile.name)}</b>, ${fullProfile.age}
// -------------------------------
// ${cleanText(fullProfile.country)},📍${cleanText(fullProfile.city)}
// -------------------------------
// <em>${about.length > 300 ? about.substring(0, 300) + `...<a href="http://t.me/magicboss_bot/magic">читать полностью в ✨Magic</a>` : about}</em>
// 🔹🔹🔹🔹🔹🔹🔹🔹🔹🔹
// 🚫 <b>КОНТАКТЫ СКРЫТЫ</b>
// -------------------------------
// 💎 <b>Для получения контактов и полного доступа ко всем анкетам необходимо:</b>

// ✅ <b>1. Активная подписка</b>
// ✅ <b>2. Подписка на канал @MagicYourClub</b>

// ✨ После получения доступа вы увидите:
// • Все контакты профилей (Telegram, WhatsApp, телефон)
// • Полные описания анкет
// • Неограниченный доступ ко всем анкетам
// • Максимальную скорость работы

// -------------------------------
// <a href="http://t.me/magicboss_bot/magic"><b>✨Magic WebApp</b></a>
// `.trim();

//                     let keyboard = [];
//                     if (isLast) {
//                         const filterKey = `country:${ctx.session.filterCountry || 'all'}:age:${ctx.session.ageRange?.label || 'all'}:city:${ctx.session.filterCity || 'all'}`;
//                         const filteredProfiles = isDemo ? 
//                             cacheManager.getCachedDemoFilteredProfiles(filterKey) : 
//                             cacheManager.getCachedFilteredProfiles(filterKey);
//                         const totalPages = Math.ceil((filteredProfiles?.length || 0) / SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE);
                        
//                         const currentFilters = {
//                             country: ctx.session?.displayCountry,
//                             city: ctx.session?.filterCity,
//                             ageRange: ctx.session?.ageRange
//                         };
                        
//                         keyboard = createEnhancedPaginationKeyboard(page, totalPages, filterKey, currentFilters, isDemo);

//                         keyboard.push(
//                             [{ text: "🎂 Фильтр по возрасту", callback_data: "filter_by_age" }],
//                             [{ text: "🌍 Все страны", callback_data: "all_countries_with_check" }],
//                             [{ text: "🧹 Очистить экран", callback_data: "clear_screen" }]
//                         );
//                     }

//                     let photosToSend = [];
//                     const seenUrls = new Set();

//                     if (fullProfile.photoUrl && typeof fullProfile.photoUrl === 'string' && fullProfile.photoUrl.trim() !== '') {
//                         try {
//                             const urlObj = new URL(fullProfile.photoUrl.trim());
//                             const cleanUrl = urlObj.href;
//                             if ((urlObj.protocol === 'http:' || urlObj.protocol === 'https:') && !seenUrls.has(cleanUrl)) {
//                                 seenUrls.add(cleanUrl);
//                                 photosToSend.push(cleanUrl);
//                             }
//                         } catch (e) {
//                             console.log(`❌ Ошибка обработки основного фото:`, e.message);
//                         }
//                     }

//                     if (Array.isArray(fullProfile.photos) && fullProfile.photos.length > 0) {
//                         fullProfile.photos.forEach((url, index) => {
//                             if (typeof url === 'string' && url.trim() !== '') {
//                                 try {
//                                     const urlObj = new URL(url.trim());
//                                     const cleanUrl = urlObj.href;
//                                     if ((urlObj.protocol === 'http:' || urlObj.protocol === 'https:') && !seenUrls.has(cleanUrl)) {
//                                         seenUrls.add(cleanUrl);
//                                         photosToSend.push(cleanUrl);
//                                     }
//                                 } catch (e) {
//                                     console.log(`❌ Ошибка обработки фото ${index + 1}:`, e.message);
//                                 }
//                             }
//                         });
//                     }

//                     photosToSend = photosToSend.slice(0, 10);
                    
//                     console.log(`📸 [DEMO PHOTO] Уникальные фото для ${fullProfile.name}: ${photosToSend.length} (из ${fullProfile.photos?.length || 0} в галерее)`);

//                     const sendPhotoSafely = async (photoUrl, photoNumber, totalPhotos) => {
//                         try {
//                             const emojiNumbers = ['1️⃣', '2️⃣', '3️⃣', '4️⃣', '5️⃣', '6️⃣', '7️⃣', '8️⃣', '9️⃣', '🔟'];
//                             const numberEmoji = photoNumber <= 10 ? emojiNumbers[photoNumber - 1] : `${photoNumber}.`;
//                             const photoCaption = `${numberEmoji} Фото ${photoNumber}/${totalPhotos}`;
                            
//                             return await ctx.replyWithPhoto(photoUrl, { 
//                                 caption: photoCaption, 
//                                 parse_mode: "HTML" 
//                             });
//                         } catch (error) {
//                             console.log(`❌ Ошибка отправки фото ${photoNumber}:`, error.message);
                            
//                             try {
//                                 return await ctx.replyWithPhoto(photoUrl);
//                             } catch (e) {
//                                 console.log(`❌ Не удалось отправить фото ${photoNumber} даже без caption:`, e.message);
//                                 return null;
//                             }
//                         }
//                     };

//                     let infoMessage = null;
                    
//                     if (photosToSend.length > 0) {
//                         const profileInfo = `✨✨✨✨✨✨✨✨✨✨ \n <a href="http://t.me/MagicYourClub"><b>Новые анкеты в нашем ➡️ канале</b></a>\n\n`;
//                         infoMessage = await ctx.reply(profileInfo, { parse_mode: "HTML" });
//                         messageManager.track(ctx.chat.id, infoMessage.message_id);
//                         await new Promise(resolve => setTimeout(resolve, 500));
//                     }

//                     const sentPhotoMessages = [];
                    
//                     if (photosToSend.length === 0) {
//                         console.log(`📭 [DEMO PHOTO] Нет валидных фото для ${fullProfile.name}, отправляем только текст`);
//                         const msg = await ctx.reply(demoCaption, {
//                             parse_mode: "HTML",
//                             reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
//                         });
//                         messageManager.track(ctx.chat.id, msg.message_id);
//                         return msg;
//                     }
//                     else {
//                         for (let i = 0; i < photosToSend.length; i++) {
//                             const photoUrl = photosToSend[i];
//                             const photoNumber = i + 1;
//                             const totalPhotos = photosToSend.length;
                            
//                             console.log(`🔄 [PHOTO] Отправляем фото ${photoNumber}/${totalPhotos} для ${fullProfile.name}`);
                            
//                             const photoMsg = await sendPhotoSafely(photoUrl, photoNumber, totalPhotos);
//                             if (photoMsg) {
//                                 sentPhotoMessages.push(photoMsg);
//                                 messageManager.track(ctx.chat.id, photoMsg.message_id);
                                
//                                 if (i < photosToSend.length - 1) {
//                                     await new Promise(resolve => setTimeout(resolve, 800));
//                                 }
//                             }
//                         }
                        
//                         if (sentPhotoMessages.length === 0) {
//                             console.log(`⚠️ [DEMO PHOTO] Все фото не удалось отправить для ${fullProfile.name}`);
//                             const fallbackMsg = await ctx.reply(`📷 [Все фото недоступны]\n\n${demoCaption}`, { 
//                                 parse_mode: "HTML",
//                                 reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
//                             });
//                             messageManager.track(ctx.chat.id, fallbackMsg.message_id);
//                             return fallbackMsg;
//                         }
//                     }

//                     await new Promise(resolve => setTimeout(resolve, 500));

//                     const textMsg = await ctx.reply(demoCaption, {
//                         parse_mode: "HTML",
//                         reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
//                     });

//                     messageManager.track(ctx.chat.id, textMsg.message_id);
//                     console.log(`✅ [DEMO PROFILE] Анкета ${fullProfile.name} отправлена: ${sentPhotoMessages.length} фото + текст`);

//                     return textMsg;

//                 } else {
//                     const formatTelegram = (username) => {
//                         if (!username) return "";
//                         const cleanUsername = cleanText(username);
//                         if (/^[0-9+\-() ]+$/.test(cleanUsername)) {
//                             const cleanDigits = cleanUsername.replace(/[^0-9]/g, "");
//                             if (cleanDigits.startsWith('7') || cleanDigits.startsWith('8') || (cleanDigits.length >= 10 && !cleanDigits.startsWith('1'))) {
//                                 let telegramNumber = cleanDigits;
//                                 if (telegramNumber.startsWith('7') && telegramNumber.length === 11) telegramNumber = telegramNumber.substring(1);
//                                 else if (telegramNumber.startsWith('8') && telegramNumber.length === 11) telegramNumber = telegramNumber.substring(1);
//                                 return `🔵 <a href="https://t.me/${telegramNumber}">Telegram</a>`;
//                             }
//                         }
//                         if (cleanUsername.startsWith("https://t.me/")) {
//                             const cleaned = decodeURIComponent(cleanUsername).replace("https://t.me/", "").replace(/^%40/, "@").replace(/^\+/, "");
//                             return `🔵 <a href="https://t.me/${cleaned}">Telegram</a>`;
//                         }
//                         const cleaned = cleanUsername.replace(/^[@+]/, "");
//                         return `🔵 <a href="https://t.me/${cleaned}">Telegram</a>`;
//                     };

//                     const formatWhatsApp = (url) => {
//                         if (!url) return "";
//                         const cleanUrl = cleanText(url);
//                         if (/^[0-9+\-() ]+$/.test(cleanUrl)) {
//                             let cleanDigits = cleanUrl.replace(/[^0-9]/g, "");
//                             if (cleanDigits.startsWith('8') && cleanDigits.length === 11) cleanDigits = '7' + cleanDigits.substring(1);
//                             else if (cleanDigits.length === 10) cleanDigits = '7' + cleanDigits;
//                             if (cleanDigits.length === 11 && cleanDigits.startsWith('7')) return `🟢 <a href="https://wa.me/${cleanDigits}">WhatsApp</a>`;
//                         }
//                         return `🟢 <a href="${cleanUrl}">WhatsApp</a>`;
//                     };

//                     const formatPhone = (phone) => {
//                         if (!phone) return "";
//                         let cleanDigits = cleanText(phone).replace(/[^0-9]/g, "");
//                         if (!cleanDigits) return "";
//                         let formattedPhone = cleanText(phone);
//                         if (cleanDigits.length === 11 || cleanDigits.length === 10) {
//                             if (cleanDigits.startsWith('7') && cleanDigits.length === 11) formattedPhone = `+${cleanDigits}`;
//                             else if (cleanDigits.startsWith('8') && cleanDigits.length === 11) formattedPhone = `+7${cleanDigits.substring(1)}`;
//                             else if (cleanDigits.length === 10) formattedPhone = `+7${cleanDigits}`;
//                         }
//                         return `📞 ${formattedPhone}`;
//                     };

//                     const fullCaption = `
// 👤 <b>${cleanText(fullProfile.name)}</b>, ${fullProfile.age}
// -------------------------------
// ${cleanText(fullProfile.country)},📍${cleanText(fullProfile.city)}
// -------------------------------
// <em>${about.length > 300 ? about.substring(0, 300) + `...<a href="http://t.me/magicboss_bot/magic">читать полностью в ✨Magic</a>` : about}</em>
// 🔹🔹🔹🔹🔹🔹🔹🔹🔹🔹
// <b>Контакты:</b>
// -------------------------------
// ${fullProfile.phone ? formatPhone(fullProfile.phone) : ""}${fullProfile.telegram ? "\n-------------------------------\n" + formatTelegram(fullProfile.telegram) : ""}${fullProfile.whatsapp ? "\n-------------------------------\n" + formatWhatsApp(fullProfile.whatsapp) : ""}${(fullProfile.phone || fullProfile.telegram || fullProfile.whatsapp) ? "\n-------------------------------" : ""}
// ⚠️ <b>ЕСЛИ КТО-ТО ПРОСИТ: Криптовалюту наперед, деньги на такси🚕 или дорогу, предоплату любым способом, переводы на карты💳 или электронные кошельки, чеки или подтверждения оплаты</b>
// 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 
// <b>ЭТО 100% МОШЕННИКИ!
// НИ В КОЕМ СЛУЧАЕ НЕ ОТПРАВЛЯЙТЕ ПРЕДОПЛАТУ  🛑 ВАС ОБМАНУТ!</b>
// -------------------------------
// <a href="http://t.me/magicboss_bot/magic"><b>✨Magic WebApp</b></a>
// `.trim();

//                     let keyboard = [];
//                     if (isLast) {
//                         const filterKey = `country:${ctx.session.filterCountry || 'all'}:age:${ctx.session.ageRange?.label || 'all'}:city:${ctx.session.filterCity || 'all'}`;
//                         const filteredProfiles = cacheManager.getCachedFilteredProfiles(filterKey);
//                         const totalPages = Math.ceil((filteredProfiles?.length || 0) / SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE);
                        
//                         const currentFilters = {
//                             country: ctx.session?.displayCountry,
//                             city: ctx.session?.filterCity,
//                             ageRange: ctx.session?.ageRange
//                         };
                        
//                         keyboard = createEnhancedPaginationKeyboard(page, totalPages, filterKey, currentFilters);

//                         keyboard.push(
//                             [{ text: "🎂 Фильтр по возрасту", callback_data: "filter_by_age" }],
//                             [{ text: "🌍 Все страны", callback_data: "all_countries_with_check" }],
//                             [{ text: "🧹 Очистить экран", callback_data: "clear_screen" }]
//                         );
//                     }

//                     let photosToSend = [];
//                     const seenUrls = new Set();

//                     if (fullProfile.photoUrl && typeof fullProfile.photoUrl === 'string' && fullProfile.photoUrl.trim() !== '') {
//                         try {
//                             const urlObj = new URL(fullProfile.photoUrl.trim());
//                             const cleanUrl = urlObj.href;
//                             if ((urlObj.protocol === 'http:' || urlObj.protocol === 'https:') && !seenUrls.has(cleanUrl)) {
//                                 seenUrls.add(cleanUrl);
//                                 photosToSend.push(cleanUrl);
//                             }
//                         } catch (e) {
//                             console.log(`❌ Ошибка обработки основного фото:`, e.message);
//                         }
//                     }

//                     if (Array.isArray(fullProfile.photos) && fullProfile.photos.length > 0) {
//                         fullProfile.photos.forEach((url, index) => {
//                             if (typeof url === 'string' && url.trim() !== '') {
//                                 try {
//                                     const urlObj = new URL(url.trim());
//                                     const cleanUrl = urlObj.href;
//                                     if ((urlObj.protocol === 'http:' || urlObj.protocol === 'https:') && !seenUrls.has(cleanUrl)) {
//                                         seenUrls.add(cleanUrl);
//                                         photosToSend.push(cleanUrl);
//                                     }
//                                 } catch (e) {
//                                     console.log(`❌ Ошибка обработки фото ${index + 1}:`, e.message);
//                                 }
//                             }
//                         });
//                     }

//                     photosToSend = photosToSend.slice(0, 10);
                    
//                     console.log(`📸 [PHOTO] Уникальные фото для ${fullProfile.name}: ${photosToSend.length} (из ${fullProfile.photos?.length || 0} в галерее)`);

//                     const sendPhotoSafely = async (photoUrl, photoNumber, totalPhotos) => {
//                         try {
//                             const emojiNumbers = ['1️⃣', '2️⃣', '3️⃣', '4️⃣', '5️⃣', '6️⃣', '7️⃣', '8️⃣', '9️⃣', '🔟'];
//                             const numberEmoji = photoNumber <= 10 ? emojiNumbers[photoNumber - 1] : `${photoNumber}.`;
//                             const photoCaption = `${numberEmoji} Фото ${photoNumber}/${totalPhotos}`;
                            
//                             return await ctx.replyWithPhoto(photoUrl, { 
//                                 caption: photoCaption, 
//                                 parse_mode: "HTML" 
//                             });
//                         } catch (error) {
//                             console.log(`❌ Ошибка отправки фото ${photoNumber}:`, error.message);
                            
//                             try {
//                                 return await ctx.replyWithPhoto(photoUrl);
//                             } catch (e) {
//                                 console.log(`❌ Не удалось отправить фото ${photoNumber} даже без caption:`, e.message);
//                                 return null;
//                             }
//                         }
//                     };

//                     let infoMessage = null;
                    
//                     if (photosToSend.length > 0) {
//                         const profileInfo = `✨✨✨✨✨✨✨✨✨✨ \n <a href="http://t.me/MagicYourClub"><b>Новые анкеты в нашем ➡️ канале</b></a>\n\n`;
//                         infoMessage = await ctx.reply(profileInfo, { parse_mode: "HTML" });
//                         messageManager.track(ctx.chat.id, infoMessage.message_id);
//                         await new Promise(resolve => setTimeout(resolve, 500));
//                     }

//                     const sentPhotoMessages = [];
                    
//                     if (photosToSend.length === 0) {
//                         console.log(`📭 [PHOTO] Нет валидных фото для ${fullProfile.name}, отправляем только текст`);
//                         const msg = await ctx.reply(fullCaption, {
//                             parse_mode: "HTML",
//                             reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
//                         });
//                         messageManager.track(ctx.chat.id, msg.message_id);
//                         return msg;
//                     }
//                     else {
//                         for (let i = 0; i < photosToSend.length; i++) {
//                             const photoUrl = photosToSend[i];
//                             const photoNumber = i + 1;
//                             const totalPhotos = photosToSend.length;
                            
//                             console.log(`🔄 [PHOTO] Отправляем фото ${photoNumber}/${totalPhotos} для ${fullProfile.name}`);
                            
//                             const photoMsg = await sendPhotoSafely(photoUrl, photoNumber, totalPhotos);
//                             if (photoMsg) {
//                                 sentPhotoMessages.push(photoMsg);
//                                 messageManager.track(ctx.chat.id, photoMsg.message_id);
                                
//                                 if (i < photosToSend.length - 1) {
//                                     await new Promise(resolve => setTimeout(resolve, 800));
//                                 }
//                             }
//                         }
                        
//                         if (sentPhotoMessages.length === 0) {
//                             console.log(`⚠️ [PHOTO] Все фото не удалось отправить для ${fullProfile.name}`);
//                             const fallbackMsg = await ctx.reply(`📷 [Все фото недоступны]\n\n${fullCaption}`, { 
//                                 parse_mode: "HTML",
//                                 reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
//                             });
//                             messageManager.track(ctx.chat.id, fallbackMsg.message_id);
//                             return fallbackMsg;
//                         }
//                     }

//                     await new Promise(resolve => setTimeout(resolve, 500));

//                     const textMsg = await ctx.reply(fullCaption, {
//                         parse_mode: "HTML",
//                         reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
//                     });

//                     messageManager.track(ctx.chat.id, textMsg.message_id);
//                     console.log(`✅ [PROFILE] Анкета ${fullProfile.name} отправлена: ${sentPhotoMessages.length} фото + текст`);

//                     return textMsg;
//                 }

//             } catch (error) {
//                 console.error("❌ Критическая ошибка отправки анкеты:", error);
//                 try {
//                     const fallbackText = `👤 ${fullProfile.name}, ${fullProfile.age}\n📍 ${fullProfile.city}, ${fullProfile.country}\n\n${fullProfile.about || 'Описание недоступно'}\n\n⚠️ Приносим извинения, возникли технические проблемы с отображением фото.`;
//                     const msg = await ctx.reply(fallbackText, { parse_mode: "HTML" });
//                     messageManager.track(ctx.chat.id, msg.message_id);
//                     return msg;
//                 } catch (finalError) {
//                     console.error("💥 Не удалось отправить даже текстовое сообщение:", finalError);
//                     return null;
//                 }
//             }
//         });
//     };

//     bot.command("stats", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 const stats = readingStats.getStats();
//                 const cacheStats = cacheManager.getCacheStats();
                
//                 const statsMessage = `
// 📊 **Статистика системы**

// **Операции чтения:**
// • Всего операций: ${stats.totalReads}
// • Firestore чтений: ${stats.firestoreReads}
// • Кэш попадания: ${stats.operations.cacheHits}
// • Кэш промахи: ${stats.operations.cacheMisses}
// • Эффективность кэша: ${stats.cacheEfficiency}

// **Детализация операций:**
// • Профили: ${stats.operations.profiles}
// • Подписки: ${stats.operations.subscriptions}
// • Проверки канала: ${stats.operations.channelSubscriptions}
// • Страны: ${stats.operations.countries}
// • Города: ${stats.operations.cities}

// **Пользователи:**
// • Уникальные: ${stats.uniqueUsers}
// • Операций на пользователя: ${stats.readsPerUser.toFixed(2)}

// **Кэш:**
// • Полный кэш загружен: ${cacheStats.fullCacheLoaded ? '✅' : '❌'}
// • Демо кэш загружен: ${cacheStats.demoCacheLoaded ? '✅' : '❌'}
// • Полных профилей: ${cacheStats.profilesCount}
// • Демо профилей: ${cacheStats.demoProfilesCount}
// • Ключей фильтров: ${cacheStats.filterKeysCount}/${cacheStats.filterCacheLimit}
// • Подписок в кэше: ${cacheStats.subscriptionCacheCount}
// • Проверок канала в кэше: ${cacheStats.channelCacheCount}
// • Статусов пользователей: ${cacheStats.userCacheStatusCount}

// **Очередь:**
// • Активные задачи: ${messageQueue.pending}
// • Задачи в ожидании: ${messageQueue.size}
//                 `;
                
//                 const msg = await ctx.reply(statsMessage, { parse_mode: "Markdown" });
//                 messageManager.track(ctx.chat.id, msg.message_id);
                
//             } catch (error) {
//                 console.error("❌ Ошибка команды stats:", error);
//             }
//         });
//     });

//     bot.command("reset_stats", async (ctx) => {
//         await messageQueue.add(async () => {
//             try {
//                 readingStats.resetStats();
//                 const msg = await ctx.reply("✅ Статистика сброшена");
//                 messageManager.track(ctx.chat.id, msg.message_id);
//             } catch (error) {
//                 console.error("❌ Ошибка сброса статистики:", error);
//             }
//         });
//     });

//     // Экспорт функций
//     module.exports.ensureProperCache = ensureProperCache;
//     module.exports.checkFullAccess = checkFullAccess;
//     module.exports.checkSubscription = checkSubscription;
//     module.exports.checkChannelSubscription = checkChannelSubscription;

//     // Инициализация демо-кэша при старте бота
//     setTimeout(async () => {
//         if (!globalCacheInitialized) {
//             console.log('🚀 [BOT START] Фоновая инициализация демо-кэша...');
//             try {
//                 if (demoCacheLoading) {
//                     console.log('⏳ [BOT START] Демо-кэш уже загружается...');
//                     return;
//                 }
                
//                 demoCacheLoading = true;
//                 demoCacheLoadPromise = loadProfileCache(db, false)
//                     .then(() => {
//                         globalCacheInitialized = true;
//                         console.log('✅ [BOT START] Демо-кэш загружен в фоне');
//                     })
//                     .catch(error => {
//                         console.error('❌ [BOT START] Ошибка загрузки демо-кэша:', error);
//                     })
//                     .finally(() => {
//                         demoCacheLoading = false;
//                         demoCacheLoadPromise = null;
//                     });
                
//                 await demoCacheLoadPromise;
                
//             } catch (error) {
//                 console.error('❌ [BOT START] Ошибка загрузки демо-кэша:', error);
//                 demoCacheLoading = false;
//                 demoCacheLoadPromise = null;
//             }
            
//             // Периодическое обновление кэша (раз в 7 дней)
//             setInterval(async () => {
//                 const randomDelay = Math.random() * 30 * 60 * 1000;
//                 await new Promise(resolve => setTimeout(resolve, randomDelay));
                
//                 console.log('🔄 [CACHE UPDATE] Периодическое обновление демо-кэша...');
//                 try {
//                     await loadProfileCache(db, false);
                    
//                     // Если полный кэш тоже загружен, обновляем и его
//                     if (cacheManager.isFullCacheLoaded()) {
//                         console.log('🔄 [CACHE UPDATE] Обновление полного кэша...');
//                         await loadProfileCache(db, true);
//                     }
//                 } catch (error) {
//                     console.error('❌ [CACHE UPDATE] Ошибка обновления кэша:', error);
//                 }
//             }, 7 * 24 * 60 * 60 * 1000);
//         }
//     }, 3000); 

//     console.log(`✅ Модуль профилей инициализирован с ОПТИМИЗИРОВАННОЙ загрузкой кэша`);
//     console.log(`📊 Кэш подписок: 24 часа`);
//     console.log(`📊 Кэш проверки канала: 24 часа`);
//     console.log(`📊 Сессионное кэширование: 5-30 минут`);
//     console.log(`📊 Одна загрузка полного кэша при первом оплатившем пользователе`);
// };

const RateLimiter = require("telegraf-ratelimit");
const { default: PQueue } = require("p-queue");
const NodeCache = require("node-cache");
const fs = require('fs');
const path = require('path');
const zlib = require('zlib');

// ===== УДАЛЕНИЕ LOCK ФАЙЛА ПРИ ЗАПУСКЕ =====
const LOCK_FILE = path.join(__dirname, 'bot.lock');
try {
    if (fs.existsSync(LOCK_FILE)) {
        console.log('🗑️ УДАЛЯЕМ LOCK ФАЙЛ ДЛЯ RENDER');
        fs.unlinkSync(LOCK_FILE);
    }
} catch (error) {
    console.log('⚠️ Не удалось удалить lock файл:', error.message);
}

// ===================== БЛОКИРОВКА ОТ МНОЖЕСТВЕННОГО ЗАПУСКА =====================
if (fs.existsSync(LOCK_FILE)) {
    const existingPid = fs.readFileSync(LOCK_FILE, 'utf8');
    console.error(`❌ Бот уже запущен с PID: ${existingPid}`);
    console.error('❌ Остановите предыдущий процесс или удалите файл bot.lock');
    process.exit(1);
}
fs.writeFileSync(LOCK_FILE, process.pid.toString());
process.on('exit', () => { 
    if (fs.existsSync(LOCK_FILE)) fs.unlinkSync(LOCK_FILE); 
});
process.on('SIGINT', () => { 
    if (fs.existsSync(LOCK_FILE)) fs.unlinkSync(LOCK_FILE); 
    process.exit(0); 
});

// ===================== ГЛОБАЛЬНЫЕ ФЛАГИ ДЛЯ ПРЕДОТВРАЩЕНИЯ ПОВТОРНОЙ ЗАГРУЗКИ =====================
let globalCacheInitialized = false;
let fullCacheLoading = false;
let fullCacheLoadPromise = null;
let demoCacheLoading = false;
let demoCacheLoadPromise = null;

// ===================== ФУНКЦИЯ ПРЕЛОАДЕРА =====================
// ===================== КРАСИВЫЙ ПРЕЛОАДЕР =====================
const sendPreloader = async (ctx, type = "default", title = "✨Magic") => {
    try {
        const preloaderConfigs = {
            'city': {
                title: "🔍 ПОИСК АНКЕТ",
                text: `🔍 <b>Поиск анкет в выбранном городе...</b>\n\n⏳ <i>База данных обновляется, это может занять до 2 минут</i>\n\n📊 <i>Ищем самые свежие анкеты для вас...</i>\n\n✨ <i>Пожалуйста, подождите</i>`
            },
            'country': {
                title: "🌍 ЗАГРУЗКА СТРАН", 
                text: `🌍 <b>Загружаем список стран и городов...</b>\n\n⏳ <i>Обновляем географию анкет</i>\n\n🌎 <i>Ищем анкеты со всего мира</i>\n\n✨ <i>Пожалуйста, подождите</i>`
            },
            'access': {
                title: "🔍 ПРОВЕРКА ДОСТУПА",
                text: `🔍 <b>Проверяем ваш доступ...</b>\n\n⏳ <i>Проверяем подписку и статус канала</i>\n\n✅ <i>Определяем режим работы</i>\n\n✨ <i>Пожалуйста, подождите</i>`
            },
            'default': {
                title: "✨Magic",
                text: `🔄 <b>${title}</b>\n\n⏳ <i>Пожалуйста, подождите...</i>\n\n✨ <i>Система готовится к работе</i>`
            }
        };

        const config = preloaderConfigs[type] || preloaderConfigs.default;
        
        const preloaderMsg = await ctx.reply(config.text, {
            parse_mode: "HTML",
            reply_markup: {
                inline_keyboard: [
                    [{ text: "🔄 Загружается...", callback_data: "loading" }]
                ]
            }
        });
        
        return preloaderMsg;
    } catch (error) {
        console.log("❌ Не удалось отправить прелоадер:", error.message);
        return null;
    }
};

// Функция для удаления прелоадера
const removePreloader = async (ctx, preloaderMsg) => {
    if (preloaderMsg) {
        try {
            await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
        } catch (error) {
            console.log("❌ Не удалось удалить прелоадер:", error.message);
        }
    }
};

// ===================== СИСТЕМА БЛОКИРОВКИ ПОЛЬЗОВАТЕЛЯ =====================
const userLocks = new Map();

const acquireUserLock = (userId, timeoutMs = 10000) => {
    const now = Date.now();
    const userLock = userLocks.get(userId);
    
    if (userLock && now < userLock.expires) {
        return false;
    }
    
    userLocks.set(userId, {
        expires: now + timeoutMs,
        timestamp: now
    });
    return true;
};

const releaseUserLock = (userId) => {
    userLocks.delete(userId);
};

setInterval(() => {
    const now = Date.now();
    let cleanedCount = 0;
    
    userLocks.forEach((lock, userId) => {
        if (now >= lock.expires) {
            userLocks.delete(userId);
            cleanedCount++;
        }
    });
    
    if (cleanedCount > 0) {
        console.log(`🧹 [LOCKS] Очищено ${cleanedCount} старых блокировок`);
    }
}, 60000);

// ===================== КОНФИГУРАЦИЯ МАСШТАБИРОВАНИЯ =====================
const SCALING_CONFIG = {
    MESSAGE_QUEUE: {
        CONCURRENCY: 50,
        INTERVAL: 1000,
        INTERVAL_CAP: 200,
        TIMEOUT: 30000,
    },
    
    CACHE: {
        PROFILES_TTL: 7 * 24 * 60 * 60, // 7 дней
        FILTERS_TTL: 600,
        SESSIONS_TTL: 7 * 24 * 60 * 60, // 7 дней
        SUBSCRIPTION_TTL: 24 * 60 * 60, // 24 часа для проверки подписки
        CHANNEL_TTL: 5 * 60, // 5 минут для канала
        MAX_FILTER_KEYS: 500,
        CHECKPERIOD: 300,
    },
    
    PERFORMANCE: {
        PROFILES_PER_PAGE: 1,
        MAX_CAPTION_LENGTH: 900,
        MESSAGE_TTL: 86400000,
        FILTER_CHUNK_SIZE: 500,
        MAX_CONCURRENT_FILTERS: 10,
    }
};

// ===================== ВСПОМОГАТЕЛЬНЫЕ КОНСТАНТЫ =====================
const AGE_RANGES = [
    { label: "18-25", min: 18, max: 25 },
    { label: "26-35", min: 26, max: 35 },
    { label: "36-45", min: 36, max: 45 },
    { label: "46+", min: 46, max: 999 },
];

const POPULAR_COUNTRIES = [
    { name: "Россия", flag: "🇷🇺" },
    { name: "Украина", flag: "🇺🇦" },
    { name: "Беларусь", flag: "🇧🇾" },
    { name: "Казахстан", flag: "🇰🇿" },
    { name: "Турция", flag: "🇹🇷" },
    { name: "Германия", flag: "🇩🇪" },
    { name: "США", flag: "🇺🇸" },
    { name: "Израиль", flag: "🇮🇱" },
];

const PAGINATION_JUMP_SECTIONS = [
    { label: "1-1000", start: 0, end: 999 },
    { label: "1000-2000", start: 1000, end: 1999 },
    { label: "2000-3000", start: 2000, end: 2999 },
];

// Карта нормализации украинских городов
const cityNormalizationMap = {
  kyiv: "Киев", kiev: "Киев", kiyv: "Киев",
  "kryvyi rih": "Кривой Рог", "kryvyi rig": "Кривой Рог",
  odesa: "Одесса", odessa: "Одесса",
  kharkiv: "Харьков", lviv: "Львов", dnipro: "Днепр",
  zaporizhzhia: "Запорожье", zaporozhye: "Запорожье",
  vinnytsia: "Винница", vinnitsa: "Винница",
  ternopil: "Тернополь",
  khmelnytskyi: "Хмельницкий", khmelnitsky: "Хмельницкий",
  cherkasy: "Черкассы", chernivtsi: "Черновцы", chernovtsy: "Черновцы",
  "ivano-frankivsk": "Ивано-Франковск",
  kropyvnytskyi: "Кропивницкий",
  mykolaiv: "Николаев", nikolaev: "Николаев",
  poltava: "Полтава", rivne: "Ровно", rovno: "Ровно",
  sumy: "Сумы", uzhhorod: "Ужгород", zhytomyr: "Житомир",
  kramatorsk: "Краматорск", sloviansk: "Славянск",
  lutsk: "Луцк", kherson: "Херсон", bukovel: "Буковель",

  київ: "Киев", "кривий ріг": "Кривой Рог", одеса: "Одесса",
  харків: "Харьков", львів: "Львов", дніпро: "Днепр",
  дніпропетровськ: "Днепр", запоріжжя: "Запорожье",
  вінниця: "Винница", тернопіль: "Тернополь",
  хмельницький: "Хмельницкий", черкаси: "Черкассы",
  чернівці: "Черновцы", "івано-франківськ": "Ивано-Франковск",
  кропивницький: "Кропивницкий", миколаїв: "Николаев",
  полтава: "Полтава", рівне: "Ровно", суми: "Сумы",
  ужгород: "Ужгород", житомир: "Житомир",
  краматорськ: "Краматорск", "слов'янськ": "Славянск",
  луцьк: "Луцк", херсон: "Херсон", буковель: "Буковель",
};

const countryNormalizationMap = {
    // Существующие страны Европы
    'russia': '🇷🇺 Россия', 'russian federation': '🇷🇺 Россия', 'rus': '🇷🇺 Россия',
    'ukraine': '🇺🇦 Украина', 'ukr': '🇺🇦 Украина',
    'belarus': '🇧🇾 Беларусь', 'belarus republic': '🇧🇾 Беларусь', 'blr': '🇧🇾 Беларусь',
    'kazakhstan': '🇰🇿 Казахстан', 'kaz': '🇰🇿 Казахстан',
    'germany': '🇩🇪 Германия', 'deutschland': '🇩🇪 Германия', 'deu': '🇩🇪 Германия', 'ger': '🇩🇪 Германия',
    'france': '🇫🇷 Франция', 'french republic': '🇫🇷 Франция', 'fra': '🇫🇷 Франция',
    'italy': '🇮🇹 Италия', 'italian republic': '🇮🇹 Италия', 'ita': '🇮🇹 Италия',
    'spain': '🇪🇸 Испания', 'kingdom of spain': '🇪🇸 Испания', 'esp': '🇪🇸 Испания',
    'united kingdom': '🇬🇧 Великобритания', 'great britain': '🇬🇧 Великобритания', 'england': '🇬🇧 Великобритания', 'gb': '🇬🇧 Великобритания', 'gbr': '🇬🇧 Великобритания',
    'poland': '🇵🇱 Польша', 'republic of poland': '🇵🇱 Польша', 'pol': '🇵🇱 Польша',
    'netherlands': '🇳🇱 Нидерланды', 'holland': '🇳🇱 Нидерланды', 'nld': '🇳🇱 Нидерланды',
    'belgium': '🇧🇪 Бельгия', 'belgian': '🇧🇪 Бельгия', 'bel': '🇧🇪 Бельгия',
    'switzerland': '🇨🇭 Швейцария', 'swiss': '🇨🇭 Швейцария', 'che': '🇨🇭 Швейцария',
    'austria': '🇦🇹 Австрия', 'aut': '🇦🇹 Австрия',
    'portugal': '🇵🇹 Португалия', 'prt': '🇵🇹 Португалия',
    'greece': '🇬🇷 Греция', 'grc': '🇬🇷 Греция',
    'czech republic': '🇨🇿 Чехия', 'czech': '🇨🇿 Чехия', 'cze': '🇨🇿 Чехия',
    'sweden': '🇸🇪 Швеция', 'swe': '🇸🇪 Швеция',
    'norway': '🇳🇴 Норвегия', 'nor': '🇳🇴 Норвегия',
    'finland': '🇫🇮 Финляндия', 'fin': '🇫🇮 Финляндия',
    'denmark': '🇩🇰 Дания', 'dnk': '🇩🇰 Дания',
    'ireland': '🇮🇪 Ирландия', 'irl': '🇮🇪 Ирландия',
    'hungary': '🇭🇺 Венгрия', 'hun': '🇭🇺 Венгрия',
    'romania': '🇷🇴 Румыния', 'rou': '🇷🇴 Румыния',
    'bulgaria': '🇧🇬 Болгария', 'bgr': '🇧🇬 Болгария',
    'serbia': '🇷🇸 Сербия', 'srb': '🇷🇸 Сербия',
    'croatia': '🇭🇷 Хорватия', 'hrv': '🇭🇷 Хорватия',
    'slovakia': '🇸🇰 Словакия', 'svk': '🇸🇰 Словакия',
    'slovenia': '🇸🇮 Словения', 'svn': '🇸🇮 Словения',
    'lithuania': '🇱🇹 Литва', 'ltu': '🇱🇹 Литва',
    'latvia': '🇱🇻 Латвия', 'lva': '🇱🇻 Латвия',
    'estonia': '🇪🇪 Эстония', 'est': '🇪🇪 Эстония',
    'moldova': '🇲🇩 Молдова', 'mda': '🇲🇩 Молдова',
    'georgia': '🇬🇪 Грузия', 'geo': '🇬🇪 Грузия',
    'armenia': '🇦🇲 Армения', 'arm': '🇦🇲 Армения',
    'azerbaijan': '🇦🇿 Азербайджан', 'aze': '🇦🇿 Азербайджан',
    'luxembourg': '🇱🇺 Люксембург', 'lux': '🇱🇺 Люксембург',
    'malta': '🇲🇹 Мальта', 'mlt': '🇲🇹 Мальта',
    'monaco': '🇲🇨 Монако', 'mco': '🇲🇨 Монако',
    'montenegro': '🇲🇪 Черногория', 'mne': '🇲🇪 Черногория',
    'iceland': '🇮🇸 Исландия', 'isl': '🇮🇸 Исландия',
    'albania': '🇦🇱 Албания', 'alb': '🇦🇱 Албания',
    'bosnia': '🇧🇦 Босния', 'bosnia and herzegovina': '🇧🇦 Босния', 'bih': '🇧🇦 Босния',
    'cyprus': '🇨🇾 Кипр', 'cyp': '🇨🇾 Кипр',
    'north macedonia': '🇲🇰 Северная Македония', 'macedonia': '🇲🇰 Северная Македония', 'mkd': '🇲🇰 Северная Македония',

    // Африка
    'south africa': '🇿🇦 ЮАР', 'south africa republic': '🇿🇦 ЮАР', 'zaf': '🇿🇦 ЮАР',
    'egypt': '🇪🇬 Египет', 'egy': '🇪🇬 Египет',
    'morocco': '🇲🇦 Марокко', 'mar': '🇲🇦 Марокко',
    'algeria': '🇩🇿 Алжир', 'dza': '🇩🇿 Алжир',
    'tunisia': '🇹🇳 Тунис', 'tun': '🇹🇳 Тунис',
    'nigeria': '🇳🇬 Нигерия', 'nga': '🇳🇬 Нигерия',
    'kenya': '🇰🇪 Кения', 'ken': '🇰🇪 Кения',
    'ethiopia': '🇪🇹 Эфиопия', 'eth': '🇪🇹 Эфиопия',
    'ghana': '🇬🇭 Гана', 'gha': '🇬🇭 Гана',
    'tanzania': '🇹🇿 Танзания', 'tza': '🇹🇿 Танзания',
    'uganda': '🇺🇬 Уганда', 'uga': '🇺🇬 Уганда',
    'cameroon': '🇨🇲 Камерун', 'cmr': '🇨🇲 Камерун',
    'ivory coast': '🇨🇮 Кот-д\'Ивуар', 'cote d\'ivoire': '🇨🇮 Кот-д\'Ивуар', 'civ': '🇨🇮 Кот-д\'Ивуар',
    'senegal': '🇸🇳 Сенегал', 'sen': '🇸🇳 Сенегал',

    // Северная и Южная Америка
    'usa': '🇺🇸 США', 'united states': '🇺🇸 США', 'america': '🇺🇸 США', 'united states of america': '🇺🇸 США',
    'canada': '🇨🇦 Канада', 'can': '🇨🇦 Канада',
    'mexico': '🇲🇽 Мексика', 'mex': '🇲🇽 Мексика',
    'brazil': '🇧🇷 Бразилия', 'bra': '🇧🇷 Бразилия',
    'argentina': '🇦🇷 Аргентина', 'arg': '🇦🇷 Аргентина',
    'colombia': '🇨🇴 Колумбия', 'col': '🇨🇴 Колумбия',
    'chile': '🇨🇱 Чили', 'chl': '🇨🇱 Чили',
    'peru': '🇵🇪 Перу', 'per': '🇵🇪 Перу',
    'venezuela': '🇻🇪 Венесуэла', 'ven': '🇻🇪 Венесуэла',
    'ecuador': '🇪🇨 Эквадор', 'ecu': '🇪🇨 Эквадор',
    'bolivia': '🇧🇴 Боливия', 'bol': '🇧🇴 Боливия',
    'paraguay': '🇵🇾 Парагвай', 'pry': '🇵🇾 Парагвай',
    'uruguay': '🇺🇾 Уругвай', 'ury': '🇺🇾 Уругвай',
    'cuba': '🇨🇺 Куба', 'cub': '🇨🇺 Куба',
    'dominican republic': '🇩🇴 Доминикана', 'dom': '🇩🇴 Доминикана',
    'puerto rico': '🇵🇷 Пуэрто-Рико', 'pri': '🇵🇷 Пуэрто-Рико',
    'panama': '🇵🇦 Панама', 'pan': '🇵🇦 Панама',
    'costa rica': '🇨🇷 Коста-Рика', 'cri': '🇨🇷 Коста-Рика',

    // Азия
    'china': '🇨🇳 Китай', 'chn': '🇨🇳 Китай',
    'japan': '🇯🇵 Япония', 'jpn': '🇯🇵 Япония',
    'south korea': '🇰🇷 Южная Корея', 'korea': '🇰🇷 Южная Корея', 'kor': '🇰🇷 Южная Корея',
    'north korea': '🇰🇵 Северная Корея', 'prk': '🇰🇵 Северная Корея',
    'india': '🇮🇳 Индия', 'ind': '🇮🇳 Индия',
    'pakistan': '🇵🇰 Пакистан', 'pak': '🇵🇰 Пакистан',
    'bangladesh': '🇧🇩 Бангладеш', 'bgd': '🇧🇩 Бангладеш',
    'indonesia': '🇮🇩 Индонезия', 'idn': '🇮🇩 Индонезия',
    'philippines': '🇵🇭 Филиппины', 'phl': '🇵🇭 Филиппины',
    'vietnam': '🇻🇳 Вьетнам', 'vnm': '🇻🇳 Вьетнам',
    'thailand': '🇹🇭 Таиланд', 'tha': '🇹🇭 Таиланд',
    'malaysia': '🇲🇾 Малайзия', 'mys': '🇲🇾 Малайзия',
    'singapore': '🇸🇬 Сингапур', 'sgp': '🇸🇬 Сингапур',
    'myanmar': '🇲🇲 Мьянма', 'burma': '🇲🇲 Мьянма', 'mmr': '🇲🇲 Мьянма',
    'sri lanka': '🇱🇰 Шри-Ланка', 'lka': '🇱🇰 Шри-Ланка',
    'nepal': '🇳🇵 Непал', 'npl': '🇳🇵 Непал',
    'afghanistan': '🇦🇫 Афганистан', 'afg': '🇦🇫 Афганистан',
    'iran': '🇮🇷 Иран', 'irn': '🇮🇷 Иран',
    'iraq': '🇮🇶 Ирак', 'irq': '🇮🇶 Ирак',
    'saudi arabia': '🇸🇦 Саудовская Аравия', 'sau': '🇸🇦 Саудовская Аравия',
    'united arab emirates': '🇦🇪 ОАЭ', 'uae': '🇦🇪 ОАЭ', 'are': '🇦🇪 ОАЭ',
    'qatar': '🇶🇦 Катар', 'qat': '🇶🇦 Катар',
    'kuwait': '🇰🇼 Кувейт', 'kwt': '🇰🇼 Кувейт',
    'oman': '🇴🇲 Оман', 'omn': '🇴🇲 Оман',
    'bahrain': '🇧🇭 Бахрейн', 'bhr': '🇧🇭 Бахрейн',
    'jordan': '🇯🇴 Иордания', 'jor': '🇯🇴 Иордания',
    'lebanon': '🇱🇧 Ливан', 'lbn': '🇱🇧 Ливан',
    'syria': '🇸🇾 Сирия', 'syr': '🇸🇾 Сирия',
    'yemen': '🇾🇪 Йемен', 'yem': '🇾🇪 Йемен',
    'israel': '🇮🇱 Израиль', 'isr': '🇮🇱 Израиль',
    'palestine': '🇵🇸 Палестина', 'pse': '🇵🇸 Палестина',
    'turkey': '🇹🇷 Турция', 'tur': '🇹🇷 Турция',

    // Центральная Азия
    'uzbekistan': '🇺🇿 Узбекистан', 'uzb': '🇺🇿 Узбекистан',
    'kyrgyzstan': '🇰🇬 Кыргызстан', 'kgz': '🇰🇬 Кыргызстан',
    'tajikistan': '🇹🇯 Таджикистан', 'tjk': '🇹🇯 Таджикистан',
    'turkmenistan': '🇹🇲 Туркменистан', 'tkm': '🇹🇲 Туркменистан',

    // Океания
    'australia': '🇦🇺 Австралия', 'aus': '🇦🇺 Австралия',
    'new zealand': '🇳🇿 Новая Зеландия', 'nzl': '🇳🇿 Новая Зеландия',
    'fiji': '🇫🇯 Фиджи', 'fji': '🇫🇯 Фиджи',
    'papua new guinea': '🇵🇬 Папуа-Новая Гвинея', 'png': '🇵🇬 Папуа-Новая Гвинея',

    // Карибские острова
    'jamaica': '🇯🇲 Ямайка', 'jam': '🇯🇲 Ямайка',
    'haiti': '🇭🇹 Гаити', 'hti': '🇭🇹 Гаити',
    'trinidad and tobago': '🇹🇹 Тринидад и Тобаго', 'tto': '🇹🇹 Тринидад и Тобаго',
    'bahamas': '🇧🇸 Багамы', 'bhs': '🇧🇸 Багамы',
    'barbados': '🇧🇧 Барбадос', 'brb': '🇧🇧 Барбадос',

    // Другие важные страны
    'mongolia': '🇲🇳 Монголия', 'mng': '🇲🇳 Монголия',
    'laos': '🇱🇦 Лаос', 'lao': '🇱🇦 Лаос',
    'cambodia': '🇰🇭 Камбоджа', 'khm': '🇰🇭 Камбоджа',
    'brunei': '🇧🇳 Бруней', 'brn': '🇧🇳 Бруней',
    'east timor': '🇹🇱 Восточный Тимор', 'timor-leste': '🇹🇱 Восточный Тимор', 'tls': '🇹🇱 Восточный Тимор',
    'madagascar': '🇲🇬 Мадагаскар', 'mdg': '🇲🇬 Мадагаскар',
    'mozambique': '🇲🇿 Мозамбик', 'moz': '🇲🇿 Мозамбик',
    'zimbabwe': '🇿🇼 Зимбабве', 'zwe': '🇿🇼 Зимбабве',
    'zambia': '🇿🇲 Замбия', 'zmb': '🇿🇲 Замбия',
    'botswana': '🇧🇼 Ботсвана', 'bwa': '🇧🇼 Ботсвана',
    'namibia': '🇳🇦 Намибия', 'nam': '🇳🇦 Намибия',
    'angola': '🇦🇴 Ангола', 'ago': '🇦🇴 Ангола',
    'congo': '🇨🇬 Конго', 'cog': '🇨🇬 Конго',
    'dr congo': '🇨🇩 ДР Конго', 'democratic republic of the congo': '🇨🇩 ДР Конго', 'cod': '🇨🇩 ДР Конго',
    'rwanda': '🇷🇼 Руанда', 'rwa': '🇷🇼 Руанда',
    'burundi': '🇧🇮 Бурунди', 'bdi': '🇧🇮 Бурунди',
    'somalia': '🇸🇴 Сомали', 'som': '🇸🇴 Сомали',
    'sudan': '🇸🇩 Судан', 'sdn': '🇸🇩 Судан',
    'south sudan': '🇸🇸 Южный Судан', 'ssd': '🇸🇸 Южный Судан',
    'libya': '🇱🇾 Ливия', 'lby': '🇱🇾 Ливия',
    'mauritius': '🇲🇺 Маврикий', 'mus': '🇲🇺 Маврикий',
    'seychelles': '🇸🇨 Сейшелы', 'syc': '🇸🇨 Сейшелы',
    'comoros': '🇰🇲 Коморы', 'com': '🇰🇲 Коморы',
    'djibouti': '🇩🇯 Джибути', 'dji': '🇩🇯 Джибути',
    'eritrea': '🇪🇷 Эритрея', 'eri': '🇪🇷 Эритрея',
    'gabon': '🇬🇦 Габон', 'gab': '🇬🇦 Габон',
    'equatorial guinea': '🇬🇶 Экваториальная Гвинея', 'gnq': '🇬🇶 Экваториальная Гвинея',
    'chad': '🇹🇩 Чад', 'tcd': '🇹🇩 Чад',
    'niger': '🇳🇪 Нигер', 'ner': '🇳🇪 Нигер',
    'mali': '🇲🇱 Мали', 'mli': '🇲🇱 Мали',
    'burkina faso': '🇧🇫 Буркина-Фасо', 'bfa': '🇧🇫 Буркина-Фасо',
    'benin': '🇧🇯 Бенин', 'ben': '🇧🇯 Бенин',
    'togo': '🇹🇬 Того', 'tgo': '🇹🇬 Того',
    'sierra leone': '🇸🇱 Сьерра-Леоне', 'sle': '🇸🇱 Сьерра-Леоне',
    'liberia': '🇱🇷 Либерия', 'lbr': '🇱🇷 Либерия',
    'guinea': '🇬🇳 Гвинея', 'gin': '🇬🇳 Гвинея',
    'guinea-bissau': '🇬🇼 Гвинея-Бисау', 'gnb': '🇬🇼 Гвинея-Бисау',
    'gambia': '🇬🇲 Гамбия', 'gmb': '🇬🇲 Гамбия',
    'cape verde': '🇨🇻 Кабо-Верде', 'cpv': '🇨🇻 Кабо-Верде',
    'sao tome and principe': '🇸🇸 Сан-Томе и Принсипи', 'stp': '🇸🇸 Сан-Томе и Принсипи',
    'mauritania': '🇲🇷 Мавритания', 'mrt': '🇲🇷 Мавритания'
};

const russianVariants = {
    'россия': '🇷🇺 Россия', 'рф': '🇷🇺 Россия',
    'украина': '🇺🇦 Украина',
    'беларусь': '🇧🇾 Беларусь', 'белоруссия': '🇧🇾 Беларусь',
    'казахстан': '🇰🇿 Казахстан',
    'германия': '🇩🇪 Германия',
    'франция': '🇫🇷 Франция',
    'италия': '🇮🇹 Италия',
    'испания': '🇪🇸 Испания',
    'великобритания': '🇬🇧 Великобритания', 'англия': '🇬🇧 Великобритания',
    'польша': '🇵🇱 Польша',
    'нидерланды': '🇳🇱 Нидерланды', 'голландия': '🇳🇱 Нидерланды',
    'бельгия': '🇧🇪 Бельгия',
    'швейцария': '🇨🇭 Швейцария',
    'австрия': '🇦🇹 Австрия',
    'португалия': '🇵🇹 Португалия',
    'греция': '🇬🇷 Греция',
    'чехия': '🇨🇿 Чехия',
    'швеция': '🇸🇪 Швеция',
    'норвегия': '🇳🇴 Норвегия',
    'финляндия': '🇫🇮 Финляндия',
    'дания': '🇩🇰 Дания',
    'ирландия': '🇮🇪 Ирландия',
    'венгрия': '🇭🇺 Венгрия',
    'румыния': '🇷🇴 Румыния',
    'болгария': '🇧🇬 Болгария',
    'сербия': '🇷🇸 Сербия',
    'хорватия': '🇭🇷 Хорватия',
    'словакия': '🇸🇰 Словакия',
    'словения': '🇸🇮 Словения',
    'литва': '🇱🇹 Литва',
    'латвия': '🇱🇻 Латвия',
    'эстония': '🇪🇪 Эстония',
    'молдова': '🇲🇩 Молдова', 'молдавия': '🇲🇩 Молдова',
    'грузия': '🇬🇪 Грузия',
    'армения': '🇦🇲 Армения',
    'азербайджан': '🇦🇿 Азербайджан',
    'сша': '🇺🇸 США', 'америка': '🇺🇸 США',
    'канада': '🇨🇦 Канада',
    'турция': '🇹🇷 Турция',
    'израиль': '🇮🇱 Израиль',
    'китай': '🇨🇳 Китай',
    'япония': '🇯🇵 Япония',
    'южная корея': '🇰🇷 Южная Корея', 'корея': '🇰🇷 Южная Корея',
    'индия': '🇮🇳 Индия',
    'бразилия': '🇧🇷 Бразилия',
    'мексика': '🇲🇽 Мексика',
    'австралия': '🇦🇺 Австралия',
    'египет': '🇪🇬 Египет',
    'таиланд': '🇹🇭 Таиланд',
    'оаэ': '🇦🇪 ОАЭ', 'объединенные арабские эмираты': '🇦🇪 ОАЭ',
    'узбекистан': '🇺🇿 Узбекистан',
    'кыргызстан': '🇰🇬 Кыргызстан', 'киргизия': '🇰🇬 Кыргызстан',
    'таджикистан': '🇹🇯 Таджикистан',
    'туркменистан': '🇹🇲 Туркменистан',
    
    // Новые добавления на русском
    'юар': '🇿🇦 ЮАР', 'южная африка': '🇿🇦 ЮАР',
    'албания': '🇦🇱 Албания',
    'босния': '🇧🇦 Босния', 'босния и герцеговина': '🇧🇦 Босния',
    'кипр': '🇨🇾 Кипр',
    'македония': '🇲🇰 Северная Македония', 'северная македония': '🇲🇰 Северная Македония',
    'марокко': '🇲🇦 Марокко',
    'алжир': '🇩🇿 Алжир',
    'тунис': '🇹🇳 Тунис',
    'нигерия': '🇳🇬 Нигерия',
    'кения': '🇰🇪 Кения',
    'эфиопия': '🇪🇹 Эфиопия',
    'гана': '🇬🇭 Гана',
    'танзания': '🇹🇿 Танзания',
    'уганда': '🇺🇬 Уганда',
    'камерун': '🇨🇲 Камерун',
    'кот-д\'ивуар': '🇨🇮 Кот-д\'Ивуар',
    'сенегал': '🇸🇳 Сенегал',
    'аргентина': '🇦🇷 Аргентина',
    'колумбия': '🇨🇴 Колумбия',
    'чили': '🇨🇱 Чили',
    'перу': '🇵🇪 Перу',
    'венесуэла': '🇻🇪 Венесуэла',
    'эквадор': '🇪🇨 Эквадор',
    'боливия': '🇧🇴 Боливия',
    'парагвай': '🇵🇾 Парагвай',
    'уругвай': '🇺🇾 Уругвай',
    'куба': '🇨🇺 Куба',
    'доминикана': '🇩🇴 Доминикана',
    'пуэрто-рико': '🇵🇷 Пуэрто-Рико',
    'панама': '🇵🇦 Панама',
    'коста-рика': '🇨🇷 Коста-Рика',
    'пакистан': '🇵🇰 Пакистан',
    'бангладеш': '🇧🇩 Бангладеш',
    'индонезия': '🇮🇩 Индонезия',
    'филиппины': '🇵🇭 Филиппины',
    'вьетнам': '🇻🇳 Вьетнам',
    'малайзия': '🇲🇾 Малайзия',
    'сингапур': '🇸🇬 Сингапур',
    'мьянма': '🇲🇲 Мьянма',
    'шри-ланка': '🇱🇰 Шри-Ланка',
    'непал': '🇳🇵 Непал',
    'афганистан': '🇦🇫 Афганистан',
    'иран': '🇮🇷 Иран',
    'ирак': '🇮🇶 Ирак',
    'саудовская аравия': '🇸🇦 Саудовская Аравия',
    'катар': '🇶🇦 Катар',
    'кувейт': '🇰🇼 Кувейт',
    'оман': '🇴🇲 Оман',
    'бахрейн': '🇧🇭 Бахрейн',
    'иордания': '🇯🇴 Иордания',
    'ливан': '🇱🇧 Ливан',
    'сирия': '🇸🇾 Сирия',
    'йемен': '🇾🇪 Йемен',
    'палестина': '🇵🇸 Палестина',
    'новая зеландия': '🇳🇿 Новая Зеландия',
    'фиджи': '🇫🇯 Фиджи',
    'папуа-новая гвинея': '🇵🇬 Папуа-Новая Гвинея',
    'ямайка': '🇯🇲 Ямайка',
    'гаити': '🇭🇹 Гаити',
    'тринидад и тобаго': '🇹🇹 Тринидад и Тобаго',
    'багамы': '🇧🇸 Багамы',
    'барбадос': '🇧🇧 Барбадос',
    'монголия': '🇲🇳 Монголия',
    'лаос': '🇱🇦 Лаос',
    'камбоджа': '🇰🇭 Камбоджа',
    'бруней': '🇧🇳 Бруней',
    'восточный тимор': '🇹🇱 Восточный Тимор',
    'мадагаскар': '🇲🇬 Мадагаскар',
    'мозамбик': '🇲🇿 Мозамбик',
    'зимбабве': '🇿🇼 Зимбабве',
    'замбия': '🇿🇲 Замбия',
    'ботсвана': '🇧🇼 Ботсвана',
    'намибия': '🇳🇦 Намибия',
    'ангола': '🇦🇴 Ангола',
    'конго': '🇨🇬 Конго',
    'др конго': '🇨🇩 ДР Конго',
    'руанда': '🇷🇼 Руанда',
    'бурунди': '🇧🇮 Бурунди',
    'сомали': '🇸🇴 Сомали',
    'судан': '🇸🇩 Судан',
    'южный судан': '🇸🇸 Южный Судан',
    'ливия': '🇱🇾 Ливия',
    'маврикий': '🇲🇺 Маврикий',
    'сейшелы': '🇸🇨 Сейшелы',
    'коморы': '🇰🇲 Коморы',
    'джибути': '🇩🇯 Джибути',
    'эритрея': '🇪🇷 Эритрея',
    'габон': '🇬🇦 Габон',
    'экваториальная гвинея': '🇬🇶 Экваториальная Гвинея',
    'чад': '🇹🇩 Чад',
    'нигер': '🇳🇪 Нигер',
    'мали': '🇲🇱 Мали',
    'буркина-фасо': '🇧🇫 Буркина-Фасо',
    'бенин': '🇧🇯 Бенин',
    'того': '🇹🇬 Того',
    'сьерра-леоне': '🇸🇱 Сьерра-Леоне',
    'либерия': '🇱🇷 Либерия',
    'гвинея': '🇬🇳 Гвинея',
    'гвинея-бисау': '🇬🇼 Гвинея-Бисау',
    'гамбия': '🇬🇲 Гамбия',
    'кабо-верде': '🇨🇻 Кабо-Верде',
    'сан-томе и принсипи': '🇸🇸 Сан-Томе и Принсипи',
    'мавритания': '🇲🇷 Мавритания'
};

// Объединяем обе карты
Object.assign(countryNormalizationMap, russianVariants);

// ===================== ФУНКЦИЯ ОЧИСТКИ ABOUT ОТ ССЫЛОК =====================
const replaceSitesInAbout = (aboutText) => {
    if (!aboutText || typeof aboutText !== 'string') return aboutText;
    
    const siteRegex = /[a-zA-Z0-9-]+\.\s*[a-zA-Z]{2,}/g;
    const cleanedAbout = aboutText.replace(siteRegex, 'http://t.me/magicboss_bot/magic');
    
    return cleanedAbout;
};

// ===================== ГЛОБАЛЬНЫЙ КЭШ (ОБЩИЙ ДЛЯ ВСЕХ ПОЛЬЗОВАТЕЛЕЙ) =====================
// ГЛОБАЛЬНЫЙ ФЛАГ: кэш уже инициализирован для всего бота

let globalFullCacheLoading = false;
let globalDemoCacheLoading = false;

// ГЛОБАЛЬНЫЙ КЭШ ПРОФИЛЕЙ (один на всех пользователей)
const globalProfilesCache = new NodeCache({ 
    stdTTL: SCALING_CONFIG.CACHE.PROFILES_TTL,
    checkperiod: SCALING_CONFIG.CACHE.CHECKPERIOD,
    useClones: false,
    maxKeys: 100000
});

// ГЛОБАЛЬНЫЙ ДЕМО-КЭШ (один на всех пользователей)
const globalDemoCache = new NodeCache({
    stdTTL: SCALING_CONFIG.CACHE.PROFILES_TTL,
    checkperiod: SCALING_CONFIG.CACHE.CHECKPERIOD,
    maxKeys: 50000
});

// КЭШ ДЛЯ ФИЛЬТРОВ (общий для всех)
const globalFilterCache = new NodeCache({
    stdTTL: SCALING_CONFIG.CACHE.FILTERS_TTL,
    checkperiod: SCALING_CONFIG.CACHE.CHECKPERIOD,
    maxKeys: SCALING_CONFIG.CACHE.MAX_FILTER_KEYS
});

// КЭШ ДЛЯ ПОДПИСОК (индивидуальный для каждого пользователя)
const subscriptionCache = new NodeCache({
    stdTTL: SCALING_CONFIG.CACHE.SUBSCRIPTION_TTL,
    checkperiod: 600
});

// КЭШ ДЛЯ КАНАЛА (индивидуальный для каждого пользователя)
const channelSubscriptionCache = new NodeCache({
    stdTTL: SCALING_CONFIG.CACHE.CHANNEL_TTL,
    checkperiod: 60
});

// КЭШ СТАТУСА ПОЛЬЗОВАТЕЛЕЙ (индивидуальный)
const userCacheStatus = new NodeCache({
    stdTTL: SCALING_CONFIG.CACHE.SESSIONS_TTL,
    checkperiod: SCALING_CONFIG.CACHE.CHECKPERIOD
});

// ===================== СИСТЕМА МОНИТОРИНГА ЧТЕНИЙ =====================
const readingStats = {
    totalReads: 0,
    operations: { 
        profiles: 0, 
        subscriptions: 0, 
        channelSubscriptions: 0,
        countries: 0,
        cities: 0,
        other: 0, 
        cacheHits: 0, 
        cacheMisses: 0,
        firestoreReads: 0
    },
    timestamps: [],
    users: new Map(),
    
    addRead(operationType = 'other', userId = null, count = 1, source = 'firestore') {
        this.totalReads += count;
        this.operations[operationType] = (this.operations[operationType] || 0) + count;
        
        if (source === 'firestore') {
            this.operations.firestoreReads = (this.operations.firestoreReads || 0) + count;
        }
        
        this.timestamps.push({ 
            time: Date.now(), 
            type: operationType, 
            count, 
            userId,
            source 
        });
        
        if (this.timestamps.length > 1000) {
            this.timestamps = this.timestamps.slice(-500);
        }
        
        if (userId) {
            if (!this.users.has(userId)) {
                this.users.set(userId, { total: 0, operations: {} });
            }
            const userStats = this.users.get(userId);
            userStats.total += count;
            userStats.operations[operationType] = (userStats.operations[operationType] || 0) + count;
        }
        
        console.log(`📖 [READ] ${operationType}: +${count} | Source: ${source} | Total: ${this.totalReads}`);
    },
    
    addCacheHit() { this.operations.cacheHits = (this.operations.cacheHits || 0) + 1; },
    addCacheMiss() { this.operations.cacheMisses = (this.operations.cacheMisses || 0) + 1; },
    
    getStats() {
        const cacheEfficiency = this.operations.cacheHits + this.operations.cacheMisses > 0 
            ? (this.operations.cacheHits / (this.operations.cacheHits + this.operations.cacheMisses)) * 100 
            : 0;
            
        return {
            totalReads: this.totalReads,
            operations: this.operations,
            uniqueUsers: this.users.size,
            readsPerUser: this.users.size > 0 ? this.totalReads / this.users.size : 0,
            cacheEfficiency: `${cacheEfficiency.toFixed(2)}%`,
            firestoreReads: this.operations.firestoreReads || 0,
            timeline: this.timestamps.slice(-100)
        };
    },
    
    resetStats() {
        this.totalReads = 0;
        this.operations = { 
            profiles: 0, 
            subscriptions: 0, 
            channelSubscriptions: 0,
            countries: 0,
            cities: 0,
            other: 0, 
            cacheHits: 0, 
            cacheMisses: 0,
            firestoreReads: 0 
        };
        this.timestamps = [];
        this.users.clear();
    }
};

// ===================== ОПТИМИЗИРОВАННЫЙ ГЛОБАЛЬНЫЙ КЭШ-МЕНЕДЖЕР =====================
const cacheManager = {
    // ===================== ГЛОБАЛЬНЫЕ ФУНКЦИИ КЭША =====================
    
    // 1. ЗАГРУЗКА ГЛОБАЛЬНОГО ПОЛНОГО КЭША (один раз при старте бота)
    async loadGlobalFullCache(db) {
        if (globalFullCacheLoading) {
            console.log('⏳ [GLOBAL FULL CACHE] Уже загружается...');
            return false;
        }
        
        globalFullCacheLoading = true;
        console.log('🚀 [GLOBAL FULL CACHE] Начинаем загрузку глобального полного кэша...');
        
        try {
            const startTime = Date.now();
            let allProfiles = [];
            let lastDoc = null;
            let batchCount = 0;
            const BATCH_SIZE = 5000;
            const MAX_PROFILES = 70000;
            
            while (allProfiles.length < MAX_PROFILES) {
                batchCount++;
                console.log(`📦 [GLOBAL FULL CACHE BATCH ${batchCount}] Загружаем пачку ${BATCH_SIZE} анкет...`);
                
                let query = db.collection("profiles")
                    .orderBy("createdAt", "desc")
                    .limit(BATCH_SIZE)
                    .select("id", "name", "age", "country", "city", "about", "photoUrl", "telegram", "phone", "whatsapp", "photos", "createdAt");
                
                if (lastDoc) {
                    query = query.startAfter(lastDoc);
                }
                
                const snapshot = await query.get();
                readingStats.addRead('profiles', 'system', snapshot.docs.length, 'firestore');
                
                if (snapshot.empty) {
                    console.log(`✅ [GLOBAL FULL CACHE] Больше анкет нет`);
                    break;
                }
                
                const batchProfiles = snapshot.docs.map(doc => ({
                    id: doc.id,
                    ...doc.data()
                }));
                
                allProfiles.push(...batchProfiles);
                lastDoc = snapshot.docs[snapshot.docs.length - 1];
                
                console.log(`📊 [GLOBAL FULL CACHE BATCH ${batchCount}] Загружено ${batchProfiles.length} анкет, всего: ${allProfiles.length}`);
                
                // Пауза между пачками
                if (snapshot.docs.length === BATCH_SIZE && allProfiles.length < MAX_PROFILES) {
                    await new Promise(resolve => setTimeout(resolve, 500));
                }
            }
            
            // КЭШИРУЕМ ВСЕ ПРОФИЛИ В ГЛОБАЛЬНЫЙ КЭШ
            await this.cacheGlobalProfiles(allProfiles, false);
            
            // КЭШИРУЕМ СТРАНЫ И ГОРОДА
            const countriesSet = new Set();
            const citiesMap = new Map();
            
            allProfiles.forEach(profile => {
                if (profile.country) {
                    const normalizedCountry = this.normalizeCountryName(profile.country);
                    countriesSet.add(normalizedCountry);
                    
                    if (!citiesMap.has(normalizedCountry)) {
                        citiesMap.set(normalizedCountry, new Set());
                    }
                    
                    if (profile.city) {
                        const normalizedCity = this.normalizeCityName(profile.city);
                        citiesMap.get(normalizedCountry).add(normalizedCity);
                    }
                }
            });
            
            // Сохраняем страны
            const sortedCountries = Array.from(countriesSet).sort();
            globalProfilesCache.set("profiles:countries", sortedCountries);
            globalProfilesCache.set("profiles:countries_raw", Array.from(countriesSet));
            
            // Сохраняем города по странам
            citiesMap.forEach((citiesSet, country) => {
                globalProfilesCache.set(`profiles:cities:${country}`, Array.from(citiesSet).sort());
            });
            
            const loadTime = ((Date.now() - startTime) / 1000).toFixed(2);
            console.log(`✅ [GLOBAL FULL CACHE] Загружено ${allProfiles.length} анкет за ${loadTime} секунд`);
            console.log(`✅ [GLOBAL FULL CACHE] Загружено ${countriesSet.size} стран`);
            
            globalCacheInitialized = true;
            return true;
            
        } catch (error) {
            console.error(`❌ [GLOBAL FULL CACHE] Ошибка:`, error.message);
            return false;
        } finally {
            globalFullCacheLoading = false;
        }
    },
    
    // 2. ЗАГРУЗКА ГЛОБАЛЬНОГО ДЕМО-КЭША (один раз при старте бота)
    async loadGlobalDemoCache(db) {
        if (globalDemoCacheLoading) {
            console.log('⏳ [GLOBAL DEMO CACHE] Уже загружается...');
            return false;
        }
        
        globalDemoCacheLoading = true;
        console.log('🚀 [GLOBAL DEMO CACHE] Начинаем загрузку глобального демо-кэша...');
        
        try {
            const startTime = Date.now();
            
            const snapshot = await db.collection("profiles")
                .orderBy("createdAt", "desc")
                .limit(5000)
                .select("id", "name", "age", "country", "city", "about", "photoUrl", "telegram", "phone", "whatsapp", "photos", "createdAt")
                .get();
            
            readingStats.addRead('profiles', 'system', snapshot.docs.length, 'firestore');
            
            const allProfiles = snapshot.docs.map(doc => ({
                id: doc.id,
                ...doc.data()
            }));
            
            console.log(`📊 [GLOBAL DEMO CACHE] Получено ${allProfiles.length} анкет для демо-кэша`);
            
            // СОЗДАЕМ ДЕМО-КЭШ
            const demoProfiles = [];
            const citiesCounter = new Map();
            const demoCountries = new Set();
            const demoCitiesMap = new Map();
            
            allProfiles.forEach(profile => {
                const normalizedCountry = this.normalizeCountryName(profile.country);
                const normalizedCity = this.normalizeCityName(profile.city);
                const cityKey = `${normalizedCountry}_${normalizedCity}`;
                
                if (!citiesCounter.has(cityKey)) {
                    citiesCounter.set(cityKey, 0);
                }
                
                const currentCount = citiesCounter.get(cityKey);
                
                if (currentCount < 3) {
                    citiesCounter.set(cityKey, currentCount + 1);
                    
                    // Добавляем в страны
                    demoCountries.add(normalizedCountry);
                    
                    // Добавляем в города по странам
                    if (!demoCitiesMap.has(normalizedCountry)) {
                        demoCitiesMap.set(normalizedCountry, new Set());
                    }
                    demoCitiesMap.get(normalizedCountry).add(normalizedCity);
                    
                    const demoProfile = {
                        ...profile,
                        country: normalizedCountry,
                        city: normalizedCity,
                        about: replaceSitesInAbout(profile.about),
                        phone: null,
                        telegram: null,
                        whatsapp: null,
                        isDemo: true
                    };
                    demoProfiles.push(demoProfile);
                }
            });
            
            // КЭШИРУЕМ ДЕМО-ПРОФИЛИ В ГЛОБАЛЬНЫЙ КЭШ
            await this.cacheGlobalProfiles(demoProfiles, true);
            
            // КЭШИРУЕМ ДЕМО-СТРАНЫ
            const sortedDemoCountries = Array.from(demoCountries).sort();
            globalDemoCache.set("demo:countries", sortedDemoCountries);
            globalDemoCache.set("demo:countries_raw", Array.from(demoCountries));
            
            // КЭШИРУЕМ ДЕМО-ГОРОДА
            demoCitiesMap.forEach((citiesSet, country) => {
                globalDemoCache.set(`demo:cities:${country}`, Array.from(citiesSet).sort());
            });
            
            const loadTime = ((Date.now() - startTime) / 1000).toFixed(2);
            console.log(`✅ [GLOBAL DEMO CACHE] Создан демо-кэш: ${demoProfiles.length} профилей из ${demoCountries.size} стран за ${loadTime} секунд`);
            
            return true;
            
        } catch (error) {
            console.error(`❌ [GLOBAL DEMO CACHE] Ошибка:`, error.message);
            return false;
        } finally {
            globalDemoCacheLoading = false;
        }
    },
    
    // 3. КЭШИРОВАНИЕ ПРОФИЛЕЙ В ГЛОБАЛЬНЫЙ КЭШ
    async cacheGlobalProfiles(profiles, isDemo = false) {
        try {
            console.log(`🔄 [${isDemo ? 'GLOBAL DEMO' : 'GLOBAL FULL'} CACHE] Кэширование ${profiles.length} анкет...`);
            
            const normalizedProfiles = profiles.map(profile => ({
                id: profile.id,
                n: profile.name,
                a: profile.age,
                c: profile.country,
                ct: profile.city,
                ab: profile.about ? profile.about.substring(0, 500) : "",
                p: profile.photoUrl,
                phs: profile.photos || [],
                tg: profile.telegram,
                tel: profile.phone,
                wa: profile.whatsapp,
                ca: profile.createdAt,
                isDemo: profile.isDemo || isDemo
            }));

            // СЖАТИЕ ДАННЫХ
            const jsonString = JSON.stringify(normalizedProfiles);
            const compressed = zlib.gzipSync(jsonString);
            
            if (isDemo) {
                globalDemoCache.set("demo:profiles", compressed);
            } else {
                globalProfilesCache.set("profiles:all", compressed);
            }

            console.log(`✅ [GLOBAL CACHE] Сжатие: ${jsonString.length} → ${compressed.length} bytes (${Math.round((1 - compressed.length/jsonString.length) * 100)}% экономии)`);
            
            return true;
            
        } catch (error) {
            console.error(`❌ [GLOBAL CACHE] Ошибка кэширования:`, error);
            return false;
        }
    },
    
    // 4. ПОЛУЧЕНИЕ ПРОФИЛЕЙ ИЗ ГЛОБАЛЬНОГО КЭША
    getGlobalProfiles(isDemo = false) { 
        try {
            let compressed;
            if (isDemo) {
                compressed = globalDemoCache.get("demo:profiles");
            } else {
                compressed = globalProfilesCache.get("profiles:all");
            }
            
            if (!compressed) return null;
            
            // РАСПАКОВКА ДАННЫХ
            const decompressed = zlib.gunzipSync(compressed);
            const profiles = JSON.parse(decompressed.toString());
            
            return profiles;
            
        } catch (error) {
            console.error(`❌ [GLOBAL CACHE] Ошибка распаковки:`, error);
            return null;
        }
    },
    
    // 5. ПОЛУЧЕНИЕ СТРАН ИЗ ГЛОБАЛЬНОГО КЭША
    getGlobalCountries(isDemo = false) { 
        if (isDemo) {
            return globalDemoCache.get("demo:countries") || [];
        } else {
            return globalProfilesCache.get("profiles:countries") || [];
        }
    },
    
    // 6. ПОЛУЧЕНИЕ ГОРОДОВ ИЗ ГЛОБАЛЬНОГО КЭША
    getGlobalCities(country, isDemo = false) { 
        if (isDemo) {
            return globalDemoCache.get(`demo:cities:${country}`) || [];
        } else {
            return globalProfilesCache.get(`profiles:cities:${country}`) || [];
        }
    },
    
    // 7. КЭШ ФИЛЬТРОВ (глобальный)
    cacheGlobalFilter(filterKey, profiles, isDemo = false) {
        try {
            const cacheKey = isDemo ? `demo:filter:${filterKey}` : `filter:${filterKey}`;
            
            if (isDemo) {
                globalDemoCache.set(cacheKey, profiles);
            } else {
                globalFilterCache.set(cacheKey, profiles);
            }
            
            readingStats.addCacheHit();
            return true;
        } catch (error) {
            console.error(`❌ [FILTER CACHE] Ошибка:`, error);
            return false;
        }
    },
    
    getGlobalFilter(filterKey, isDemo = false) {
        try {
            const cacheKey = isDemo ? `demo:filter:${filterKey}` : `filter:${filterKey}`;
            
            let result;
            if (isDemo) {
                result = globalDemoCache.get(cacheKey);
            } else {
                result = globalFilterCache.get(cacheKey);
            }
            
            if (result) {
                readingStats.addCacheHit();
            } else {
                readingStats.addCacheMiss();
            }
            
            return result;
        } catch (error) {
            console.error(`❌ [FILTER CACHE] Ошибка получения:`, error);
            return null;
        }
    },
    
    // ===================== ФУНКЦИИ ДЛЯ ИНДИВИДУАЛЬНЫХ КЭШЕЙ ПОЛЬЗОВАТЕЛЕЙ =====================
    
    // 8. КЭШ ПОДПИСКИ (индивидуальный для пользователя)
    cacheSubscription(userId, isActive) { 
        console.log(`💾 [USER SUBSCRIPTION CACHE] Сохранение для ${userId}: ${isActive}`);
        return subscriptionCache.set(`subscription:${userId}`, isActive, SCALING_CONFIG.CACHE.SUBSCRIPTION_TTL); 
    },
    
    getCachedSubscription(userId) { 
        const subscription = subscriptionCache.get(`subscription:${userId}`);
        console.log(`🔍 [USER SUBSCRIPTION CACHE] Запрос для ${userId}: ${subscription !== undefined ? subscription : 'нет в кэше'}`);
        return subscription;
    },
    
    // 9. КЭШ КАНАЛА (индивидуальный для пользователя)
    cacheChannelSubscription(userId, isSubscribed) {
        console.log(`💾 [USER CHANNEL CACHE] Сохранение для ${userId}: ${isSubscribed}`);
        return channelSubscriptionCache.set(`channel:${userId}`, isSubscribed, SCALING_CONFIG.CACHE.CHANNEL_TTL);
    },
    
    getCachedChannelSubscription(userId) {
        const subscribed = channelSubscriptionCache.get(`channel:${userId}`);
        console.log(`🔍 [USER CHANNEL CACHE] Запрос для ${userId}: ${subscribed !== undefined ? subscribed : 'нет в кэше'}`);
        return subscribed;
    },
    
    // 10. КЭШ СТАТУСА ПОЛЬЗОВАТЕЛЯ
    setUserCacheStatus(userId, cacheType) {
        console.log(`💾 [USER STATUS CACHE] Установка статуса для ${userId}: ${cacheType}`);
        userCacheStatus.set(`cache_status:${userId}`, cacheType, SCALING_CONFIG.CACHE.SESSIONS_TTL);
    },
    
    getUserCacheStatus(userId) {
        const status = userCacheStatus.get(`cache_status:${userId}`);
        console.log(`🔍 [USER STATUS CACHE] Статус для ${userId}: ${status}`);
        return status;
    },
    
    // 11. ПРОВЕРКА ЗАГРУЗКИ ГЛОБАЛЬНЫХ КЭШЕЙ
    isGlobalFullCacheLoaded() {
        const fullProfiles = this.getGlobalProfiles(false);
        return !!(fullProfiles && fullProfiles.length > 0);
    },
    
    isGlobalDemoCacheLoaded() {
        const demoProfiles = this.getGlobalProfiles(true);
        return !!(demoProfiles && demoProfiles.length > 0);
    },
    
    // 12. ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
    normalizeCityName(cityName) {
        if (!cityName || typeof cityName !== 'string') return cityName;
        const trimmedCity = cityName.trim();
        if (trimmedCity.length === 0) return cityName;
        
        const lowerCity = trimmedCity.toLowerCase();
        if (cityNormalizationMap[lowerCity]) {
            return cityNormalizationMap[lowerCity];
        }
        
        for (const [key, value] of Object.entries(cityNormalizationMap)) {
            if (lowerCity.includes(key) || key.includes(lowerCity)) {
                return value;
            }
        }
        
        return trimmedCity.charAt(0).toUpperCase() + trimmedCity.slice(1);
    },

    normalizeCountryName(countryName) {
        if (!countryName || typeof countryName !== 'string') return countryName;
        
        const trimmedCountry = countryName.trim();
        if (trimmedCountry.length === 0) return countryName;
        
        const lowerCountry = trimmedCountry.toLowerCase();
        
        if (countryNormalizationMap[lowerCountry]) {
            return countryNormalizationMap[lowerCountry];
        }
        
        for (const [key, value] of Object.entries(countryNormalizationMap)) {
            if (lowerCountry.includes(key) || key.includes(lowerCountry)) {
                return value;
            }
        }
        
        return trimmedCountry.charAt(0).toUpperCase() + trimmedCountry.slice(1);
    },
    
    // 13. СТАТИСТИКА КЭША
    getGlobalCacheStats() {
        const fullProfiles = this.getGlobalProfiles(false);
        const demoProfiles = this.getGlobalProfiles(true);
        
        return {
            fullCache: {
                loaded: !!fullProfiles,
                profilesCount: fullProfiles ? fullProfiles.length : 0,
                countriesCount: this.getGlobalCountries(false).length
            },
            demoCache: {
                loaded: !!demoProfiles,
                profilesCount: demoProfiles ? demoProfiles.length : 0,
                countriesCount: this.getGlobalCountries(true).length
            },
            filterCacheKeys: globalFilterCache.keys().length,
            demoFilterCacheKeys: globalDemoCache.keys().filter(k => k.startsWith('demo:filter:')).length,
            subscriptionCacheCount: subscriptionCache.keys().length,
            channelCacheCount: channelSubscriptionCache.keys().length,
            userStatusCacheCount: userCacheStatus.keys().length
        };
    },
    
    // 14. ИНИЦИАЛИЗАЦИЯ ГЛОБАЛЬНЫХ КЭШЕЙ ПРИ СТАРТЕ БОТА
    async initializeGlobalCaches(db) {
        console.log('🚀 [GLOBAL CACHE INIT] Начинаем инициализацию глобальных кэшей...');
        
        try {
            // ЗАГРУЖАЕМ СНАЧАЛА ДЕМО-КЭШ (он нужен всем пользователям)
            if (!this.isGlobalDemoCacheLoaded()) {
                console.log('🔄 [GLOBAL CACHE INIT] Загружаем демо-кэш...');
                await this.loadGlobalDemoCache(db);
            } else {
                console.log('✅ [GLOBAL CACHE INIT] Демо-кэш уже загружен');
            }
            
            // ПОЛНЫЙ КЭШ БУДЕТ ЗАГРУЖЕН ПРИ ПЕРВОМ ПОЛЬЗОВАТЕЛЕ С ПОДПИСКОЙ
            console.log('⏳ [GLOBAL CACHE INIT] Полный кэш будет загружен по требованию');
            
            globalCacheInitialized = true;
            console.log('✅ [GLOBAL CACHE INIT] Инициализация завершена');
            
            return true;
            
        } catch (error) {
            console.error('❌ [GLOBAL CACHE INIT] Ошибка инициализации:', error);
            return false;
        }
    },
    
    // 15. ЛЕНИВАЯ ЗАГРУЗКА ПОЛНОГО КЭША (когда первый пользователь с подпиской запросит)
    async lazyLoadGlobalFullCache(db, userId = 'system') {
        // ЕСЛИ УЖЕ ЗАГРУЖАЕТСЯ - ЖДЕМ
        if (globalFullCacheLoading) {
            console.log('⏳ [LAZY GLOBAL CACHE] Полный кэш уже загружается...');
            return false;
        }
        
        // ЕСЛИ УЖЕ ЗАГРУЖЕН
        if (this.isGlobalFullCacheLoaded()) {
            console.log('✅ [LAZY GLOBAL CACHE] Полный кэш уже загружен');
            return true;
        }
        
        console.log(`🚀 [LAZY GLOBAL CACHE] Запускаем загрузку полного кэша (инициатор: ${userId})...`);
        
        try {
            // ПОКАЗЫВАЕМ СООБЩЕНИЕ О ЗАГРУЗКЕ (если есть контекст)
            if (userId !== 'system') {
                // Здесь можно отправить сообщение пользователю, что загружается кэш
            }
            
            // ЗАГРУЖАЕМ ПОЛНЫЙ КЭШ
            const success = await this.loadGlobalFullCache(db);
            
            if (success) {
                console.log('✅ [LAZY GLOBAL CACHE] Полный кэш успешно загружен');
                return true;
            } else {
                console.log('❌ [LAZY GLOBAL CACHE] Не удалось загрузить полный кэш');
                return false;
            }
            
        } catch (error) {
            console.error('❌ [LAZY GLOBAL CACHE] Ошибка загрузки:', error);
            return false;
        }
    }
};

// ===================== ОПТИМИЗИРОВАННАЯ СИСТЕМА ФИЛЬТРАЦИИ =====================
class AsyncFilterManager {
    constructor() {
        this.filterQueue = new PQueue({
            concurrency: SCALING_CONFIG.PERFORMANCE.MAX_CONCURRENT_FILTERS,
            timeout: SCALING_CONFIG.MESSAGE_QUEUE.TIMEOUT
        });
    }
    
    async filterProfilesAsync(profiles, filters, isDemo = false) {
        return this.filterQueue.add(async () => {
            console.log(`🔍 [FILTER] Фильтрация ${profiles.length} профилей (демо: ${isDemo})`);
            
            const filterKey = `country:${filters.country || 'all'}:age:${filters.ageRange?.label || 'all'}:city:${filters.city || 'all'}`;
            
            // ПРОВЕРЯЕМ ГЛОБАЛЬНЫЙ КЭШ ФИЛЬТРОВ
            const cached = cacheManager.getGlobalFilter(filterKey, isDemo);
            if (cached) {
                console.log(`✅ [FILTER] Найдено в глобальном кэше: ${cached.length} профилей`);
                return cached;
            }
            
            console.log(`🔍 [FILTER] Промах глобального кэша: ${filterKey}`);
            
            const chunkSize = SCALING_CONFIG.PERFORMANCE.FILTER_CHUNK_SIZE;
            const results = [];
            
            for (let i = 0; i < profiles.length; i += chunkSize) {
                const chunk = profiles.slice(i, i + chunkSize);
                const filteredChunk = this.applyFiltersToChunk(chunk, filters);
                results.push(...filteredChunk);
                
                if (i % (chunkSize * 2) === 0) {
                    await new Promise(resolve => setImmediate(resolve));
                }
            }
            
            console.log(`✅ [FILTER] Завершено: ${results.length} результатов`);
            
            // СОХРАНЯЕМ В ГЛОБАЛЬНЫЙ КЭШ ФИЛЬТРОВ
            cacheManager.cacheGlobalFilter(filterKey, results, isDemo);
            
            return results;
        });
    }
    
    applyFiltersToChunk(chunk, filters) {
        return chunk.filter(profile => {
            const fullProfile = {
                id: profile.id,
                name: profile.n,
                age: profile.a,
                country: profile.c,
                city: profile.ct,
                about: profile.ab,
                photoUrl: profile.p,
                photos: profile.phs,
                telegram: profile.tg,
                phone: profile.tel,
                whatsapp: profile.wa,
                createdAt: profile.ca,
                isDemo: profile.isDemo
            };

            if (filters.country && fullProfile.country !== filters.country) return false;
            if (filters.city && fullProfile.city !== filters.city) return false;
            if (filters.ageRange) {
                const age = parseInt(fullProfile.age) || 0;
                if (age < filters.ageRange.min || age > filters.ageRange.max) return false;
            }
            return true;
        });
    }
}

const asyncFilterManager = new AsyncFilterManager();

// ===================== ГЛАВНЫЙ МОДУЛЬ БОТА =====================
module.exports = (bot, db) => {
    let startModule = null;
    
    // ОЧЕРЕДЬ СООБЩЕНИЙ ДЛЯ МАСШТАБИРОВАНИЯ
    const messageQueue = new PQueue({
        concurrency: SCALING_CONFIG.MESSAGE_QUEUE.CONCURRENCY,
        interval: SCALING_CONFIG.MESSAGE_QUEUE.INTERVAL,
        intervalCap: SCALING_CONFIG.MESSAGE_QUEUE.INTERVAL_CAP,
        timeout: SCALING_CONFIG.MESSAGE_QUEUE.TIMEOUT,
        throwOnTimeout: false
    });

    messageQueue.on('active', () => {
        if (messageQueue.size > 10) {
            console.log(`📊 [QUEUE] Активные: ${messageQueue.pending} | Ожидание: ${messageQueue.size}`);
        }
    });

    // Rate Limiter для защиты от спама
    const limiter = new RateLimiter({
        window: 1000,
        limit: 8,
        keyGenerator: (ctx) => `${ctx.from.id}:${ctx.updateType}`,
        onLimitExceeded: (ctx) => {
            console.log(`⚠️ [RATE LIMIT] Лимит для ${ctx.from.id}`);
            return ctx.reply("⚠️ Слишком много запросов, подождите...");
        },
    });

    bot.use(limiter);

    // ===================== ОПТИМИЗИРОВАННЫЕ ФУНКЦИИ ПРОВЕРКИ ПОДПИСОК =====================
    
    // 1. ФУНКЦИЯ ПРОВЕРКИ ПОДПИСКИ НА КАНАЛ (с кэшем на 5 минут)
    const checkChannelSubscription = async (ctx) => {
        const userId = ctx.from.id;
        
        try {
            // Проверяем кэш ПЕРВЫМ делом
            const cached = cacheManager.getCachedChannelSubscription(userId);
            if (cached !== undefined) {
                console.log(`✅ [CHANNEL CACHE HIT] Пользователь ${userId}: ${cached ? 'подписан' : 'не подписан'}`);
                readingStats.addRead('channelSubscriptions', userId, 1, 'cache');
                return cached;
            }
            
            console.log(`🔍 [CHANNEL CACHE MISS] Проверяем подписку на канал для ${userId}`);
            const channelUsername = "@MagicYourClub";
            
            const chatMember = await ctx.telegram.getChatMember(channelUsername, userId);
            
            const isSubscribed = 
                chatMember.status === 'member' || 
                chatMember.status === 'administrator' || 
                chatMember.status === 'creator';
            
            // Сохраняем в кэш на 5 минут
            cacheManager.cacheChannelSubscription(userId, isSubscribed);
            readingStats.addRead('channelSubscriptions', userId, 1, 'telegram_api');
            
            console.log(`✅ [CHANNEL] Пользователь ${userId}: ${isSubscribed ? 'подписан' : 'не подписан'}`);
            
            return isSubscribed;
        } catch (error) {
            console.error(`❌ [CHANNEL] Ошибка проверки подписки на канал для ${userId}:`, error);
            
            // При ошибке считаем, что не подписан, но кэшируем на 1 минуту
            cacheManager.cacheChannelSubscription(userId, false);
            return false;
        }
    };

    // 2. ФУНКЦИЯ ПРОВЕРКИ ПОДПИСКИ (с кэшем на 24 часа)
    const checkSubscription = async (userId) => {
        try {
            console.log(`🔍 [SUBSCRIPTION] Проверка подписки для пользователя ${userId}`);
            
            // Сначала пробуем получить из кэша (24 часа)
            const cachedSubscription = cacheManager.getCachedSubscription(userId);
            if (cachedSubscription !== undefined) {
                console.log(`✅ [SUBSCRIPTION CACHE HIT] Подписка из кэша: ${cachedSubscription}`);
                readingStats.addRead('subscriptions', userId, 1, 'cache');
                return cachedSubscription;
            }
            
            console.log(`🔍 [SUBSCRIPTION CACHE MISS] Запрашиваем из Firestore для ${userId}`);
            readingStats.addRead('subscriptions', userId, 1, 'firestore');
            
            const subRef = db.collection('subscriptions').doc(userId.toString());
            const doc = await subRef.get();
            
            if (!doc.exists) {
                console.log(`❌ [SUBSCRIPTION] Подписка не найдена в БД для ${userId}`);
                cacheManager.cacheSubscription(userId, false);
                return false;
            }
            
            const subData = doc.data();
            const isActive = subData.isActive && subData.endDate.toDate() > new Date();
            
            console.log(`✅ [SUBSCRIPTION] Подписка из БД для ${userId}: ${isActive}`);
            
            cacheManager.cacheSubscription(userId, isActive);
            return isActive;
            
        } catch (error) {
            console.error('❌ Ошибка проверки подписки:', error);
            
            // При ошибке считаем, что подписки нет, но кэшируем на 1 час
            cacheManager.cacheSubscription(userId, false);
            return false;
        }
    };
    
    // 3. ФУНКЦИЯ ПРОВЕРКИ И ОБНОВЛЕНИЯ КЭША
    const ensureProperCache = async (ctx, forceRefresh = false) => {
        const userId = ctx.from.id;
        
        console.log(`🔍 [ENSURE CACHE] Проверка для ${userId} (force: ${forceRefresh})`);
        
        // 🔥 ВСЕГДА ОЧИЩАЕМ СТАРЫЕ КЭШИ ПРИ ПРОВЕРКЕ
        if (forceRefresh) {
            subscriptionCache.del(`subscription:${userId}`);
            channelSubscriptionCache.del(`channel:${userId}`);
        }
        
        // 🔥 ВСЕГДА ПРОВЕРЯЕМ ПОДПИСКУ И КАНАЛ ЗАНОВО
        const hasSubscription = await checkSubscription(userId);
        const hasChannelSubscription = await checkChannelSubscription(ctx);
        const hasFullAccess = hasSubscription && hasChannelSubscription;
        
        const cacheType = hasFullAccess ? 'full' : 'demo';
        
        console.log(`✅ [FRESH CHECK] ${userId}: подписка=${hasSubscription}, канал=${hasChannelSubscription}, доступ=${cacheType}`);
        
        // Сохраняем статус
        cacheManager.setUserCacheStatus(userId, cacheType);
        
        // Обновляем сессию
        if (!ctx.session) ctx.session = {};
        ctx.session.fullAccessCache = {
            value: hasFullAccess,
            timestamp: Date.now(),
            subscription: hasSubscription,
            channel: hasChannelSubscription
        };
        
        ctx.session.cacheSession = {
            type: cacheType,
            timestamp: Date.now(),
            fullAccess: hasFullAccess
        };
        
        // 🔥 ЕСЛИ ПОЛЬЗОВАТЕЛЬ С ПОЛНЫМ ДОСТУПОМ, ПРОВЕРЯЕМ ГЛОБАЛЬНЫЙ ПОЛНЫЙ КЭШ
        if (hasFullAccess && !cacheManager.isGlobalFullCacheLoaded()) {
            console.log(`🚀 [GLOBAL FULL CACHE NEEDED] Пользователь ${userId} имеет полный доступ, проверяем глобальный кэш`);
            
            // ЗАГРУЖАЕМ ГЛОБАЛЬНЫЙ ПОЛНЫЙ КЭШ В ФОНЕ
            setTimeout(async () => {
                try {
                    await cacheManager.lazyLoadGlobalFullCache(db, userId);
                } catch (error) {
                    console.error(`❌ [GLOBAL FULL CACHE ERROR] Ошибка загрузки для ${userId}:`, error);
                }
            }, 1000);
        }
        
        return cacheType;
    };
    
// ===================== ФУНКЦИЯ ПРОВЕРКИ ПОЛНОГО ДОСТУПА =====================
const checkFullAccess = async (ctx, forceRefresh = false) => {
    const userId = ctx.from.id;
    
    // Используем сессионное кэширование
    if (!forceRefresh && ctx.session?.fullAccessCache) {
        const accessCache = ctx.session.fullAccessCache;
        const accessAge = Date.now() - accessCache.timestamp;
        
        // Кэш действителен 10 минут для канала, 60 минут для подписки
        if (accessAge < 10 * 60 * 1000) {
            console.log(`✅ [FULL ACCESS CACHE] Используем кэш для ${userId}: ${accessCache.value}`);
            return accessCache.value;
        }
    }
    
    try {
        // 🔥 КРИТИЧЕСКИЙ ФИКС: ПРОВЕРЯЕМ ОБА УСЛОВИЯ
        const [hasSubscription, hasChannelSubscription] = await Promise.all([
            checkSubscription(userId),
            checkChannelSubscription(ctx)
        ]);
        
        const hasFullAccess = hasSubscription && hasChannelSubscription;
        
        console.log(`📊 [FULL ACCESS] ${userId}: подписка=${hasSubscription}, канал=${hasChannelSubscription}, полный доступ=${hasFullAccess}`);
        
        // Сохраняем в сессионный кэш
        if (!ctx.session) ctx.session = {};
        ctx.session.fullAccessCache = {
            value: hasFullAccess,
            timestamp: Date.now(),
            subscription: hasSubscription,
            channel: hasChannelSubscription
        };
        
        // 🔥 ЕСЛИ ПОЛЬЗОВАТЕЛЬ С ПОЛНЫМ ДОСТУПОМ, ПРОВЕРЯЕМ ГЛОБАЛЬНЫЙ ПОЛНЫЙ КЭШ
        if (hasFullAccess && !cacheManager.isGlobalFullCacheLoaded()) {
            console.log(`🚀 [GLOBAL FULL CACHE NEEDED] Пользователь ${userId} имеет полный доступ, проверяем глобальный кэш`);
            
            // ЗАГРУЖАЕМ ГЛОБАЛЬНЫЙ ПОЛНЫЙ КЭШ В ФОНЕ
            setTimeout(async () => {
                try {
                    await cacheManager.lazyLoadGlobalFullCache(db, userId);
                } catch (error) {
                    console.error(`❌ [GLOBAL FULL CACHE ERROR] Ошибка загрузки для ${userId}:`, error);
                }
            }, 1000);
        }
        
        return hasFullAccess;
        
    } catch (error) {
        console.error(`❌ [FULL ACCESS ERROR] Ошибка для ${userId}:`, error);
        
        // При ошибке - демо
        if (!ctx.session) ctx.session = {};
        ctx.session.fullAccessCache = {
            value: false,
            timestamp: Date.now(),
            error: error.message
        };
        
        return false;
    }
};
    
    // ===================== СИСТЕМА УПРАВЛЕНИЯ СООБЩЕНИЯМИ =====================
    const chatStorage = {
        messages: new Map(),
        mainMenu: new Map(),
        userState: new Map(),
        messageTimestamps: new Map(),
        countryKeyboard: new Map(),
        cityKeyboard: new Map(),
    };

    setInterval(() => {
        const now = Date.now();
        let cleanedCount = 0;
        
        chatStorage.messages.forEach((messages, chatId) => {
            messages.forEach(messageId => {
                if (now - (chatStorage.messageTimestamps.get(messageId) || 0) > SCALING_CONFIG.PERFORMANCE.MESSAGE_TTL) {
                    messages.delete(messageId);
                    chatStorage.messageTimestamps.delete(messageId);
                    cleanedCount++;
                }
            });
            
            if (messages.size === 0) {
                chatStorage.messages.delete(chatId);
            }
        });
        
        if (cleanedCount > 0) {
            console.log(`🧹 [CLEANUP] Очищено ${cleanedCount} сообщений`);
        }
    }, 3600000);

    // ===================== ОПТИМИЗИРОВАННАЯ ФУНКЦИЯ ПОЛУЧЕНИЯ СТРАН =====================
    const getUniqueCountries = async (isDemo = false) => {
        try {
            console.log(`🔍 [COUNTRIES] Запрос списка стран (демо: ${isDemo})`);
            
            // 🔥 ВАЖНО: ИСПОЛЬЗУЕМ ГЛОБАЛЬНЫЙ КЭШ
            const cachedCountries = cacheManager.getGlobalCountries(isDemo);
            
            if (cachedCountries && cachedCountries.length > 0) {
                console.log(`✅ [COUNTRIES] Страны из глобального кэша: ${cachedCountries.length}`);
                readingStats.addRead('countries', null, 1, 'cache');
                return cachedCountries;
            }
            
            // 🔥 ЕСЛИ ГЛОБАЛЬНЫЙ КЭШ ПУСТ, СОЗДАЕМ МИНИМАЛЬНЫЙ КЭШ НА ЛЕТУ
            console.log(`⚠️ [COUNTRIES] Нет стран в глобальном кэше, создаем минимальный кэш из популярных стран`);
            
            const popularCountries = POPULAR_COUNTRIES.map(c => c.flag + ' ' + c.name);
            
            console.log(`✅ [COUNTRIES] Создан минимальный кэш из ${popularCountries.length} популярных стран`);
            readingStats.addRead('countries', null, 1, 'generated');
            
            return popularCountries;
            
        } catch (error) {
            console.error("❌ [COUNTRIES] Ошибка загрузки стран:", error);
            return POPULAR_COUNTRIES.map(c => c.flag + ' ' + c.name);
        }
    };

    // ===================== ОПТИМИЗИРОВАННАЯ ФУНКЦИЯ ПОЛУЧЕНИЯ ГОРОДОВ =====================
    const getUniqueCitiesForCountry = async (country, isDemo = false) => {
        try {
            console.log(`🔍 [CITIES] Запрос городов для ${country} (демо: ${isDemo})`);
            
            // 🔥 ВАЖНО: ИСПОЛЬЗУЕМ ГЛОБАЛЬНЫЙ КЭШ
            const cachedCities = cacheManager.getGlobalCities(country, isDemo);
            
            if (cachedCities && cachedCities.length > 0) {
                console.log(`✅ [CITIES] Города из глобального кэша для ${country}: ${cachedCities.length}`);
                readingStats.addRead('cities', null, 1, 'cache');
                return cachedCities;
            }
            
            console.log(`⚠️ [CITIES] Нет городов в глобальном кэше для ${country}! Возвращаем пустой массив`);
            readingStats.addRead('cities', null, 1, 'empty');
            
            // Если кэш пуст, возвращаем пустой массив
            return [];
            
        } catch (error) {
            console.error(`❌ [CITIES] Ошибка загрузки городов для ${country}:`, error);
            return [];
        }
    };

    const formatCountryWithFlag = (countryName) => {
        return countryName;
    };

    // ===================== СИСТЕМА ПАГИНАЦИИ =====================
    const createEnhancedPaginationKeyboard = (currentPage, totalPages, filterKey, currentFilters = {}, isDemo = false) => {
        const keyboard = [];
        
        if (currentFilters.country || currentFilters.city || currentFilters.ageRange) {
            let filtersText = "";
            const filters = [];
            if (currentFilters.country) filters.push(currentFilters.country);
            if (currentFilters.city) filters.push(currentFilters.city);
            if (currentFilters.ageRange) filters.push(currentFilters.ageRange.label);
            filtersText += filters.join(", ");
            
            keyboard.push([{ text: filtersText, callback_data: "filters_info" }]);
        }
        
        const navRow = [];
        if (currentPage > 0) {
            navRow.push({ text: "⏪", callback_data: `page_first_${currentPage}` });
            navRow.push({ text: "◀️", callback_data: `page_prev_${currentPage}` });
        }
        
        navRow.push({ text: `${currentPage + 1}/${totalPages}`, callback_data: "page_info" });
        
        if (currentPage < totalPages - 1) {
            navRow.push({ text: "▶️", callback_data: `page_next_${currentPage}` });
            navRow.push({ text: "⏩", callback_data: `page_last_${currentPage}` });
        }
        
        keyboard.push(navRow);

        if (totalPages > 10) {
            const jumpRow = [];
            const totalProfiles = totalPages * SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE;
            
            PAGINATION_JUMP_SECTIONS.forEach(section => {
                if (section.start < totalProfiles) {
                    const sectionPage = Math.floor(section.start / SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE);
                    if (sectionPage < totalPages) {
                        jumpRow.push({ text: section.label, callback_data: `page_${sectionPage}_${currentPage}` });
                    }
                }
            });
            
            if (jumpRow.length > 0) keyboard.push(jumpRow);
        }

        if (totalPages > 1) {
            const quickPagesRow = [];
            const pagesToShow = Math.min(5, totalPages);
            let startPage = Math.max(0, currentPage - Math.floor(pagesToShow / 2));
            
            if (startPage + pagesToShow > totalPages) startPage = Math.max(0, totalPages - pagesToShow);

            for (let i = 0; i < pagesToShow; i++) {
                const pageNum = startPage + i;
                if (pageNum >= 0 && pageNum < totalPages) {
                    quickPagesRow.push({
                        text: pageNum === currentPage ? `• ${pageNum + 1} •` : `${pageNum + 1}`,
                        callback_data: `page_${pageNum}_${currentPage}`,
                    });
                }
            }
            
            if (quickPagesRow.length > 0) keyboard.push(quickPagesRow);
        }

        keyboard.push([
            { text: "📝 СОЗДАТЬ АНКЕТУ", web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } }
        ]);

        if (isDemo) {
            keyboard.push([
                { text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }
            ]);
        }

        return keyboard;
    };

    // ===================== ОПТИМИЗИРОВАННАЯ ФУНКЦИЯ ПОЛУЧЕНИЯ ПРОФИЛЕЙ =====================
    const getProfilesPage = async (page = 0, searchCountry = null, ageRange = null, searchCity = null, isDemo = false) => {
        try {
            console.log(`🔍 [PROFILES PAGE] Запрос страницы ${page}, демо=${isDemo}, страна=${searchCountry}, город=${searchCity}`);
            
            // 🔥 ВАЖНО: ИСПОЛЬЗУЕМ ГЛОБАЛЬНЫЙ КЭШ
            let allProfiles = cacheManager.getGlobalProfiles(isDemo);
            
            if (!allProfiles || allProfiles.length === 0) {
                console.log(`❌ [PROFILES PAGE] Нет профилей в глобальном кэше (демо: ${isDemo})`);
                
                // ЕСЛИ ПОЛНЫЙ КЭШ НЕ ЗАГРУЖЕН, НО ПОЛЬЗОВАТЕЛЬ С ПОЛНЫМ ДОСТУПОМ
                if (!isDemo && !cacheManager.isGlobalFullCacheLoaded()) {
                    console.log(`⚠️ [PROFILES PAGE] Пользователь с полным доступом, но глобальный полный кэш не загружен`);
                    
                    // Показываем сообщение о загрузке
                    return [];
                }
                
                // ЕСЛИ ДЕМО-КЭШ НЕ ЗАГРУЖЕН
                if (isDemo && !cacheManager.isGlobalDemoCacheLoaded()) {
                    console.log(`💥 [PROFILES PAGE] Глобальный демо-кэш не загружен! Это критическая ошибка!`);
                    return [];
                }
                
                return [];
            }

            const normalizedSearchCity = searchCity ? cacheManager.normalizeCityName(searchCity) : null;
            const normalizedSearchCountry = searchCountry ? cacheManager.normalizeCountryName(searchCountry) : null;
            
            const filterKey = `country:${normalizedSearchCountry || 'all'}:age:${ageRange?.label || 'all'}:city:${normalizedSearchCity || 'all'}`;
            
            // 🔥 ПРОВЕРЯЕМ ГЛОБАЛЬНЫЙ КЭШ ФИЛЬТРОВ
            let filteredProfiles = cacheManager.getGlobalFilter(filterKey, isDemo);
            
            if (!filteredProfiles) {
                console.log(`🔍 [FILTER] Промах глобального кэша фильтров: ${filterKey} (демо: ${isDemo})`);
                
                console.log(`📊 [FILTER] Всего профилей в глобальном кэше: ${allProfiles.length} (демо: ${isDemo})`);
                
                filteredProfiles = await asyncFilterManager.filterProfilesAsync(allProfiles, {
                    country: normalizedSearchCountry,
                    city: normalizedSearchCity,
                    ageRange: ageRange
                }, isDemo);

                console.log(`✅ [FILTER] Отфильтровано: ${filteredProfiles.length} профилей`);
                
                // 🔥 СОХРАНЯЕМ В ГЛОБАЛЬНЫЙ КЭШ ФИЛЬТРОВ
                cacheManager.cacheGlobalFilter(filterKey, filteredProfiles, isDemo);
            }

            const startIndex = page * SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE;
            const endIndex = startIndex + SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE;
            
            const result = filteredProfiles.slice(startIndex, endIndex);
            console.log(`📄 [PAGE] Возвращаем страницу ${page}: ${result.length} профилей`);
            
            return result;

        } catch (error) {
            console.error("❌ [PROFILES PAGE] Ошибка загрузки анкет:", error);
            return [];
        }
    };

    // ===================== МЕНЕДЖЕР СООБЩЕНИЙ =====================
    const messageManager = {
        track: function (chatId, messageId) {
            if (!messageId) return;
            if (!chatStorage.messages.has(chatId)) chatStorage.messages.set(chatId, new Set());
            chatStorage.messages.get(chatId).add(messageId);
            chatStorage.messageTimestamps.set(messageId, Date.now());
        },

        clear: async function (ctx, keepCityKeyboard = false, keepCountryKeyboard = false) {
            const chatId = ctx.chat.id;
            if (!chatStorage.messages.has(chatId)) return;

            const messages = [...chatStorage.messages.get(chatId)];
            const mainMenuId = chatStorage.mainMenu.get(chatId);
            const countryKeyboardId = chatStorage.countryKeyboard.get(chatId);
            const cityKeyboardId = chatStorage.cityKeyboard.get(chatId);

            let deletedCount = 0;

            for (const messageId of messages) {
                const shouldKeep = 
                    (keepCountryKeyboard && messageId === countryKeyboardId) ||
                    (keepCityKeyboard && messageId === cityKeyboardId) ||
                    messageId === mainMenuId;
                    
                if (!shouldKeep) {
                    try {
                        await ctx.telegram.deleteMessage(chatId, messageId);
                        chatStorage.messages.get(chatId).delete(messageId);
                        chatStorage.messageTimestamps.delete(messageId);
                        deletedCount++;
                    } catch (e) {
                        if (e.response?.error_code !== 400) {
                            console.error(`❌ Ошибка удаления ${messageId}:`, e.message);
                        }
                    }
                }
            }

            if (cityKeyboardId && !keepCityKeyboard) {
                try {
                    await ctx.telegram.deleteMessage(chatId, cityKeyboardId);
                    chatStorage.messages.get(chatId)?.delete(cityKeyboardId);
                    chatStorage.messageTimestamps.delete(cityKeyboardId);
                    chatStorage.cityKeyboard.delete(chatId);
                    deletedCount++;
                } catch (e) {
                    if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления клавиатуры городов:", e);
                }
            }

            if (countryKeyboardId && !keepCountryKeyboard) {
                try {
                    await ctx.telegram.deleteMessage(chatId, countryKeyboardId);
                    chatStorage.messages.get(chatId)?.delete(countryKeyboardId);
                    chatStorage.messageTimestamps.delete(countryKeyboardId);
                    chatStorage.countryKeyboard.delete(chatId);
                    deletedCount++;
                } catch (e) {
                    if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления клавиатуры стран:", e);
                }
            }

            chatStorage.userState.delete(ctx.from.id);
            if (deletedCount > 0) console.log(`🧹 [CLEAN] Удалено ${deletedCount} сообщений для чата ${chatId}`);
        },

        sendMainMenu: async function (ctx) {
            return messageQueue.add(async () => {
                const chatId = ctx.chat.id;
                const self = this;

                try {
                    if (chatStorage.mainMenu.has(chatId)) {
                        try {
                            await ctx.telegram.deleteMessage(chatId, chatStorage.mainMenu.get(chatId));
                            chatStorage.messages.get(chatId)?.delete(chatStorage.mainMenu.get(chatId));
                            chatStorage.messageTimestamps.delete(chatStorage.mainMenu.get(chatId));
                        } catch (e) {
                            if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления меню:", e);
                        }
                    }

                    const hasFullAccess = await checkFullAccess(ctx);
                    const menuButtons = [];

                    menuButtons.push([{ text: "🎂 Фильтр по возрасту", callback_data: "filter_by_age" }]);
                    menuButtons.push([{ text: "🌍 Все страны", callback_data: "all_countries_with_check" }]);
                    
                    menuButtons.push([{ text: "📝 СОЗДАТЬ АНКЕТУ", web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } }]);
                    
                    menuButtons.push([{ text: "🧹 Очистить экран", callback_data: "clear_screen" }]);

                    if (!hasFullAccess) {
                        menuButtons.push([{ text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }]);
                    }

                    const menu = await ctx.reply("Главное меню:", { reply_markup: { inline_keyboard: menuButtons } });
                    chatStorage.mainMenu.set(chatId, menu.message_id);
                    self.track(chatId, menu.message_id);

                } catch (error) {
                    console.error("❌ Ошибка отправки меню:", error);
                    throw error;
                }
            });
        },
        
        sendCountriesKeyboard: async function (ctx, isDemo = false, page = 0) {
            return messageQueue.add(async () => {
                const chatId = ctx.chat.id;
                const self = this;

                try {
                    if (chatStorage.countryKeyboard.has(chatId)) {
                        try {
                            await ctx.telegram.deleteMessage(chatId, chatStorage.countryKeyboard.get(chatId));
                            chatStorage.messages.get(chatId)?.delete(chatStorage.countryKeyboard.get(chatId));
                            chatStorage.messageTimestamps.delete(chatStorage.countryKeyboard.get(chatId));
                        } catch (e) {
                            if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления клавиатуры стран:", e);
                        }
                    }

                    const preloaderMsg = await sendPreloader(ctx, 'country');
                    const uniqueCountries = await getUniqueCountries(isDemo);
                    await removePreloader(ctx, preloaderMsg);
                    
                    if (!uniqueCountries || uniqueCountries.length === 0) {
                        const msg = await ctx.reply("❌ Список стран временно недоступен. Попробуйте позже.");
                        self.track(chatId, msg.message_id);
                        return;
                    }
                    
                    const countriesPerPage = 60;
                    const totalPages = Math.ceil(uniqueCountries.length / countriesPerPage);
                    
                    if (page < 0) page = 0;
                    if (page >= totalPages) page = totalPages - 1;
                    
                    const startIndex = page * countriesPerPage;
                    const endIndex = Math.min(startIndex + countriesPerPage, uniqueCountries.length);
                    const pageCountries = uniqueCountries.slice(startIndex, endIndex);

                    const keyboard = [];
                    let row = [];

                    pageCountries.forEach((country, index) => {
                        const countryWithFlag = formatCountryWithFlag(country);
                        row.push({ text: countryWithFlag, callback_data: `country_${country}` });

                        if (row.length === 3 || index === pageCountries.length - 1) {
                            keyboard.push(row);
                            row = [];
                        }
                    });

                    if (totalPages > 1) {
                        const paginationRow = [];
                        
                        if (page > 0) {
                            paginationRow.push({ 
                                text: "◀️ Назад", 
                                callback_data: `countries_page_${page - 1}` 
                            });
                        }
                        
                        paginationRow.push({ 
                            text: `Страна ${page + 1}/${totalPages}`, 
                            callback_data: "countries_page_info" 
                        });
                        
                        if (page < totalPages - 1) {
                            paginationRow.push({ 
                                text: "Вперед ▶️", 
                                callback_data: `countries_page_${page + 1}` 
                            });
                        }
                        
                        if (paginationRow.length > 0) {
                            keyboard.push(paginationRow);
                        }
                        
                        if (totalPages > 5) {
                            const quickPagesRow = [];
                            const pagesToShow = Math.min(5, totalPages);
                            
                            for (let i = 0; i < pagesToShow; i++) {
                                quickPagesRow.push({
                                    text: i === page ? `• ${i + 1} •` : `${i + 1}`,
                                    callback_data: `countries_page_${i}`
                                });
                            }
                            
                            if (page >= pagesToShow) {
                                quickPagesRow.push({ text: "...", callback_data: "countries_page_info" });
                                quickPagesRow.push({
                                    text: `• ${page + 1} •`,
                                    callback_data: `countries_page_${page}`
                                });
                            }
                            
                            if (quickPagesRow.length > 0) {
                                keyboard.push(quickPagesRow);
                            }
                        }
                    }

                    keyboard.push([{ text: "📝 СОЗДАТЬ АНКЕТУ", web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } }]);

                    if (isDemo) {
                        keyboard.push([{ text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }]);
                    }

                    keyboard.push([{ text: "🔙 Назад", callback_data: "back_to_menu" }]);

                    const msgText = isDemo ? 
                        `👀 ДЕМО-РЕЖИМ: Выберите страну (показано по 3 анкеты на город)\n\n📄 Страница ${page + 1} из ${totalPages}\n🌍 Всего стран: ${uniqueCountries.length}\n\n💎 Для полного доступа Вы должны быть подписаны на наш канал @MagicYourClub и оплатить подписку` : 
                        `🌍 Выберите страну (страница ${page + 1} из ${totalPages})\n📊 Всего стран: ${uniqueCountries.length}\n📍 Показано: ${pageCountries.length} стран`;

                    const msg = await ctx.reply(msgText, { 
                        reply_markup: { inline_keyboard: keyboard },
                        parse_mode: "HTML"
                    });
                    
                    chatStorage.countryKeyboard.set(chatId, msg.message_id);
                    self.track(chatId, msg.message_id);
                    
                } catch (error) {
                    console.error("❌ Ошибка отправки клавиатуры стран:", error);
                    throw error;
                }
            });
        },

        sendCitiesKeyboard: async function (ctx, country, isDemo = false) {
            return messageQueue.add(async () => {
                const chatId = ctx.chat.id;
                const self = this;

                try {
                    if (chatStorage.cityKeyboard.has(chatId)) {
                        try {
                            await ctx.telegram.deleteMessage(chatId, chatStorage.cityKeyboard.get(chatId));
                            chatStorage.messages.get(chatId)?.delete(chatStorage.cityKeyboard.get(chatId));
                            chatStorage.messageTimestamps.delete(chatStorage.cityKeyboard.get(chatId));
                        } catch (e) {
                            if (e.response?.error_code !== 400) console.error("❌ Ошибка удаления клавиатуры городов:", e);
                        }
                    }

                    const preloaderMsg = await sendPreloader(ctx, 'profiles');
                    
                    const cities = await getUniqueCitiesForCountry(country, isDemo);
                    
                    await removePreloader(ctx, preloaderMsg);
                    
                    if (!cities || cities.length === 0) {
                        const msg = await ctx.reply(`❌ Для страны "${country}" нет доступных городов`);
                        self.track(chatId, msg.message_id);
                        return;
                    }

                    console.log(`🏙️ [CITIES] Показано городов для ${country}: ${cities.length}`);

                    const citiesPerPage = 50;
                    let currentPage = 0;
                    const totalPages = Math.ceil(cities.length / citiesPerPage);

                    const createCitiesKeyboard = (page) => {
                        const startIndex = page * citiesPerPage;
                        const endIndex = Math.min(startIndex + citiesPerPage, cities.length);
                        const pageCities = cities.slice(startIndex, endIndex);
                        
                        const keyboard = [];
                        let row = [];

                        pageCities.forEach((city, index) => {
                            row.push({ text: city, callback_data: `city_${city}` });
                            if (row.length === 3 || index === pageCities.length - 1) {
                                keyboard.push(row);
                                row = [];
                            }
                        });

                        const paginationRow = [];
                        if (totalPages > 1) {
                            if (page > 0) {
                                paginationRow.push({ 
                                    text: "◀️ Предыдущие", 
                                    callback_data: `cities_page_${country}_${page - 1}` 
                                });
                            }
                            
                            paginationRow.push({ 
                                text: `${page + 1}/${totalPages}`, 
                                callback_data: "cities_page_info" 
                            });
                            
                            if (page < totalPages - 1) {
                                paginationRow.push({ 
                                    text: "Следующие ▶️", 
                                    callback_data: `cities_page_${country}_${page + 1}` 
                                });
                            }
                            
                            if (paginationRow.length > 0) {
                                keyboard.push(paginationRow);
                            }
                        }

                        keyboard.push([{ 
                            text: "📝 СОЗДАТЬ АНКЕТУ", 
                            web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } 
                        }]);

                        if (isDemo) {
                            keyboard.push([{ 
                                text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", 
                                callback_data: "get_full_access" 
                            }]);
                        }

                        keyboard.push([{ 
                            text: "🔙 Назад к странам", 
                            callback_data: "back_to_countries" 
                        }]);

                        return keyboard;
                    };

                    const msgText = isDemo ?
                        `👀 ДЕМО-РЕЖИМ: Выберите город в ${country} (${cities.length} городов, показано по 3 анкеты на город)\n\n💎 Для полного доступа ко всем анкетам получите подписку!` :
                        `🏙️ Выберите город в ${country} (всего ${cities.length} городов):`;

                    const msg = await ctx.reply(msgText, { 
                        reply_markup: { inline_keyboard: createCitiesKeyboard(currentPage) } 
                    });
                    
                    chatStorage.cityKeyboard.set(chatId, msg.message_id);
                    self.track(chatId, msg.message_id);
                    
                } catch (error) {
                    console.error("❌ Ошибка отправки клавиатуры городов:", error);
                    throw error;
                }
            });
        },
    };

    // ===================== ОБРАБОТЧИКИ КОМАНД =====================
    
    bot.command("start", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                console.log(`🚀 Пользователь ${ctx.from.id} запустил бота`);
                
                // 🔥 ОЧИЩАЕМ ЭКРАН
                await messageManager.clear(ctx);
                
                // 🔥 ПОКАЗЫВАЕМ МЕНЮ СРАЗУ
                await messageManager.sendMainMenu(ctx);
                
                // 🔥 ЗАГРУЖАЕМ ГЛОБАЛЬНЫЙ ДЕМО-КЭШ В ФОНЕ (если еще не загружен)
                if (!cacheManager.isGlobalDemoCacheLoaded() && !globalDemoCacheLoading) {
                    console.log(`🔄 [START] Фоновая загрузка глобального демо-кэша...`);
                    
                    setTimeout(async () => {
                        try {
                            await cacheManager.loadGlobalDemoCache(db);
                            console.log(`✅ [START] Глобальный демо-кэш загружен в фоне`);
                        } catch (error) {
                            console.error(`❌ [START] Ошибка загрузки глобального демо-кэша:`, error.message);
                        }
                    }, 1000); // Задержка 1 секунда
                }
                
            } catch (error) {
                console.error("❌ Ошибка команды start:", error);
                await ctx.reply("⚠️ Ошибка запуска. Попробуйте еще раз.");
            }
        });
    });

    bot.command("init_cache", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const userId = ctx.from.id;
                console.log(`🚀 [INIT CACHE] Принудительная инициализация глобального кэша для ${userId}`);
                
                await ctx.reply(`
🔄 <b>ИНИЦИАЛИЗАЦИЯ ГЛОБАЛЬНОГО КЭША</b>

Загружаем глобальный демо-кэш...
⏱️ Это может занять несколько секунд

<em>Пожалуйста, подождите</em>
                `, { parse_mode: "HTML" });
                
                try {
                    // Пытаемся загрузить глобальный демо-кэш
                    await cacheManager.loadGlobalDemoCache(db);
                    
                    await ctx.reply(`
✅ <b>ГЛОБАЛЬНЫЙ КЭШ УСПЕШНО ЗАГРУЖЕН!</b>

• 📊 Анкеты: загружены в глобальный кэш
• 🌍 Страны: доступны для всех пользователей
• 📍 Города: доступны для всех пользователей

Теперь все пользователи могут использовать бота полноценно!
                    `, { parse_mode: "HTML" });
                    
                } catch (error) {
                    console.error(`❌ [INIT CACHE] Ошибка:`, error.message);
                    
                    await ctx.reply(`
⚠️ <b>НЕ УДАЛОСЬ ЗАГРУЗИТЬ ГЛОБАЛЬНЫЙ КЭШ</b>

<b>Что делать:</b>
1. Проверьте подключение к интернету
2. Попробуйте через 5 минут
3. Используйте команду /start

Бот работает в ограниченном режиме.
                    `, { parse_mode: "HTML" });
                }
                
            } catch (error) {
                console.error("❌ Ошибка команды init_cache:", error);
                await ctx.reply("❌ Ошибка инициализации глобального кэша");
            }
        });
    });

    bot.action("all_countries_with_check", async (ctx) => {
        const userId = ctx.from.id;
        console.log(`🌍 [COUNTRIES] Пользователь ${userId} запросил страны`);
        
        // 🔥 ДОБАВЛЯЕМ ПРЕЛОАДЕР СРАЗУ
        const accessPreloader = await sendPreloader(ctx, 'access', "🔍 ПРОВЕРКА ДОСТУПА");
        
        // 🔥 БЫСТРАЯ ПРОВЕРКА БЛОКИРОВКИ
        if (!acquireUserLock(userId, 2000)) {
            try {
                await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
                // Удаляем прелоадер
                if (accessPreloader) {
                    try {
                        await ctx.telegram.deleteMessage(ctx.chat.id, accessPreloader.message_id);
                    } catch (e) {}
                }
            } catch (e) {}
            return;
        }
        
        try {
            await ctx.answerCbQuery("🌍 Загружаем страны...");
            
            // Очищаем предыдущие сообщения
            await messageManager.clear(ctx);
            
            // 🔥 🔥 🔥 ЭТАП 1: ВСЕГДА ПРОВЕРЯЕМ ДОСТУП ЗАНОВО
            console.log(`🔄 [FORCE CHECK] Принудительная проверка для ${userId}`);
            
            // Очищаем ВСЕ кэши
            subscriptionCache.del(`subscription:${userId}`);
            channelSubscriptionCache.del(`channel:${userId}`);
            userCacheStatus.del(`cache_status:${userId}`);
            
            // 🔥 ПРОВЕРЯЕМ ПОДПИСКУ И КАНАЛ ЗАНОВО
            console.log(`🔍 [ACCESS CHECK] Проверка доступа для ${userId}`);
            
            const hasSubscription = await checkSubscription(userId);
            const hasChannelSubscription = await checkChannelSubscription(ctx);
            const hasFullAccess = hasSubscription && hasChannelSubscription;
            
            console.log(`✅ [ACCESS RESULT] ${userId}: подписка=${hasSubscription}, канал=${hasChannelSubscription}, полный доступ=${hasFullAccess}`);
            
            // Определяем тип кэша
            const cacheType = hasFullAccess ? 'full' : 'demo';
            const isDemo = cacheType === 'demo';
            
            // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР
            if (accessPreloader) {
                try {
                    await ctx.telegram.deleteMessage(ctx.chat.id, accessPreloader.message_id);
                } catch (e) {
                    console.log("Не удалось удалить прелоадер:", e.message);
                }
            }
            
            // 🔥 🔥 🔥 ЭТАП 2: ЕСЛИ ДЕМО - ПОКАЗЫВАЕМ ИНФОРМАЦИЮ
            if (isDemo) {
                if (hasSubscription && !hasChannelSubscription) {
                    // ЕСТЬ ПОДПИСКА, НЕТ КАНАЛА
                    await ctx.reply(`
🎯 <b>ВАШ СТАТУС</b>

✅ Подписка: <b>АКТИВНА</b>
❌ Канал: <b>НЕ ПОДПИСАН</b>

<em>Вы в <b>ДЕМО-РЕЖИМЕ</b> (3 анкеты на город)</em>

📢 Для полного доступа подпишитесь на @MagicYourClub
🔄 После подписки нажмите "✅ Я ПОДПИСАЛСЯ НА КАНАЛ"
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
                                [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }],
                                [{ text: "🌍 ВСЕ СТРАНЫ (демо)", callback_data: "all_countries_with_check" }]
                            ]
                        }
                    });
                } else if (!hasSubscription && hasChannelSubscription) {
                    // ЕСТЬ КАНАЛ, НЕТ ПОДПИСКИ
                    await ctx.reply(`
⚠️ <b>ДЕМО-РЕЖИМ</b>

✅ Канал: <b>ПОДПИСАН</b>
❌ Подписка: <b>НЕТ</b>

💎 Для полного доступа купите подписку

<em>В демо-режиме вы видите по 3 анкеты на каждый город</em>
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }],
                                [{ text: "🌍 ВСЕ СТРАНЫ (демо)", callback_data: "all_countries_with_check" }]
                            ]
                        }
                    });
                } else {
                    // НИЧЕГО НЕТ
                    await ctx.reply(`
👀 <b>ДЕМО-РЕЖИМ</b>

Для полного доступа:
1. 💎 Купите подписку
2. 📢 Подпишитесь на @MagicYourClub

<em>В демо-режиме вы видите по 3 анкеты на каждый город</em>
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }],
                                [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }],
                                [{ text: "🌍 ВСЕ СТРАНЫ (демо)", callback_data: "all_countries_with_check" }]
                            ]
                        }
                    });
                }
                
                // 🔥 ПОКАЗЫВАЕМ СТРАНЫ В ДЕМО-РЕЖИМЕ
                await messageManager.sendCountriesKeyboard(ctx, true, 0);
                return;
            }
            
            // 🔥 🔥 🔥 ЭТАП 3: ЕСЛИ ПОЛНЫЙ ДОСТУП
            console.log(`🎉 [FULL ACCESS] Пользователь ${userId} получил полный доступ`);
            
            // 🔥 ЭТАП 3.1: ЕСЛИ ГЛОБАЛЬНЫЙ ПОЛНЫЙ КЭШ НЕ ЗАГРУЖЕН
            if (!cacheManager.isGlobalFullCacheLoaded()) {
                console.log(`🚀 [GLOBAL FULL CACHE NEEDED] Глобальный полный кэш не загружен, начинаем загрузку`);
                
                // Показываем сообщение о начале загрузки
                const loadingMsg = await ctx.reply(`
🔄 <b>ЗАГРУЗКА ГЛОБАЛЬНОГО ДОСТУПА</b>

🎉 У вас есть полный доступ!
📊 Начинаем загрузку 70,000+ анкет в глобальный кэш...

⏱️ <i>Это займет 2-3 минуты</i>
📦 <i>Загружаем пачками по 5000 анкет</i>
✨ <i>После загрузки доступно для всех пользователей</i>

<em>Не закрывайте чат, загрузка продолжается в фоне</em>
                `, { parse_mode: "HTML" });
                
                // 🔥 ЗАПУСКАЕМ ЗАГРУЗКУ ГЛОБАЛЬНОГО КЭША
                cacheManager.lazyLoadGlobalFullCache(db, userId)
                    .then((success) => {
                        if (success) {
                            console.log(`✅ [BACKGROUND GLOBAL CACHE] Глобальный полный кэш загружен для ${userId}`);
                            // Удаляем сообщение о загрузке
                            try {
                                ctx.telegram.deleteMessage(ctx.chat.id, loadingMsg.message_id);
                                // Показываем успешное сообщение
                                ctx.reply(`
✅ <b>ГЛОБАЛЬНЫЙ КЭШ ЗАГРУЖЕН!</b>

🎉 Теперь доступны все 70,000+ анкет для всех пользователей!
• 👤 Все контакты видны
• 📞 Телефоны, Telegram, WhatsApp
• ⚡ Максимальная скорость работы
                                `, { parse_mode: "HTML" });
                            } catch (e) {}
                        } else {
                            console.log(`❌ [BACKGROUND GLOBAL CACHE ERROR] ${userId}: Не удалось загрузить`);
                            // Показываем сообщение об ошибке
                            try {
                                ctx.telegram.editMessageText(
                                    ctx.chat.id,
                                    loadingMsg.message_id,
                                    null,
                                    `⚠️ <b>ОШИБКА ЗАГРУЗКИ ГЛОБАЛЬНОГО КЭША</b>\n\nНе удалось загрузить глобальный кэш.\nИспользуем ограниченный доступ.\n\n<em>Попробуйте через несколько минут</em>`,
                                    { parse_mode: "HTML" }
                                );
                            } catch (e) {}
                        }
                    })
                    .catch(error => {
                        console.error(`❌ [BACKGROUND GLOBAL CACHE ERROR] ${userId}:`, error.message);
                    });
                
                // 🔥 ПОКАЗЫВАЕМ ПЕРВУЮ ПАЧКУ СТРАН (из глобального демо-кэша пока идет загрузка)
                if (cacheManager.isGlobalDemoCacheLoaded()) {
                    console.log(`📊 [TEMPORARY] Показываем демо-страны пока загружается глобальный полный кэш`);
                    await messageManager.sendCountriesKeyboard(ctx, false, 0);
                } else {
                    // Если даже глобальный демо-кэша нет, показываем сообщение и ждем
                    await ctx.reply(`
⏳ <b>ИНИЦИАЛИЗАЦИЯ ГЛОБАЛЬНОГО КЭША</b>

📊 Загружаем первые анкеты...
⏱️ Подождите 30 секунд

<em>Система готовится к работе</em>
                    `, { parse_mode: "HTML" });
                    
                    // Ждем 5 секунд и показываем страны
                    setTimeout(async () => {
                        try {
                            await messageManager.sendCountriesKeyboard(ctx, false, 0);
                        } catch (e) {
                            console.error("Ошибка показа стран:", e);
                        }
                    }, 5000);
                }
                
                return;
            }
            
            // 🔥 ЭТАП 3.2: ЕСЛИ ГЛОБАЛЬНЫЙ ПОЛНЫЙ КЭШ УЖЕ ЗАГРУЖЕН
            console.log(`✅ [GLOBAL FULL CACHE READY] Глобальный полный кэш уже загружен для ${userId}`);
            
            // Показываем сообщение об успешном доступе
            await ctx.reply(`
✅ <b>ПОЛНЫЙ ДОСТУП АКТИВИРОВАН!</b>

🎉 Теперь у вас доступ ко всем 70,000+ анкетам
• 👤 Все контакты видны
• 📞 Телефоны, Telegram, WhatsApp
• ⚡ Максимальная скорость

<code>Выберите страну для начала поиска</code>
            `, {
                parse_mode: "HTML",
                reply_markup: {
                    inline_keyboard: [
                        [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
                        [{ text: "🎂 Фильтр по возрасту", callback_data: "filter_by_age" }]
                    ]
                }
            });
            
            // 🔥 ПОКАЗЫВАЕМ СТРАНЫ
            await messageManager.sendCountriesKeyboard(ctx, false, 0);
            
        } catch (error) {
            console.error("❌ [COUNTRIES CRITICAL] Критическая ошибка:", error);
            
            // УДАЛЯЕМ ПРЕЛОАДЕР ПРИ ОШИБКЕ
            if (accessPreloader) {
                try {
                    await ctx.telegram.deleteMessage(ctx.chat.id, accessPreloader.message_id);
                } catch (e) {}
            }
            
            try {
                await ctx.answerCbQuery("❌ Ошибка загрузки");
                
                // ПРИ ОШИБКЕ - ДЕМО
                await ctx.reply(`
❌ <b>КРИТИЧЕСКАЯ ОШИБКА</b>

Не удалось загрузить список стран.

<em>Используем демо-режим:</em>
• 👀 3 анкеты на каждый город
• 🚫 Контакты скрыты

Попробуйте через несколько минут
                `, { parse_mode: "HTML" });
                
                await messageManager.sendCountriesKeyboard(ctx, true, 0);
            } catch (e) {
                console.error("Не удалось отправить сообщение об ошибке:", e);
            }
            
        } finally {
            releaseUserLock(userId);
        }
    });

    bot.action("get_full_access", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                await ctx.answerCbQuery("💎 Переходим к оплате...");
                
                const keyboard = {
                    inline_keyboard: [
                        [
                            { text: "💎 Купить подписку", callback_data: "choose_payment_method" }
                        ],
                        [
                            { text: "📢 Подписаться на канал", url: "https://t.me/MagicYourClub" }
                        ],
                        [
                            { text: "🔙 Назад", callback_data: "back_to_menu" }
                        ]
                    ]
                };

                await ctx.reply(`
💎 <b>ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП</b>

Для получения полного доступа ко всем анкетам необходимо:

✅ <b>1. Активная подписка</b>
   • Доступ ко всем анкетам
   • Все контакты профилей
   • Полная функциональность

✅ <b>2. Подписка на канал @MagicYourClub</b>
   • Новые анкеты и обновления
   • Эксклюзивный контент
   • Специальные предложения

<b>После оплаты подписки и подписки на канал вы получите:</b>
• 🔓 Полный доступ ко всем анкетам
• 📞 Все контакты профилей  
• 🌍 Неограниченный поиск по странам и городам
• ⚡ Максимальную скорость работы

Нажмите "Купить подписку" чтобы начать!
                `, {
                    parse_mode: "HTML",
                    reply_markup: keyboard
                });
                
            } catch (error) {
                console.error("❌ Ошибка обработки получения доступа:", error);
            }
        });
    });

    bot.command("cache_debug", async (ctx) => {
        await messageQueue.add(async () => {
            const userId = ctx.from.id;
            const cacheType = cacheManager.getUserCacheStatus(userId);
            const hasFullAccess = await checkFullAccess(ctx, false);
            const sessionType = ctx.session?.cacheSession?.type || 'нет';
            
            const cacheStats = cacheManager.getGlobalCacheStats();
            
            const message = `
🔧 <b>ДЕБАГ ГЛОБАЛЬНОГО КЭША</b>

👤 <b>Пользователь:</b> ${userId}
💾 <b>Тип кэша:</b> ${cacheType || 'не установлен'}
🔓 <b>Полный доступ:</b> ${hasFullAccess ? '✅' : '❌'}
💼 <b>Сессия:</b> ${sessionType}

📊 <b>ГЛОБАЛЬНЫЙ КЭШ (общий для всех):</b>
• Полный кэш: ${cacheStats.fullCache.loaded ? `✅ ${cacheStats.fullCache.profilesCount} анкет, ${cacheStats.fullCache.countriesCount} стран` : '❌ Не загружен'}
• Демо кэш: ${cacheStats.demoCache.loaded ? `✅ ${cacheStats.demoCache.profilesCount} анкет, ${cacheStats.demoCache.countriesCount} стран` : '❌ Не загружен'}

🔍 <b>КЭШИ ФИЛЬТРОВ:</b>
• Полные фильтры: ${cacheStats.filterCacheKeys}
• Демо фильтры: ${cacheStats.demoFilterCacheKeys}

👥 <b>ИНДИВИДУАЛЬНЫЕ КЭШИ:</b>
• Подписки: ${cacheStats.subscriptionCacheCount}
• Каналы: ${cacheStats.channelCacheCount}
• Статусы: ${cacheStats.userStatusCacheCount}

📖 <b>Статистика чтений:</b>
• Всего чтений: ${readingStats.totalReads}
• Firestore: ${readingStats.operations.firestoreReads}
• Кэш эффективность: ${readingStats.getStats().cacheEfficiency}
            `;
            
            await ctx.reply(message, { parse_mode: "HTML" });
        });
    });

    bot.command("force_check", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const userId = ctx.from.id;
                
                // ОЧИЩАЕМ ВСЕ ИНДИВИДУАЛЬНЫЕ КЭШИ
                channelSubscriptionCache.del(`channel:${userId}`);
                subscriptionCache.del(`subscription:${userId}`);
                userCacheStatus.del(`cache_status:${userId}`);
                
                if (ctx.session) {
                    delete ctx.session.cacheType;
                    delete ctx.session.hasFullAccess;
                    delete ctx.session.fullAccessChecked;
                    delete ctx.session.fullAccessCheckTime;
                }
                
                // ПРОВЕРЯЕМ ЗАНОВО с forceRefresh
                const [hasSubscription, hasChannel] = await Promise.all([
                    checkSubscription(userId),
                    checkChannelSubscription(ctx)
                ]);
                
                const accessType = hasSubscription && hasChannel ? 'full' : 'demo';
                cacheManager.setUserCacheStatus(userId, accessType);
                
                // Сохраняем в сессию
                if (!ctx.session) ctx.session = {};
                ctx.session.hasFullAccess = hasSubscription && hasChannel;
                ctx.session.fullAccessChecked = true;
                ctx.session.fullAccessCheckTime = Date.now();
                
                await ctx.reply(`
🔄 <b>ПРИНУДИТЕЛЬНАЯ ПРОВЕРКА</b>

✅ Кэши очищены
✅ Проверка выполнена

<b>Результат:</b>
• 💎 Подписка: ${hasSubscription ? '✅ АКТИВНА' : '❌ НЕТ'}
• 📢 Канал: ${hasChannel ? '✅ ПОДПИСАН' : '❌ НЕТ'}
• 👤 Режим: ${accessType === 'full' ? '🔓 ПОЛНЫЙ' : '👀 ДЕМО'}

${!hasChannel ? '⚠️ <b>Вы не подписаны на канал @MagicYourClub</b>' : ''}
                `, { parse_mode: "HTML" });
                
            } catch (error) {
                console.error("❌ Ошибка force_check:", error);
                await ctx.reply("❌ Ошибка проверки");
            }
        });
    });

    bot.command("load_global_full_cache", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const userId = ctx.from.id;
                await ctx.answerCbQuery("🔄 Загружаем глобальный полный кэш...");
                
                console.log(`🚀 [LOAD GLOBAL FULL CACHE] Команда от пользователя ${userId}`);
                
                const hasFullAccess = await checkFullAccess(ctx, true);
                
                if (!hasFullAccess) {
                    await ctx.reply(`
❌ <b>НЕТ ДОСТУПА К КОМАНДЕ</b>

Эта команда доступна только пользователям с полным доступом.

<b>Ваш статус:</b>
💎 Подписка: ${hasFullAccess ? '✅' : '❌'}
📢 Канал: ${hasFullAccess ? '✅' : '❌'}

Используйте /refresh_access для проверки доступа
                    `, { parse_mode: "HTML" });
                    return;
                }
                
                // Показываем прогресс
                const progressMsg = await ctx.reply(`
🔄 <b>ЗАГРУЗКА ГЛОБАЛЬНОГО ПОЛНОГО КЭША</b>

⏳ Пожалуйста, подождите...
📊 Загружаем 70,000+ анкет в глобальный кэш
⏱️ Это может занять 2-3 минуты

<em>После загрузки кэш будет доступен для всех пользователей</em>
                `, { parse_mode: "HTML" });
                
                // Загружаем глобальный полный кэш
                try {
                    const success = await cacheManager.loadGlobalFullCache(db);
                    
                    if (success) {
                        await ctx.telegram.deleteMessage(ctx.chat.id, progressMsg.message_id);
                        
                        const cacheStats = cacheManager.getGlobalCacheStats();
                        
                        await ctx.reply(`
✅ <b>ГЛОБАЛЬНЫЙ КЭШ ЗАГРУЖЕН!</b>

📊 <b>Статистика:</b>
• Анкет загружено: ${cacheStats.fullCache.profilesCount}
• Стран в кэше: ${cacheStats.fullCache.countriesCount}
• Доступно для: <b>ВСЕХ ПОЛЬЗОВАТЕЛЕЙ</b>

🎉 <b>Теперь доступны все функции:</b>
• 🌍 Все 70,000+ анкет
• 📍 Полный список стран и городов
• 👤 Все контакты профилей
• ⚡ Максимальная скорость работы

<code>Кэш обновляется раз в 7 дней автоматически</code>
                        `, {
                            parse_mode: "HTML",
                            reply_markup: {
                                inline_keyboard: [
                                    [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
                                    [{ text: "📊 Статистика", callback_data: "show_cache_stats" }]
                                ]
                            }
                        });
                    } else {
                        await ctx.telegram.deleteMessage(ctx.chat.id, progressMsg.message_id);
                        throw new Error("Не удалось загрузить глобальный кэш");
                    }
                    
                } catch (error) {
                    await ctx.telegram.deleteMessage(ctx.chat.id, progressMsg.message_id);
                    throw error;
                }
                
            } catch (error) {
                console.error("❌ Ошибка загрузки глобального кэша:", error);
                await ctx.reply(`
❌ <b>ОШИБКА ЗАГРУЗКИ ГЛОБАЛЬНОГО КЭША</b>

Не удалось загрузить глобальный полный кэш.

<b>Причина:</b> ${error.message}

Попробуйте:
1. Подождать несколько минут
2. Использовать /refresh_access
3. Написать в поддержку @MagicAdd
                `, { parse_mode: "HTML" });
            }
        });
    });

    bot.command("reset_cache_stats", async (ctx) => {
        await messageQueue.add(async () => {
            const userId = ctx.from.id;
            // ТОЛЬКО ДЛЯ АДМИНОВ
            const ADMINS = [123456789]; // Ваш ID
            
            if (!ADMINS.includes(userId)) {
                return ctx.reply("❌ Эта команда только для администраторов");
            }
            
            readingStats.resetStats();
            
            await ctx.reply("✅ Статистика чтений сброшена");
        });
    });

    // Действие для показа статистики кэша
    bot.action("show_cache_stats", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const cacheStats = cacheManager.getGlobalCacheStats();
                const readStats = readingStats.getStats();
                
                const statsMessage = `
📊 <b>СТАТИСТИКА ГЛОБАЛЬНОГО КЭША</b>

🔹 <b>Глобальный демо-кэш:</b>
• Анкет: ${cacheStats.demoCache.profilesCount}
• Стран: ${cacheStats.demoCache.countriesCount}
• Загружен: ${cacheStats.demoCache.loaded ? '✅' : '❌'}

🔹 <b>Глобальный полный кэш:</b>
• Анкет: ${cacheStats.fullCache.profilesCount}
• Стран: ${cacheStats.fullCache.countriesCount}
• Загружен: ${cacheStats.fullCache.loaded ? '✅' : '❌'}

🔹 <b>Кэши фильтров:</b>
• Полные фильтры: ${cacheStats.filterCacheKeys}
• Демо фильтры: ${cacheStats.demoFilterCacheKeys}

🔹 <b>Статистика чтений:</b>
• Всего чтений: ${readStats.totalReads}
• Firestore чтений: ${readStats.firestoreReads}
• Кэш попадания: ${readStats.operations.cacheHits}
• Кэш промахи: ${readStats.operations.cacheMisses}
• Эффективность: ${readStats.cacheEfficiency}

🔹 <b>Пользователи:</b>
• Уникальные: ${readStats.uniqueUsers}
• Операций на пользователя: ${readStats.readsPerUser.toFixed(2)}

<code>Глобальный кэш обновляется раз в 7 дней</code>
                `;
                
                const keyboard = {
                    inline_keyboard: []
                };
                
                if (!cacheStats.fullCache.loaded) {
                    keyboard.inline_keyboard.push([
                        { text: "🚀 Загрузить глобальный полный кэш", callback_data: "force_load_global_full_cache" }
                    ]);
                }
                
                keyboard.inline_keyboard.push([
                    { text: "🔄 Обновить", callback_data: "show_cache_stats" },
                    { text: "🔙 Назад", callback_data: "back_to_menu" }
                ]);
                
                await ctx.reply(statsMessage, {
                    parse_mode: "HTML",
                    reply_markup: keyboard
                });
                await ctx.answerCbQuery();
                
            } catch (error) {
                console.error("❌ Ошибка показа статистики кэша:", error);
            }
        });
    });

    // Действие для принудительной загрузки глобального полного кэша
    bot.action("force_load_global_full_cache", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                await ctx.answerCbQuery("🚀 Запускаем загрузку...");
                await ctx.reply("Используйте команду /load_global_full_cache для загрузки глобального полного кэша");
            } catch (error) {
                console.error("❌ Ошибка:", error);
            }
        });
    });

    // ОБРАБОТЧИК ПАГИНАЦИИ СТРАН
    bot.action(/^countries_page_(\d+)$/, async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 2000)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            try {
                const page = parseInt(ctx.match[1]);
                
                // ПРОВЕРЯЕМ И ОБНОВЛЯЕМ КЭШ (с сессионным кэшированием)
                const cacheType = await ensureProperCache(ctx);
                const isDemo = cacheType === 'demo';
                
                // Обновляем сообщение с новой страницей
                const uniqueCountries = await getUniqueCountries(isDemo);
                const countriesPerPage = 60;
                const totalPages = Math.ceil(uniqueCountries.length / countriesPerPage);
                
                if (page < 0 || page >= totalPages) {
                    await ctx.answerCbQuery("❌ Неверная страница");
                    return;
                }
                
                const startIndex = page * countriesPerPage;
                const endIndex = Math.min(startIndex + countriesPerPage, uniqueCountries.length);
                const pageCountries = uniqueCountries.slice(startIndex, endIndex);
                
                const keyboard = [];
                let row = [];
                
                pageCountries.forEach((country, index) => {
                    const countryWithFlag = formatCountryWithFlag(country);
                    row.push({ text: countryWithFlag, callback_data: `country_${country}` });
                    
                    if (row.length === 3 || index === pageCountries.length - 1) {
                        keyboard.push(row);
                        row = [];
                    }
                });
                
                if (totalPages > 1) {
                    const paginationRow = [];
                    
                    if (page > 0) {
                        paginationRow.push({ 
                            text: "◀️ Назад", 
                            callback_data: `countries_page_${page - 1}` 
                        });
                    }
                    
                    paginationRow.push({ 
                        text: `Страна ${page + 1}/${totalPages}`, 
                        callback_data: "countries_page_info" 
                    });
                    
                    if (page < totalPages - 1) {
                        paginationRow.push({ 
                            text: "Вперед ▶️", 
                            callback_data: `countries_page_${page + 1}` 
                        });
                    }
                    
                    if (paginationRow.length > 0) {
                        keyboard.push(paginationRow);
                    }
                    
                    if (totalPages > 5) {
                        const quickPagesRow = [];
                        const pagesToShow = Math.min(5, totalPages);
                        
                        for (let i = 0; i < pagesToShow; i++) {
                            quickPagesRow.push({
                                text: i === page ? `• ${i + 1} •` : `${i + 1}`,
                                callback_data: `countries_page_${i}`
                            });
                        }
                        
                        if (page >= pagesToShow) {
                            quickPagesRow.push({ text: "...", callback_data: "countries_page_info" });
                            quickPagesRow.push({
                                text: `• ${page + 1} •`,
                                callback_data: `countries_page_${page}`
                            });
                        }
                        
                        if (quickPagesRow.length > 0) {
                            keyboard.push(quickPagesRow);
                        }
                    }
                }
                
                keyboard.push([{ text: "📝 СОЗДАТЬ АНКЕТУ", web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } }]);
                
                if (isDemo) {
                    keyboard.push([{ text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }]);
                }
                
                keyboard.push([{ text: "🔙 Назад", callback_data: "back_to_menu" }]);
                
                try {
                    await ctx.editMessageText(
                        isDemo ? 
                        `👀 ДЕМО-РЕЖИМ: Выберите страну (показано по 3 анкеты на город)\n\n📄 Страница ${page + 1} из ${totalPages}\n🌍 Всего стран: ${uniqueCountries.length}\n\n💎 Для полного доступа Вы должны быть подписаны на наш канал @MagicYourClub и оплатить подписку` : 
                        `🌍 Выберите страну (страница ${page + 1} из ${totalPages})\n📊 Всего стран: ${uniqueCountries.length}\n📍 Показано: ${pageCountries.length} стран`,
                        {
                            parse_mode: "HTML",
                            reply_markup: { inline_keyboard: keyboard }
                        }
                    );
                    
                    await ctx.answerCbQuery(`📄 Страница ${page + 1} из ${totalPages}`);
                    
                } catch (error) {
                    console.error("❌ Ошибка обновления клавиатуры стран:", error);
                    await ctx.answerCbQuery("❌ Ошибка загрузки");
                }
                
            } catch (error) {
                console.error("❌ Ошибка пагинации стран:", error);
                await ctx.answerCbQuery("❌ Ошибка загрузки");
            } finally {
                releaseUserLock(userId);
            }
        });
    });

    bot.command("countries_stats", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const cacheType = await ensureProperCache(ctx);
                const isDemo = cacheType === 'demo';
                
                const uniqueCountries = await getUniqueCountries(isDemo);
                const totalCountries = uniqueCountries.length;
                
                const removeFlag = (countryName) => {
                    if (!countryName) return countryName;
                    const firstChar = countryName.charAt(0);
                    const isEmojiFlag = firstChar.codePointAt(0) > 127;
                    
                    if (isEmojiFlag && countryName.length > 2) {
                        return countryName.substring(2).trim();
                    }
                    return countryName;
                };
                
                const regions = {
                    europe: ['Россия', 'Украина', 'Беларусь', 'Германия', 'Франция', 'Италия', 'Испания', 'Польша', 'Нидерланды', 'Бельгия', 'Швейцария', 'Австрия', 'Португалия', 'Греция', 'Чехия', 'Швеция', 'Норвегия', 'Финляндия', 'Дания', 'Ирландия', 'Венгрия', 'Румыния', 'Болгария', 'Сербия', 'Хорватия', 'Словакия', 'Словения', 'Литва', 'Латвия', 'Эстония', 'Молдова', 'Албания'],
                    asia: ['Казахстан', 'Турция', 'Китай', 'Япония', 'Южная Корея', 'Индия', 'Таиланд', 'ОАЭ', 'Узбекистан', 'Кыргызстан', 'Таджикистан', 'Туркменистан', 'Грузия', 'Армения', 'Азербайджан', 'Израиль'],
                    america: ['США', 'Канада', 'Мексика', 'Бразилия'],
                    africa: ['ЮАР', 'Египет'],
                    oceania: ['Австралия']
                };
                
                let statsByRegion = {};
                Object.keys(regions).forEach(region => {
                    const count = uniqueCountries.filter(country => {
                        const cleanCountry = removeFlag(country);
                        return regions[region].includes(cleanCountry);
                    }).length;
                    statsByRegion[region] = count;
                });
                
                const topCountries = uniqueCountries.slice(0, 10).map((country, i) => {
                    return `${i + 1}. ${country}`;
                }).join('\n');
                
                const statsMessage = `
🌍 **СТАТИСТИКА СТРАН**

📊 **Общая информация:**
• Всего стран: ${totalCountries}
• Демо-режим: ${isDemo ? '✅' : '❌'}
• Страниц: ${Math.ceil(totalCountries / 60)}

🗺️ **По регионам:**
• Европа: ${statsByRegion.europe || 0}
• Азия: ${statsByRegion.asia || 0}
• Америка: ${statsByRegion.america || 0}
• Африка: ${statsByRegion.africa || 0}
• Океания: ${statsByRegion.oceania || 0}
• Другие: ${totalCountries - Object.values(statsByRegion).reduce((a, b) => a + b, 0)}

📋 **Топ-10 стран:**
${topCountries}

${totalCountries > 10 ? `... и еще ${totalCountries - 10} стран` : ''}
                `;
                
                const msg = await ctx.reply(statsMessage, { parse_mode: "Markdown" });
                messageManager.track(ctx.chat.id, msg.message_id);
                
            } catch (error) {
                console.error("❌ Ошибка команды countries_stats:", error);
                await ctx.reply("❌ Ошибка загрузки статистики стран");
            }
        });
    });

    bot.action("countries_page_info", async (ctx) => {
        await ctx.answerCbQuery("📄 Используйте кнопки для навигации по странам");
    });

    bot.action(/^country_(.+)$/, async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 2500)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            let preloaderMsg = null;
            
            try {
                const country = ctx.match[1];
                
                // 🔥 ДОБАВЛЯЕМ ПРЕЛОАДЕР
                preloaderMsg = await sendPreloader(ctx, 'country', "🌍 ЗАГРУЗКА ГОРОДОВ");
                
                const cacheType = await ensureProperCache(ctx);
                const isDemo = cacheType === 'demo';
                
                ctx.session = ctx.session || {};
                ctx.session.profilesPage = 0;
                ctx.session.filterCountry = country;
                ctx.session.displayCountry = country;
                ctx.session.filterCity = null;
                ctx.session.isDemo = isDemo;

                await messageManager.clear(ctx);
                
                await messageManager.sendCitiesKeyboard(ctx, country, isDemo);
                
                // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР
                if (preloaderMsg) {
                    await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
                }
                
                await ctx.answerCbQuery();
                
            } catch (error) {
                console.error("❌ Ошибка обработки выбора страны:", error);
                
                // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР ПРИ ОШИБКЕ
                if (preloaderMsg) {
                    try {
                        await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
                    } catch (e) {}
                }
                
            } finally {
                releaseUserLock(userId);
            }
        });
    });
    
    bot.action(/^city_(.+)$/, async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 30000)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            let preloaderMsg = null;
            
            try {
                const city = ctx.match[1];
                
                await ctx.answerCbQuery("🔍 Ищем анкеты...");
                
                // 🔥 ДОБАВЛЯЕМ ПРЕЛОАДЕР ДЛЯ ПОИСКА ГОРОДА
                preloaderMsg = await sendPreloader(ctx, 'city', "🔍 ПОИСК АНКЕТ");
                
                const cacheType = await ensureProperCache(ctx);
                const isDemo = cacheType === 'demo';
                
                console.log(`🏙️ [CITY] Пользователь ${userId} выбрал город ${city}, демо=${isDemo}`);
                
                ctx.session = ctx.session || {};
                ctx.session.profilesPage = 0;
                ctx.session.filterCity = city;
                ctx.session.isDemo = isDemo;

                await messageManager.clear(ctx, true, true);
                
                console.log(`🔍 [CITY] Загружаем анкеты для города ${city}...`);
                const profiles = await getProfilesPage(0, ctx.session.filterCountry, ctx.session.ageRange, city, isDemo);

                // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР ПОСЛЕ ЗАГРУЗКИ
                if (preloaderMsg) {
                    await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
                }
                preloaderMsg = null;

                if (!profiles.length) {
                    const msg = await ctx.reply(`❌ Анкет из города "${city}" не найдено`);
                    messageManager.track(ctx.chat.id, msg.message_id);
                    return;
                }

                console.log(`✅ [CITY] Найдено ${profiles.length} анкет для города ${city}`);

                const foundMsg = await ctx.reply(
                    `📍 <b>Город:</b> ${city}\n` +
                    `🌍 <b>Страна:</b> ${ctx.session.filterCountry}\n` +
                    `👀 <b>Режим:</b> ${isDemo ? 'Демо (3 анкеты на город)' : 'Полный доступ'}`,
                    { parse_mode: "HTML" }
                );
                messageManager.track(ctx.chat.id, foundMsg.message_id);

                for (let i = 0; i < profiles.length; i++) {
                    const isLast = i === profiles.length - 1;
                    
                    if (i === 0 && profiles.length > 1) {
                        const progressMsg = await ctx.reply(
                            `📤 <b>Отправляем анкеты...</b>\n` +
                            `📊 <i>Прогресс: 1/${profiles.length}</i>`,
                            { parse_mode: "HTML" }
                        );
                        messageManager.track(ctx.chat.id, progressMsg.message_id);
                    }
                    
                    await sendProfile(ctx, profiles[i], 0, profiles.length, isLast, isDemo);
                    
                    if (!isLast) {
                        await new Promise((resolve) => setTimeout(resolve, 500));
                    }
                }

            } catch (error) {
                console.error("❌ Ошибка обработки выбора города:", error);
                
                // 🔥 УДАЛЯЕМ ПРЕЛОАДЕР ПРИ ОШИБКЕ
                if (preloaderMsg) {
                    try {
                        await ctx.telegram.deleteMessage(ctx.chat.id, preloaderMsg.message_id);
                    } catch (e) {
                        console.log("Не удалось удалить прелоадер при ошибке:", e.message);
                    }
                }
                
                try {
                    await ctx.reply(
                        "❌ <b>Произошла ошибка при загрузке анкет</b>\n\n" +
                        "⚠️ <i>Попробуйте еще раз через несколько секунд</i>",
                        { parse_mode: "HTML" }
                    );
                } catch (e) {
                    console.error("Не удалось отправить сообщение об ошибке:", e);
                }
            } finally {
                releaseUserLock(userId);
            }
        });
    });

    bot.action(/^cities_page_(.+)_(\d+)$/, async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 2000)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            try {
                const [_, country, page] = ctx.match;
                const pageNum = parseInt(page);
                
                const cacheType = await ensureProperCache(ctx);
                const isDemo = cacheType === 'demo';
                
                const cities = await getUniqueCitiesForCountry(country, isDemo);
                
                if (!cities || cities.length === 0) {
                    await ctx.answerCbQuery("❌ Нет доступных городов");
                    return;
                }

                const citiesPerPage = 50;
                const totalPages = Math.ceil(cities.length / citiesPerPage);
                
                if (pageNum < 0 || pageNum >= totalPages) {
                    await ctx.answerCbQuery("❌ Неверная страница");
                    return;
                }

                const keyboard = [];
                let row = [];

                const startIndex = pageNum * citiesPerPage;
                const endIndex = Math.min(startIndex + citiesPerPage, cities.length);
                const pageCities = cities.slice(startIndex, endIndex);

                pageCities.forEach((city, index) => {
                    row.push({ text: city, callback_data: `city_${city}` });
                    if (row.length === 3 || index === pageCities.length - 1) {
                        keyboard.push(row);
                        row = [];
                    }
                });

                const paginationRow = [];
                if (totalPages > 1) {
                    if (pageNum > 0) {
                        paginationRow.push({ 
                            text: "◀️ Предыдущие", 
                            callback_data: `cities_page_${country}_${pageNum - 1}` 
                        });
                    }
                    
                    paginationRow.push({ 
                        text: `${pageNum + 1}/${totalPages}`, 
                        callback_data: "cities_page_info" 
                    });
                    
                    if (pageNum < totalPages - 1) {
                        paginationRow.push({ 
                            text: "Следующие ▶️", 
                            callback_data: `cities_page_${country}_${pageNum + 1}` 
                        });
                    }
                    
                    if (paginationRow.length > 0) {
                        keyboard.push(paginationRow);
                    }
                }

                keyboard.push([{ 
                    text: "📝 СОЗДАТЬ АНКЕТУ", 
                    web_app: { url: "https://bot-vai-web-app.web.app/?tab=catalog" } 
                }]);

                if (isDemo) {
                    keyboard.push([{ 
                        text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", 
                        callback_data: "get_full_access" 
                    }]);
                }

                keyboard.push([{ 
                    text: "🔙 Назад к странам", 
                    callback_data: "back_to_countries" 
                }]);

                try {
                    await ctx.editMessageReplyMarkup({
                        inline_keyboard: keyboard
                    });
                    await ctx.answerCbQuery(`📄 Страница ${pageNum + 1} из ${totalPages}`);
                } catch (error) {
                    console.error("❌ Ошибка обновления клавиатуры городов:", error);
                    await ctx.answerCbQuery("❌ Ошибка обновления");
                }
                
            } catch (error) {
                console.error("❌ Ошибка пагинации городов:", error);
                await ctx.answerCbQuery("❌ Ошибка загрузки");
            } finally {
                releaseUserLock(userId);
            }
        });
    });

    bot.action("cities_page_info", async (ctx) => {
        await ctx.answerCbQuery("📄 Используйте кнопки для навигации по городам");
    });

    bot.action("back_to_countries", async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 2000)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            try {
                const cacheType = await ensureProperCache(ctx);
                const isDemo = cacheType === 'demo';
                
                await messageManager.clear(ctx, false, true);
                await messageManager.sendCountriesKeyboard(ctx, isDemo);
                await ctx.answerCbQuery("✅ Возврат к странам");
                
            } catch (error) {
                console.error("❌ Ошибка возврата к странам:", error);
                try {
                    await ctx.answerCbQuery("❌ Ошибка возврата");
                } catch (e) {
                }
            } finally {
                releaseUserLock(userId);
            }
        });
    });
    
    bot.action("back_to_menu", async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 2000)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            try {
                await messageManager.clear(ctx);
                await messageManager.sendMainMenu(ctx);
                await ctx.answerCbQuery();
            } catch (error) {
                console.error("❌ Ошибка возврата в меню:", error);
            } finally {
                releaseUserLock(userId);
            }
        });
    });

    bot.action("filter_by_age", async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 2000)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            try {
                const chatId = ctx.chat.id;
                const countryKeyboardId = chatStorage.countryKeyboard.get(chatId);
                const cityKeyboardId = chatStorage.cityKeyboard.get(chatId);

                await messageManager.clear(ctx, true, true);
                if (countryKeyboardId) chatStorage.countryKeyboard.set(chatId, countryKeyboardId);
                if (cityKeyboardId) chatStorage.cityKeyboard.set(chatId, cityKeyboardId);

                const keyboard = AGE_RANGES.map((range) => [
                    { text: range.label, callback_data: `age_range_${range.label}` },
                ]);
                
                keyboard.push([{ text: "❌ Сбросить фильтр возраста", callback_data: "age_range_reset" }]);
                
                const hasFullAccess = await checkFullAccess(ctx);
                if (!hasFullAccess) {
                    keyboard.push([{ text: "💎 ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП", callback_data: "get_full_access" }]);
                }
                
                keyboard.push([{ text: "🔙 Назад в меню", callback_data: "back_to_menu" }]);

                const msg = await ctx.reply("Выберите возрастной диапазон:", { reply_markup: { inline_keyboard: keyboard } });
                messageManager.track(ctx.chat.id, msg.message_id);
                await ctx.answerCbQuery();
            } catch (error) {
                console.error("❌ Ошибка фильтра по возрасту:", error);
            } finally {
                releaseUserLock(userId);
            }
        });
    });

    bot.action(/^age_range_(.+)$/, async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 3000)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            try {
                const [_, range] = ctx.match;
                
                const cacheType = await ensureProperCache(ctx);
                const isDemo = cacheType === 'demo';
                
                ctx.session = ctx.session || {};
                ctx.session.profilesPage = 0;
                ctx.session.isDemo = isDemo;

                if (range === "reset") {
                    ctx.session.ageRange = null;
                    await ctx.answerCbQuery("✅ Фильтр по возрасту сброшен");
                } else {
                    const selectedRange = AGE_RANGES.find((r) => r.label === range);
                    if (selectedRange) {
                        ctx.session.ageRange = selectedRange;
                        await ctx.answerCbQuery(`✅ Установлен фильтр: ${range} лет`);
                    }
                }

                const currentCountry = ctx.session.filterCountry;
                const currentCity = ctx.session.filterCity;
                const chatId = ctx.chat.id;
                const countryKeyboardId = chatStorage.countryKeyboard.get(chatId);
                const cityKeyboardId = chatStorage.cityKeyboard.get(chatId);

                await messageManager.clear(ctx, true, true);
                if (countryKeyboardId) chatStorage.countryKeyboard.set(chatId, countryKeyboardId);
                if (cityKeyboardId) chatStorage.cityKeyboard.set(chatId, cityKeyboardId);

                const profiles = await getProfilesPage(0, currentCountry, ctx.session.ageRange, currentCity, isDemo);

                if (!profiles.length) {
                    const msg = await ctx.reply("Анкет по выбранным критериям не найдено.");
                    messageManager.track(ctx.chat.id, msg.message_id);
                    return;
                }

                let filtersText = "🎯 Применены фильтры: ";
                if (ctx.session.ageRange) filtersText += `Возраст: ${ctx.session.ageRange.label}`;
                if (currentCountry) filtersText += `, Страна: ${currentCountry}`;
                if (currentCity) filtersText += `, Город: ${currentCity}`;
                
                if (isDemo) {
                    filtersText += "\n👀 ДЕМО-РЕЖИМ: показано по 3 анкеты на город";
                }
                
                const filtersMsg = await ctx.reply(filtersText);
                messageManager.track(ctx.chat.id, filtersMsg.message_id);

                for (let i = 0; i < profiles.length; i++) {
                    const isLast = i === profiles.length - 1;
                    await sendProfile(ctx, profiles[i], 0, profiles.length, isLast, isDemo);
                    if (!isLast) await new Promise((resolve) => setTimeout(resolve, 300));
                }

                if (currentCountry && !currentCity) {
                    await messageManager.sendCitiesKeyboard(ctx, currentCountry, isDemo);
                } else {
                    await messageManager.sendMainMenu(ctx);
                }
            } catch (error) {
                console.error("❌ Ошибка обработки возрастного диапазона:", error);
                await ctx.answerCbQuery("❌ Ошибка применения фильтра");
            } finally {
                releaseUserLock(userId);
            }
        });
    });

    // ===================== УЛУЧШЕННАЯ ПРОВЕРКА ПОДПИСКИ НА КАНАЛ =====================
    bot.action("check_channel_subscription", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const userId = ctx.from.id;
                await ctx.answerCbQuery("🔍 Проверяем подписку на канал...");
                
                console.log(`🔍 [CHANNEL CHECK] Пользователь ${userId} проверяет подписку на канал`);
                
                // 🔥 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: ОЧИЩАЕМ ВСЕ КЭШИ
                console.log(`🔄 [CHANNEL CHECK] Очищаем кэши для ${userId}`);
                
                // Очищаем ВСЕ кэши
                subscriptionCache.del(`subscription:${userId}`);
                channelSubscriptionCache.del(`channel:${userId}`);
                userCacheStatus.del(`cache_status:${userId}`);
                
                // Очищаем сессию
                if (ctx.session) {
                    delete ctx.session.fullAccessCache;
                    delete ctx.session.cacheSession;
                    delete ctx.session.recentChannelCheck;
                }
                
                // 🔥 ПРОВЕРЯЕМ ПОДПИСКУ И КАНАЛ ЗАНОВО
                const hasSubscription = await checkSubscription(userId);
                const hasChannelSubscription = await checkChannelSubscription(ctx);
                const hasFullAccess = hasSubscription && hasChannelSubscription;
                
                console.log(`✅ [CHANNEL CHECK RESULT] ${userId}: подписка=${hasSubscription}, канал=${hasChannelSubscription}, полный доступ=${hasFullAccess}`);
                
                // 🔥 ЕСЛИ ПОЛНЫЙ ДОСТУП - ПОКАЗЫВАЕМ УСПЕХ И ПРЕДЛАГАЕМ ПЕРЕЙТИ К АНКЕТАМ
                if (hasFullAccess) {
                    console.log(`🎉 [CHANNEL CHECK SUCCESS] ${userId} получил полный доступ!`);
                    
                    // 🔥 ТАКЖЕ ОЧИЩАЕМ КЭШ ПОЛЬЗОВАТЕЛЯ (ВАЖНО!)
                    cacheManager.setUserCacheStatus(userId, 'full');
                    
                    // Обновляем сессию
                    if (!ctx.session) ctx.session = {};
                    ctx.session.fullAccessCache = {
                        value: true,
                        timestamp: Date.now(),
                        subscription: hasSubscription,
                        channel: hasChannelSubscription
                    };
                    
                    ctx.session.cacheSession = {
                        type: 'full',
                        timestamp: Date.now(),
                        fullAccess: true
                    };
                    
                    const successKeyboard = {
                        inline_keyboard: [
                            [
                                {
                                    text: "🌍 СМОТРЕТЬ АНКЕТЫ",
                                    callback_data: "all_countries_with_check",
                                },
                            ],
                            [
                                { text: "🔙 Назад", callback_data: "back_to_main" },
                                { text: "🧹 Очистить экран", callback_data: "clear_screen" },
                            ],
                        ],
                    };

                    await ctx.reply(
                        `
🎉 <b>ПОДПИСКА ПОДТВЕРЖДЕНА!</b>

✅ Все условия выполнены!
✨ Теперь у вас полный доступ ко всем анкетам!

<b>Что теперь доступно:</b>
• 👤 Все 70,000+ анкет
• 📞 Контакты профилей (телефоны, Telegram, WhatsApp)
• 🌍 Неограниченный поиск по странам и городам
• ⚡ Максимальная скорость работы

<em>Нажмите "СМОТРЕТЬ АНКЕТЫ" чтобы начать!</em>
                        `,
                        {
                            parse_mode: "HTML",
                            reply_markup: successKeyboard,
                        }
                    );
                    
                } else if (hasSubscription && !hasChannelSubscription) {
                    // 🔥 ЕСТЬ ПОДПИСКА, НО КАНАЛ ВСЕ ЕЩЕ НЕ НАЙДЕН
                    await ctx.reply(`
❌ <b>ПОДПИСКА НА КАНАЛ НЕ НАЙДЕНА</b>

✅ Подписка: <b>АКТИВНА</b>
❌ Канал: <b>НЕ ПОДПИСАН</b>

<em>Мы все еще не видим вашу подписку на @MagicYourClub</em>

<b>Что нужно сделать:</b>
1. Убедитесь, что вы подписались на канал @MagicYourClub
2. Если подписались - попробуйте выйти и зайти в канал
3. Проверьте, что используете тот же аккаунт Telegram
4. Подождите 1-2 минуты и проверьте снова

<code>Если проблема persists, напишите в поддержку @MagicAdd</code>
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
                                [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }],
                                [{ text: "🔄 Проверить подписку", callback_data: "check_subscription_now" }]
                            ]
                        }
                    });
                    
                } else if (!hasSubscription && hasChannelSubscription) {
                    // 🔥 ЕСТЬ КАНАЛ, НЕТ ПОДПИСКИ
                    await ctx.reply(`
✅ <b>Вы подписаны на канал!</b>

❌ <b>Но у вас нет активной подписки</b>

Что делать:
1. Нажмите "💎 КУПИТЬ ПОДПИСКУ" для оплаты
2. После оплаты проверьте доступ снова

<em>Без подписки доступ ограничен демо-режимом (3 анкеты на город)</em>
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }],
                                [{ text: "🔄 Проверить подписку", callback_data: "check_subscription_now" }],
                                [{ text: "🌍 ВСЕ СТРАНЫ (демо)", callback_data: "all_countries_with_check" }]
                            ]
                        }
                    });
                    
                } else {
                    // 🔥 НИЧЕГО НЕТ
                    await ctx.reply(`
❌ <b>ПОДПИСКА НА КАНАЛ НЕ НАЙДЕНА</b>

Мы не видим вашу подписку на @MagicYourClub
И у вас нет активной подписки

<b>Что нужно сделать:</b>
1. Нажмите "📢 ПОДПИСАТЬСЯ НА КАНАЛ"
2. После подписки нажмите "✅ Я ПОДПИСАЛСЯ" снова
3. Купите подписку для полного доступа

<em>Если вы уже подписаны, но бот не видит подписку:</em>
• Попробуйте выйти и зайти в канал
• Убедитесь, что вы используете тот же аккаунт Telegram
• Подождите 1-2 минуты и проверьте снова
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
                                [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }],
                                [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }]
                            ]
                        }
                    });
                }
                
            } catch (error) {
                console.error("Ошибка проверки подписки на канал:", error);
                await ctx.answerCbQuery("❌ Ошибка проверки");
            }
        });
    });

    // ===================== ПРОВЕРКА ТОЛЬКО ПОДПИСКИ =====================
    bot.action("check_subscription_now", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const userId = ctx.from.id;
                await ctx.answerCbQuery("🔍 Проверяем подписку...");
                
                console.log(`🔍 [SUBSCRIPTION CHECK] Пользователь ${userId} проверяет подписку`);
                
                // 🔥 ТАКЖЕ ОЧИЩАЕМ КЭШИ
                subscriptionCache.del(`subscription:${userId}`);
                
                const hasSubscription = await checkSubscription(userId);
                const hasChannelSubscription = await checkChannelSubscription(ctx);
                const hasFullAccess = hasSubscription && hasChannelSubscription;
                
                if (hasFullAccess) {
                    // 🔥 ОЧИЩАЕМ ВСЕ КЭШИ И ОБНОВЛЯЕМ СТАТУС
                    channelSubscriptionCache.del(`channel:${userId}`);
                    userCacheStatus.del(`cache_status:${userId}`);
                    cacheManager.setUserCacheStatus(userId, 'full');
                    
                    await ctx.reply(`
🎉 <b>ВСЕ УСЛОВИЯ ВЫПОЛНЕНЫ!</b>

✅ Подписка: <b>АКТИВНА</b>
✅ Канал: <b>ПОДПИСАН</b>

✨ Теперь у вас полный доступ ко всем анкетам!

<em>Нажмите "🌍 ВСЕ СТРАНЫ" для начала поиска</em>
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
                                [{ text: "🔄 Проверить еще раз", callback_data: "check_all_access" }]
                            ]
                        }
                    });
                } else if (hasSubscription) {
                    await ctx.reply(`
✅ <b>ПОДПИСКА АКТИВНА!</b>

❌ <b>Но нет подписки на канал @MagicYourClub</b>

Что нужно сделать:
1. Подпишитесь на @MagicYourClub
2. Нажмите "✅ Я ПОДПИСАЛСЯ НА КАНАЛ"
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
                                [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }]
                            ]
                        }
                    });
                } else {
                    await ctx.reply(`
❌ <b>ПОДПИСКА НЕ НАЙДЕНА</b>

У вас нет активной подписки.

<b>Что нужно сделать:</b>
1. Нажмите "💎 КУПИТЬ ПОДПИСКУ" для оплаты
2. После оплаты проверьте снова
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }],
                                [{ text: "🔄 Проверить снова", callback_data: "check_subscription_now" }]
                            ]
                        }
                    });
                }
                
            } catch (error) {
                console.error("Ошибка проверки подписки:", error);
                await ctx.answerCbQuery("❌ Ошибка проверки");
            }
        });
    });

    bot.action("force_refresh_access", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const userId = ctx.from.id;
                await ctx.answerCbQuery("🔄 Принудительное обновление...");
                
                console.log(`🔄 [FORCE REFRESH] Полный сброс кэшей для ${userId}`);
                
                // Очищаем ВСЕ кэши пользователя
                userCacheStatus.del(`cache_status:${userId}`);
                subscriptionCache.del(`subscription:${userId}`);
                channelSubscriptionCache.del(`channel:${userId}`);
                
                // Очищаем сессию
                if (ctx.session) {
                    ctx.session = {};
                }
                
                // Проверяем заново
                const [hasSubscription, hasChannelSubscription] = await Promise.all([
                    checkSubscription(userId),
                    checkChannelSubscription(ctx)
                ]);
                
                // Устанавливаем правильный статус кэша
                if (hasSubscription && hasChannelSubscription) {
                    cacheManager.setUserCacheStatus(userId, 'full');
                    
                    await ctx.reply(`
🔄 <b>СТАТУС ОБНОВЛЕН!</b>

✅ Подписка: ${hasSubscription ? 'АКТИВНА' : 'НЕ АКТИВНА'}
✅ Канал: ${hasChannelSubscription ? 'ПОДПИСАН' : 'НЕ ПОДПИСАН'}

${hasSubscription && hasChannelSubscription ? 
                            '🎉 <b>Теперь у вас полный доступ!</b>\nНажмите "🌍 ВСЕ СТРАНЫ"' : 
                            '❌ <b>Не все условия выполнены</b>\nПроверьте что отсутствует выше'}
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
                                [{ text: "🔄 Проверить еще раз", callback_data: "check_all_access" }]
                            ]
                        }
                    });
                }
                
            } catch (error) {
                console.error("Ошибка принудительного обновления:", error);
                await ctx.answerCbQuery("❌ Ошибка обновления");
            }
        });
    });

    // ===================== ПРОВЕРКА ВСЕХ УСЛОВИЙ =====================
    bot.action("check_all_access", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const userId = ctx.from.id;
                await ctx.answerCbQuery("🔍 Проверяем все условия...");
                
                console.log(`🔍 [FULL CHECK] Полная проверка доступа для ${userId}`);
                
                // Очищаем ВСЕ кэши
                subscriptionCache.del(`subscription:${userId}`);
                channelSubscriptionCache.del(`channel:${userId}`);
                
                const [hasSubscription, hasChannelSubscription] = await Promise.all([
                    checkSubscription(userId),
                    checkChannelSubscription(ctx)
                ]);
                
                if (hasSubscription && hasChannelSubscription) {
                    // ОБНОВЛЯЕМ СТАТУС В КЭШЕ ПОЛЬЗОВАТЕЛЯ
                    cacheManager.setUserCacheStatus(userId, 'full');
                    
                    // Очищаем сессию
                    if (ctx.session) {
                        ctx.session.cacheType = 'full';
                        ctx.session.hasFullAccess = true;
                    }
                    
                    await ctx.reply(`
🎉 <b>ВСЕ УСЛОВИЯ ВЫПОЛНЕНЫ!</b>

✅ Подписка: АКТИВНА
✅ Канал: ПОДПИСАН

<b>Полный доступ активирован!</b>

Теперь вы можете:
• 🌍 Просматривать все страны
• 👤 Видеть все контакты в анкетах
• 🚀 Использовать полный функционал

<code>Нажмите "🌍 ВСЕ СТРАНЫ" чтобы начать</code>
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                [{ text: "🌍 ВСЕ СТРАНЫ", callback_data: "all_countries_with_check" }],
                                [{ text: "🧹 Очистить экран", callback_data: "clear_screen" }]
                            ]
                        }
                    });
                    
                } else {
                    let missing = [];
                    if (!hasSubscription) missing.push("активной подписки");
                    if (!hasChannelSubscription) missing.push("подписки на канал @MagicYourClub");
                    
                    await ctx.reply(`
❌ <b>УСЛОВИЯ НЕ ВЫПОЛНЕНЫ</b>

Отсутствует: ${missing.join(" и ")}

<b>Что нужно сделать:</b>
${!hasSubscription ? "1. 💎 Купите подписку через 'ПОЛУЧИТЬ ПОЛНЫЙ ДОСТУП'\n" : ""}
${!hasChannelSubscription ? "2. 📢 Подпишитесь на канал @MagicYourClub\n" : ""}
3. 🔄 Проверьте снова после выполнения условий
                    `, {
                        parse_mode: "HTML",
                        reply_markup: {
                            inline_keyboard: [
                                !hasSubscription ? [{ text: "💎 КУПИТЬ ПОДПИСКУ", callback_data: "get_full_access" }] : [],
                                !hasChannelSubscription ? [{ text: "📢 ПОДПИСАТЬСЯ НА КАНАЛ", url: "https://t.me/MagicYourClub" }] : [],
                                [{ text: "✅ Я ПОДПИСАЛСЯ НА КАНАЛ", callback_data: "check_channel_subscription" }],
                                [{ text: "🔄 Проверить снова", callback_data: "check_all_access" }]
                            ].filter(row => row.length > 0)
                        }
                    });
                }
                
            } catch (error) {
                console.error("Ошибка полной проверки:", error);
                await ctx.answerCbQuery("❌ Ошибка проверки");
            }
        });
    });

    bot.action(/^page_(first|prev|next|last|\d+)_(\d+)$/, async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 2500)) {
            console.log(`⏳ [LOCK] Пользователь ${userId} уже выполняет действие, игнорируем клик`);
            try {
                await ctx.answerCbQuery("⏳ Подождите, загружаем...");
            } catch (e) {
            }
            return;
        }
        
        await messageQueue.add(async () => {
            try {
                const [_, action, currentPage] = ctx.match;
                let newPage = parseInt(currentPage);

                if (action === "first") newPage = 0;
                else if (action === "prev") newPage = Math.max(0, newPage - 1);
                else if (action === "next") newPage = newPage + 1;
                else if (action === "last") {
                    const isDemo = ctx.session?.isDemo || false;
                    const filterKey = `country:${ctx.session.filterCountry || 'all'}:age:${ctx.session.ageRange?.label || 'all'}:city:${ctx.session.filterCity || 'all'}`;
                    const filteredProfiles = cacheManager.getGlobalFilter(filterKey, isDemo);
                    newPage = Math.ceil((filteredProfiles?.length || 0) / SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE) - 1;
                } else {
                    newPage = parseInt(action);
                }

                await messageManager.clear(ctx, true);
                
                ctx.session = ctx.session || {};
                const isDemo = ctx.session.isDemo || false;
                const profiles = await getProfilesPage(newPage, ctx.session.filterCountry, ctx.session.ageRange, ctx.session.filterCity, isDemo);

                if (profiles.length) {
                    ctx.session.profilesPage = newPage;

                    for (let i = 0; i < profiles.length; i++) {
                        const isLast = i === profiles.length - 1;
                        await sendProfile(ctx, profiles[i], newPage, profiles.length, isLast, isDemo);
                        if (!isLast) await new Promise((resolve) => setTimeout(resolve, 300));
                    }
                    
                    await ctx.answerCbQuery(`📄 Страница ${newPage + 1}`);
                } else {
                    const msg = await ctx.reply("Больше анкет нет");
                    messageManager.track(ctx.chat.id, msg.message_id);
                    await ctx.answerCbQuery("❌ Больше анкет нет");
                }
                
            } catch (error) {
                console.error("❌ Ошибка пагинации:", error);
                try {
                    await ctx.answerCbQuery("❌ Ошибка загрузки");
                } catch (e) {
                }
            } finally {
                releaseUserLock(userId);
            }
        });
    });

    bot.action("clear_screen", async (ctx) => {
        const userId = ctx.from.id;
        
        if (!acquireUserLock(userId, 2000)) {
            await ctx.answerCbQuery("⏳ Подождите, обрабатываем предыдущий запрос...");
            return;
        }
        
        await messageQueue.add(async () => {
            try {
                await messageManager.clear(ctx);
                await ctx.answerCbQuery("Экран очищен");
            } catch (error) {
                console.error("❌ Ошибка очистки:", error);
                await ctx.answerCbQuery("Ошибка при очистке");
            } finally {
                releaseUserLock(userId);
            }
        });
    });

    // ===================== ФУНКЦИЯ ОТПРАВКИ ПРОФИЛЯ =====================
    const sendProfile = async (ctx, profile, page, total, isLast, isDemo = false) => {
        return messageQueue.add(async () => {
            try {
                const fullProfile = {
                    id: profile.id,
                    name: profile.n || profile.name,
                    age: profile.a || profile.age, 
                    country: profile.c || profile.country,
                    city: profile.ct || profile.city,
                    about: profile.ab || profile.about,
                    photoUrl: profile.p || profile.photoUrl,
                    photos: profile.phs || profile.photos || [],
                    telegram: profile.tg || profile.telegram,
                    phone: profile.tel || profile.phone,
                    whatsapp: profile.wa || profile.whatsapp,
                    createdAt: profile.ca || profile.createdAt,
                    isDemo: profile.isDemo
                };

                console.log(`🔍 [DEBUG] Профиль ${fullProfile.name}: фото URL=${fullProfile.photoUrl}, галерея=${fullProfile.photos?.length || 0} фото`);

                const cleanText = (text) => {
                    if (!text) return "";
                    return text.replace(/[^\x00-\x7F\u0400-\u04FF\u0500-\u052F\u2DE0-\u2DFF\uA640-\uA69F]/g, '')
                              .replace(/[^\w\s\u0400-\u04FF\u0500-\u052F\u2DE0-\u2DFF\uA640-\uA69F.,!?;:()\-+=\[\]{}@#$%^&*<>\/\\|'"`~]/g, '')
                              .trim();
                };

                const about = cleanText(fullProfile.about)?.length > SCALING_CONFIG.PERFORMANCE.MAX_CAPTION_LENGTH
                    ? cleanText(fullProfile.about).substring(0, SCALING_CONFIG.PERFORMANCE.MAX_CAPTION_LENGTH - 3) + "..."
                    : cleanText(fullProfile.about) || "";

                if (isDemo || fullProfile.isDemo) {
                    const demoCaption = `
👤 <b>${cleanText(fullProfile.name)}</b>, ${fullProfile.age}
-------------------------------
${cleanText(fullProfile.country)},📍${cleanText(fullProfile.city)}
-------------------------------
<em>${about.length > 300 ? about.substring(0, 300) + `...<a href="http://t.me/magicboss_bot/magic">читать полностью в ✨Magic</a>` : about}</em>
🔹🔹🔹🔹🔹🔹🔹🔹🔹🔹
🚫 <b>КОНТАКТЫ СКРЫТЫ</b>
-------------------------------
💎 <b>Для получения контактов и полного доступа ко всем анкетам необходимо:</b>

✅ <b>1. Активная подписка</b>
✅ <b>2. Подписка на канал @MagicYourClub</b>

✨ После получения доступа вы увидите:
• Все контакты профилей (Telegram, WhatsApp, телефон)
• Полные описания анкет
• Неограниченный доступ ко всем анкетам
• Максимальную скорость работы

-------------------------------
<a href="http://t.me/magicboss_bot/magic"><b>✨Magic WebApp</b></a>
`.trim();

                    let keyboard = [];
                    if (isLast) {
                        const filterKey = `country:${ctx.session.filterCountry || 'all'}:age:${ctx.session.ageRange?.label || 'all'}:city:${ctx.session.filterCity || 'all'}`;
                        const filteredProfiles = cacheManager.getGlobalFilter(filterKey, isDemo);
                        const totalPages = Math.ceil((filteredProfiles?.length || 0) / SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE);
                        
                        const currentFilters = {
                            country: ctx.session?.displayCountry,
                            city: ctx.session?.filterCity,
                            ageRange: ctx.session?.ageRange
                        };
                        
                        keyboard = createEnhancedPaginationKeyboard(page, totalPages, filterKey, currentFilters, isDemo);

                        keyboard.push(
                            [{ text: "🎂 Фильтр по возрасту", callback_data: "filter_by_age" }],
                            [{ text: "🌍 Все страны", callback_data: "all_countries_with_check" }],
                            [{ text: "🧹 Очистить экран", callback_data: "clear_screen" }]
                        );
                    }

                    let photosToSend = [];
                    const seenUrls = new Set();

                    if (fullProfile.photoUrl && typeof fullProfile.photoUrl === 'string' && fullProfile.photoUrl.trim() !== '') {
                        try {
                            const urlObj = new URL(fullProfile.photoUrl.trim());
                            const cleanUrl = urlObj.href;
                            if ((urlObj.protocol === 'http:' || urlObj.protocol === 'https:') && !seenUrls.has(cleanUrl)) {
                                seenUrls.add(cleanUrl);
                                photosToSend.push(cleanUrl);
                            }
                        } catch (e) {
                            console.log(`❌ Ошибка обработки основного фото:`, e.message);
                        }
                    }

                    if (Array.isArray(fullProfile.photos) && fullProfile.photos.length > 0) {
                        fullProfile.photos.forEach((url, index) => {
                            if (typeof url === 'string' && url.trim() !== '') {
                                try {
                                    const urlObj = new URL(url.trim());
                                    const cleanUrl = urlObj.href;
                                    if ((urlObj.protocol === 'http:' || urlObj.protocol === 'https:') && !seenUrls.has(cleanUrl)) {
                                        seenUrls.add(cleanUrl);
                                        photosToSend.push(cleanUrl);
                                    }
                                } catch (e) {
                                    console.log(`❌ Ошибка обработки фото ${index + 1}:`, e.message);
                                }
                            }
                        });
                    }

                    photosToSend = photosToSend.slice(0, 10);
                    
                    console.log(`📸 [DEMO PHOTO] Уникальные фото для ${fullProfile.name}: ${photosToSend.length} (из ${fullProfile.photos?.length || 0} в галерее)`);

                    const sendPhotoSafely = async (photoUrl, photoNumber, totalPhotos) => {
                        try {
                            const emojiNumbers = ['1️⃣', '2️⃣', '3️⃣', '4️⃣', '5️⃣', '6️⃣', '7️⃣', '8️⃣', '9️⃣', '🔟'];
                            const numberEmoji = photoNumber <= 10 ? emojiNumbers[photoNumber - 1] : `${photoNumber}.`;
                            const photoCaption = `${numberEmoji} Фото ${photoNumber}/${totalPhotos}`;
                            
                            return await ctx.replyWithPhoto(photoUrl, { 
                                caption: photoCaption, 
                                parse_mode: "HTML" 
                            });
                        } catch (error) {
                            console.log(`❌ Ошибка отправки фото ${photoNumber}:`, error.message);
                            
                            try {
                                return await ctx.replyWithPhoto(photoUrl);
                            } catch (e) {
                                console.log(`❌ Не удалось отправить фото ${photoNumber} даже без caption:`, e.message);
                                return null;
                            }
                        }
                    };

                    let infoMessage = null;
                    
                    if (photosToSend.length > 0) {
                        const profileInfo = `✨✨✨✨✨✨✨✨✨✨ \n <a href="http://t.me/MagicYourClub"><b>Новые анкеты в нашем ➡️ канале</b></a>\n\n`;
                        infoMessage = await ctx.reply(profileInfo, { parse_mode: "HTML" });
                        messageManager.track(ctx.chat.id, infoMessage.message_id);
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }

                    const sentPhotoMessages = [];
                    
                    if (photosToSend.length === 0) {
                        console.log(`📭 [DEMO PHOTO] Нет валидных фото для ${fullProfile.name}, отправляем только текст`);
                        const msg = await ctx.reply(demoCaption, {
                            parse_mode: "HTML",
                            reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
                        });
                        messageManager.track(ctx.chat.id, msg.message_id);
                        return msg;
                    }
                    else {
                        for (let i = 0; i < photosToSend.length; i++) {
                            const photoUrl = photosToSend[i];
                            const photoNumber = i + 1;
                            const totalPhotos = photosToSend.length;
                            
                            console.log(`🔄 [PHOTO] Отправляем фото ${photoNumber}/${totalPhotos} для ${fullProfile.name}`);
                            
                            const photoMsg = await sendPhotoSafely(photoUrl, photoNumber, totalPhotos);
                            if (photoMsg) {
                                sentPhotoMessages.push(photoMsg);
                                messageManager.track(ctx.chat.id, photoMsg.message_id);
                                
                                if (i < photosToSend.length - 1) {
                                    await new Promise(resolve => setTimeout(resolve, 800));
                                }
                            }
                        }
                        
                        if (sentPhotoMessages.length === 0) {
                            console.log(`⚠️ [DEMO PHOTO] Все фото не удалось отправить для ${fullProfile.name}`);
                            const fallbackMsg = await ctx.reply(`📷 [Все фото недоступны]\n\n${demoCaption}`, { 
                                parse_mode: "HTML",
                                reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
                            });
                            messageManager.track(ctx.chat.id, fallbackMsg.message_id);
                            return fallbackMsg;
                        }
                    }

                    await new Promise(resolve => setTimeout(resolve, 500));

                    const textMsg = await ctx.reply(demoCaption, {
                        parse_mode: "HTML",
                        reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
                    });

                    messageManager.track(ctx.chat.id, textMsg.message_id);
                    console.log(`✅ [DEMO PROFILE] Анкета ${fullProfile.name} отправлена: ${sentPhotoMessages.length} фото + текст`);

                    return textMsg;

                } else {
                    const formatTelegram = (username) => {
                        if (!username) return "";
                        const cleanUsername = cleanText(username);
                        if (/^[0-9+\-() ]+$/.test(cleanUsername)) {
                            const cleanDigits = cleanUsername.replace(/[^0-9]/g, "");
                            if (cleanDigits.startsWith('7') || cleanDigits.startsWith('8') || (cleanDigits.length >= 10 && !cleanDigits.startsWith('1'))) {
                                let telegramNumber = cleanDigits;
                                if (telegramNumber.startsWith('7') && telegramNumber.length === 11) telegramNumber = telegramNumber.substring(1);
                                else if (telegramNumber.startsWith('8') && telegramNumber.length === 11) telegramNumber = telegramNumber.substring(1);
                                return `🔵 <a href="https://t.me/${telegramNumber}">Telegram</a>`;
                            }
                        }
                        if (cleanUsername.startsWith("https://t.me/")) {
                            const cleaned = decodeURIComponent(cleanUsername).replace("https://t.me/", "").replace(/^%40/, "@").replace(/^\+/, "");
                            return `🔵 <a href="https://t.me/${cleaned}">Telegram</a>`;
                        }
                        const cleaned = cleanUsername.replace(/^[@+]/, "");
                        return `🔵 <a href="https://t.me/${cleaned}">Telegram</a>`;
                    };

                    const formatWhatsApp = (url) => {
                        if (!url) return "";
                        const cleanUrl = cleanText(url);
                        if (/^[0-9+\-() ]+$/.test(cleanUrl)) {
                            let cleanDigits = cleanUrl.replace(/[^0-9]/g, "");
                            if (cleanDigits.startsWith('8') && cleanDigits.length === 11) cleanDigits = '7' + cleanDigits.substring(1);
                            else if (cleanDigits.length === 10) cleanDigits = '7' + cleanDigits;
                            if (cleanDigits.length === 11 && cleanDigits.startsWith('7')) return `🟢 <a href="https://wa.me/${cleanDigits}">WhatsApp</a>`;
                        }
                        return `🟢 <a href="${cleanUrl}">WhatsApp</a>`;
                    };

                    const formatPhone = (phone) => {
                        if (!phone) return "";
                        let cleanDigits = cleanText(phone).replace(/[^0-9]/g, "");
                        if (!cleanDigits) return "";
                        let formattedPhone = cleanText(phone);
                        if (cleanDigits.length === 11 || cleanDigits.length === 10) {
                            if (cleanDigits.startsWith('7') && cleanDigits.length === 11) formattedPhone = `+${cleanDigits}`;
                            else if (cleanDigits.startsWith('8') && cleanDigits.length === 11) formattedPhone = `+7${cleanDigits.substring(1)}`;
                            else if (cleanDigits.length === 10) formattedPhone = `+7${cleanDigits}`;
                        }
                        return `📞 ${formattedPhone}`;
                    };

                    const fullCaption = `
👤 <b>${cleanText(fullProfile.name)}</b>, ${fullProfile.age}
-------------------------------
${cleanText(fullProfile.country)},📍${cleanText(fullProfile.city)}
-------------------------------
<em>${about.length > 300 ? about.substring(0, 300) + `...<a href="http://t.me/magicboss_bot/magic">читать полностью в ✨Magic</a>` : about}</em>
🔹🔹🔹🔹🔹🔹🔹🔹🔹🔹
<b>Контакты:</b>
-------------------------------
${fullProfile.phone ? formatPhone(fullProfile.phone) : ""}${fullProfile.telegram ? "\n-------------------------------\n" + formatTelegram(fullProfile.telegram) : ""}${fullProfile.whatsapp ? "\n-------------------------------\n" + formatWhatsApp(fullProfile.whatsapp) : ""}${(fullProfile.phone || fullProfile.telegram || fullProfile.whatsapp) ? "\n-------------------------------" : ""}
⚠️ <b>ЕСЛИ КТО-ТО ПРОСИТ: Криптовалюту наперед, деньги на такси🚕 или дорогу, предоплату любым способом, переводы на карты💳 или электронные кошельки, чеки или подтверждения оплаты</b>
🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 🔥 
<b>ЭТО 100% МОШЕННИКИ!
НИ В КОЕМ СЛУЧАЕ НЕ ОТПРАВЛЯЙТЕ ПРЕДОПЛАТУ  🛑 ВАС ОБМАНУТ!</b>
-------------------------------
<a href="http://t.me/magicboss_bot/magic"><b>✨Magic WebApp</b></a>
`.trim();

                    let keyboard = [];
                    if (isLast) {
                        const filterKey = `country:${ctx.session.filterCountry || 'all'}:age:${ctx.session.ageRange?.label || 'all'}:city:${ctx.session.filterCity || 'all'}`;
                        const filteredProfiles = cacheManager.getGlobalFilter(filterKey, false);
                        const totalPages = Math.ceil((filteredProfiles?.length || 0) / SCALING_CONFIG.PERFORMANCE.PROFILES_PER_PAGE);
                        
                        const currentFilters = {
                            country: ctx.session?.displayCountry,
                            city: ctx.session?.filterCity,
                            ageRange: ctx.session?.ageRange
                        };
                        
                        keyboard = createEnhancedPaginationKeyboard(page, totalPages, filterKey, currentFilters);

                        keyboard.push(
                            [{ text: "🎂 Фильтр по возрасту", callback_data: "filter_by_age" }],
                            [{ text: "🌍 Все страны", callback_data: "all_countries_with_check" }],
                            [{ text: "🧹 Очистить экран", callback_data: "clear_screen" }]
                        );
                    }

                    let photosToSend = [];
                    const seenUrls = new Set();

                    if (fullProfile.photoUrl && typeof fullProfile.photoUrl === 'string' && fullProfile.photoUrl.trim() !== '') {
                        try {
                            const urlObj = new URL(fullProfile.photoUrl.trim());
                            const cleanUrl = urlObj.href;
                            if ((urlObj.protocol === 'http:' || urlObj.protocol === 'https:') && !seenUrls.has(cleanUrl)) {
                                seenUrls.add(cleanUrl);
                                photosToSend.push(cleanUrl);
                            }
                        } catch (e) {
                            console.log(`❌ Ошибка обработки основного фото:`, e.message);
                        }
                    }

                    if (Array.isArray(fullProfile.photos) && fullProfile.photos.length > 0) {
                        fullProfile.photos.forEach((url, index) => {
                            if (typeof url === 'string' && url.trim() !== '') {
                                try {
                                    const urlObj = new URL(url.trim());
                                    const cleanUrl = urlObj.href;
                                    if ((urlObj.protocol === 'http:' || urlObj.protocol === 'https:') && !seenUrls.has(cleanUrl)) {
                                        seenUrls.add(cleanUrl);
                                        photosToSend.push(cleanUrl);
                                    }
                                } catch (e) {
                                    console.log(`❌ Ошибка обработки фото ${index + 1}:`, e.message);
                                }
                            }
                        });
                    }

                    photosToSend = photosToSend.slice(0, 10);
                    
                    console.log(`📸 [PHOTO] Уникальные фото для ${fullProfile.name}: ${photosToSend.length} (из ${fullProfile.photos?.length || 0} в галерее)`);

                    const sendPhotoSafely = async (photoUrl, photoNumber, totalPhotos) => {
                        try {
                            const emojiNumbers = ['1️⃣', '2️⃣', '3️⃣', '4️⃣', '5️⃣', '6️⃣', '7️⃣', '8️⃣', '9️⃣', '🔟'];
                            const numberEmoji = photoNumber <= 10 ? emojiNumbers[photoNumber - 1] : `${photoNumber}.`;
                            const photoCaption = `${numberEmoji} Фото ${photoNumber}/${totalPhotos}`;
                            
                            return await ctx.replyWithPhoto(photoUrl, { 
                                caption: photoCaption, 
                                parse_mode: "HTML" 
                            });
                        } catch (error) {
                            console.log(`❌ Ошибка отправки фото ${photoNumber}:`, error.message);
                            
                            try {
                                return await ctx.replyWithPhoto(photoUrl);
                            } catch (e) {
                                console.log(`❌ Не удалось отправить фото ${photoNumber} даже без caption:`, e.message);
                                return null;
                            }
                        }
                    };

                    let infoMessage = null;
                    
                    if (photosToSend.length > 0) {
                        const profileInfo = `✨✨✨✨✨✨✨✨✨✨ \n <a href="http://t.me/MagicYourClub"><b>Новые анкеты в нашем ➡️ канале</b></a>\n\n`;
                        infoMessage = await ctx.reply(profileInfo, { parse_mode: "HTML" });
                        messageManager.track(ctx.chat.id, infoMessage.message_id);
                        await new Promise(resolve => setTimeout(resolve, 500));
                    }

                    const sentPhotoMessages = [];
                    
                    if (photosToSend.length === 0) {
                        console.log(`📭 [PHOTO] Нет валидных фото для ${fullProfile.name}, отправляем только текст`);
                        const msg = await ctx.reply(fullCaption, {
                            parse_mode: "HTML",
                            reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
                        });
                        messageManager.track(ctx.chat.id, msg.message_id);
                        return msg;
                    }
                    else {
                        for (let i = 0; i < photosToSend.length; i++) {
                            const photoUrl = photosToSend[i];
                            const photoNumber = i + 1;
                            const totalPhotos = photosToSend.length;
                            
                            console.log(`🔄 [PHOTO] Отправляем фото ${photoNumber}/${totalPhotos} для ${fullProfile.name}`);
                            
                            const photoMsg = await sendPhotoSafely(photoUrl, photoNumber, totalPhotos);
                            if (photoMsg) {
                                sentPhotoMessages.push(photoMsg);
                                messageManager.track(ctx.chat.id, photoMsg.message_id);
                                
                                if (i < photosToSend.length - 1) {
                                    await new Promise(resolve => setTimeout(resolve, 800));
                                }
                            }
                        }
                        
                        if (sentPhotoMessages.length === 0) {
                            console.log(`⚠️ [PHOTO] Все фото не удалось отправить для ${fullProfile.name}`);
                            const fallbackMsg = await ctx.reply(`📷 [Все фото недоступны]\n\n${fullCaption}`, { 
                                parse_mode: "HTML",
                                reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
                            });
                            messageManager.track(ctx.chat.id, fallbackMsg.message_id);
                            return fallbackMsg;
                        }
                    }

                    await new Promise(resolve => setTimeout(resolve, 500));

                    const textMsg = await ctx.reply(fullCaption, {
                        parse_mode: "HTML",
                        reply_markup: keyboard.length > 0 ? { inline_keyboard: keyboard } : undefined,
                    });

                    messageManager.track(ctx.chat.id, textMsg.message_id);
                    console.log(`✅ [PROFILE] Анкета ${fullProfile.name} отправлена: ${sentPhotoMessages.length} фото + текст`);

                    return textMsg;
                }

            } catch (error) {
                console.error("❌ Критическая ошибка отправки анкеты:", error);
                try {
                    const fallbackText = `👤 ${fullProfile.name}, ${fullProfile.age}\n📍 ${fullProfile.city}, ${fullProfile.country}\n\n${fullProfile.about || 'Описание недоступно'}\n\n⚠️ Приносим извинения, возникли технические проблемы с отображением фото.`;
                    const msg = await ctx.reply(fallbackText, { parse_mode: "HTML" });
                    messageManager.track(ctx.chat.id, msg.message_id);
                    return msg;
                } catch (finalError) {
                    console.error("💥 Не удалось отправить даже текстовое сообщение:", finalError);
                    return null;
                }
            }
        });
    };

    bot.command("stats", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                const stats = readingStats.getStats();
                const cacheStats = cacheManager.getGlobalCacheStats();
                
                const statsMessage = `
📊 **Статистика системы**

**Операции чтения:**
• Всего операций: ${stats.totalReads}
• Firestore чтений: ${stats.firestoreReads}
• Кэш попадания: ${stats.operations.cacheHits}
• Кэш промахи: ${stats.operations.cacheMisses}
• Эффективность кэша: ${stats.cacheEfficiency}

**Детализация операций:**
• Профили: ${stats.operations.profiles}
• Подписки: ${stats.operations.subscriptions}
• Проверки канала: ${stats.operations.channelSubscriptions}
• Страны: ${stats.operations.countries}
• Города: ${stats.operations.cities}

**Пользователи:**
• Уникальные: ${stats.uniqueUsers}
• Операций на пользователя: ${stats.readsPerUser.toFixed(2)}

**Глобальный кэш:**
• Полный кэш загружен: ${cacheStats.fullCache.loaded ? '✅' : '❌'}
• Демо кэш загружен: ${cacheStats.demoCache.loaded ? '✅' : '❌'}
• Полных профилей: ${cacheStats.fullCache.profilesCount}
• Демо профилей: ${cacheStats.demoCache.profilesCount}
• Полных фильтров: ${cacheStats.filterCacheKeys}
• Демо фильтров: ${cacheStats.demoFilterCacheKeys}

**Индивидуальные кэши:**
• Подписок в кэше: ${cacheStats.subscriptionCacheCount}
• Проверок канала в кэше: ${cacheStats.channelCacheCount}
• Статусов пользователей: ${cacheStats.userStatusCacheCount}

**Очередь:**
• Активные задачи: ${messageQueue.pending}
• Задачи в ожидании: ${messageQueue.size}
                `;
                
                const msg = await ctx.reply(statsMessage, { parse_mode: "Markdown" });
                messageManager.track(ctx.chat.id, msg.message_id);
                
            } catch (error) {
                console.error("❌ Ошибка команды stats:", error);
            }
        });
    });

    bot.command("reset_stats", async (ctx) => {
        await messageQueue.add(async () => {
            try {
                readingStats.resetStats();
                const msg = await ctx.reply("✅ Статистика чтений сброшена");
                messageManager.track(ctx.chat.id, msg.message_id);
            } catch (error) {
                console.error("❌ Ошибка сброса статистики:", error);
            }
        });
    });

    // Экспорт функций
    module.exports.ensureProperCache = ensureProperCache;
    module.exports.checkFullAccess = checkFullAccess;
    module.exports.checkSubscription = checkSubscription;
    module.exports.checkChannelSubscription = checkChannelSubscription;

    // Инициализация глобальных кэшей при старте бота
    setTimeout(async () => {
        if (!globalCacheInitialized) {
            console.log('🚀 [BOT START] Инициализация глобальных кэшей...');
            try {
                // ИНИЦИАЛИЗИРУЕМ ГЛОБАЛЬНЫЕ КЭШИ
                await cacheManager.initializeGlobalCaches(db);
                
                // Периодическое обновление глобальных кэшей (раз в 7 дней)
                setInterval(async () => {
                    const randomDelay = Math.random() * 30 * 60 * 1000;
                    await new Promise(resolve => setTimeout(resolve, randomDelay));
                    
                    console.log('🔄 [GLOBAL CACHE UPDATE] Периодическое обновление глобальных кэшей...');
                    try {
                        // Обновляем демо-кэш
                        await cacheManager.loadGlobalDemoCache(db);
                        
                        // Если полный кэш загружен, обновляем и его
                        if (cacheManager.isGlobalFullCacheLoaded()) {
                            console.log('🔄 [GLOBAL CACHE UPDATE] Обновление глобального полного кэша...');
                            await cacheManager.loadGlobalFullCache(db);
                        }
                    } catch (error) {
                        console.error('❌ [GLOBAL CACHE UPDATE] Ошибка обновления кэшей:', error);
                    }
                }, 7 * 24 * 60 * 60 * 1000);
                
            } catch (error) {
                console.error('❌ [BOT START] Ошибка инициализации глобальных кэшей:', error);
            }
        }
    }, 3000);

    console.log(`✅ Модуль профилей инициализирован с ГЛОБАЛЬНЫМ КЭШЕМ`);
    console.log(`📊 Глобальный демо-кэш: загружается при старте бота`);
    console.log(`📊 Глобальный полный кэш: загружается при первом пользователе с подпиской`);
    console.log(`📊 Кэш подписок: 24 часа (индивидуальный)`);
    console.log(`📊 Кэш проверки канала: 5 минут (индивидуальный)`);
    console.log(`📊 ОДНА загрузка для всех пользователей!`);
    
    // Функция для загрузки полного кэша после оплаты (для интеграции с payments.js)
    module.exports.loadFullCacheAfterPayment = async (userId) => {
        console.log(`🚀 [AFTER PAYMENT] Пользователь ${userId} оплатил подписку, проверяем глобальный полный кэш`);
        
        if (!cacheManager.isGlobalFullCacheLoaded()) {
            console.log(`🔄 [AFTER PAYMENT] Загружаем глобальный полный кэш для ${userId}`);
            await cacheManager.lazyLoadGlobalFullCache(db, userId);
        } else {
            console.log(`✅ [AFTER PAYMENT] Глобальный полный кэш уже загружен`);
        }
    };
};